# 哪吒外卖 — 合规变更记录 (Compliance CHANGELOG)

> 每次改动合规相关机制(风控、资金流、数据保护、退款、留存)都在此记一笔。
> 格式: 日期 | 变更 | 等级 | 批准人 | 说明。等级定义见 `../INVARIANTS.md`。

| 日期 | 变更 | 等级 | 批准人 | 说明 |
|---|---|---|---|---|
| 2026-06-16 | 下单风控阈值调整（启用当日按客单价/客服打通微调） | 🟡 L2（业务参数 · 含"真实影响"拒单线调整） | 用户（逐项拍板：大额$1000 / 频次10min8·24h20 / 拒单线抬$2000 / 客服打通） | 在同日启用基础上调整（均改 `business_settings`、不改引擎代码；前端为展示层改动）：① **大额审核线** `nezha_risk_large_amount_threshold` 32000→**368120֏**($1000@368.12)；② **单笔硬拒单线**(法币 `nezha_risk_single_order_limit` + USDT `nezha_risk_usdt_single_limit`) 200000→**736240֏**($2000)——因大额审核线$1000 高于原拒单线$543 会使审核永不触发（单笔超限命中即拒、优先级最高），抬拒单线形成**三段带**：<$1000 通过 / $1000–$2000 转客服审核 / >$2000 直接拒；③ **频次** 10min `nezha_risk_freq_10min_count` 2→**8**、24h `nezha_risk_freq_24h_count` 5→**20**（原 24h=5 会压住 10min=8 使其形同虚设，一并放宽，仅登录用户计）；④ **客服打通**：风控拒单/审核时，结算支付页风控拦截面板(`OfflinePaymentForm` reject/review 块)新增「联系客服」按钮，点击直达平台客服会话深链 `/chat?type=admin&direct=1`(复用 Chat 组件既有能力：登录顾客直接打开 admin 会话、游客弹登录)；另在 `CheckoutPage` 下单 mutation 的 onError(order/place 兜底路径)对 `risk_reject`/`risk_review` 码也弹带「联系客服」的品牌 toast(新增 `RiskBlockedToast` 组件)。**诚实更正**：原拟修 `onSingleErrorResponse` 的 undefined toast bug，实查 order/place 的 onError 本就遍历 `errors[]` 显示消息、且顾客主路径是 risk-check 预检的内联面板(非 toast)，故无此 bug、未改该函数。**活体复验**(总开关已开)：三段带 pass/review/reject 逐档命中——367000 `pass`、368120/700000/736240 `review[large_amount]`、736241 `reject[single_order_limit]`、USDT 700000 `review`/800000 `reject`。**影响面≈0**(现网 2 单/1 paid)。**回退**：阈值原值已记（大额 32000 / 单笔 200000 / 10min 2 / 24h 5）。ADMIN_GUIDE 4.2 同步。前端提交见 nezha-frontend 同批 commit |
| 2026-06-16 | 下单风控① 正式启用（总开关 `nezha_risk_control_status` 0→1） | 🟡 L2（业务参数开关启用 · "真实影响开关"） | 用户（复核报告后拍板"现值直接开"） | 启用交易红旗风控①（5规则：单笔上限→拒单 / 单日累计·频次·大额特征→转人工审核）。**阈值保持当前线上值、不改代码**：单笔上限 200000֏（法币/USDT 同）→拒单不可放行、单日累计 800000֏→审核、大额特征 32000֏→审核、频次 >5单/24h 或 >2单/10min→审核、放行宽限 60min（键未持久化、引擎默认）。**汇率说明**：现汇率 368.12 下 200000/800000/32000֏ ≈ $543/$2173/$87（文档原标 $500/$2000/$80 基于旧汇率~400，本次已校准 ADMIN_GUIDE 4.2 美元标注）；阈值存**绝对德拉姆值、不随汇率波动**（同退款护栏处理）。**影响面≈0**：启用前现网仅 2 单（1 paid）、真实流量≈0，属"变更安全"（从无风控→有风控），翻开无即时影响、仅对今后超限订单生效。**活体验证**（开关开后 evaluate() 不再短路）：正常小单 1500 `pass`；大额 40000 命中 `large_amount`→`review`；单笔 250000 命中 `single_order_limit`→`reject`；USDT 250000→`reject`；游客大额 40000→`review`；`record()` 落库 status=`pending` 进审核队列、查得后**立即删除测试记录**（队列归 0，无脏数据）。`enabled()`=true。下单流程兜底接入点 OrderController L131-142（reject→risk_reject/review→risk_review，均返 403 且不创建订单）。**回退**：总开关改回 `0` 即恢复无风控（原值 0 已记）。**诚实边界**：① 大额审核阈值 32000֏($87) 偏低，多人拼单可能进审核队列（仅 review 非 reject），量大可后台上调；② `nezha_risk_support_contact` 为空且代码未引用，拒单/审核提示为固定中文文案、不渲染具体联系方式（文档 4.2 已诚实标注，如需展示需改提示模板）。ADMIN_GUIDE 4.2/4.4 同步更新。提交见同批 commit |
| 2026-06-15 | 退款护栏② 正式启用 + 限额按客单价上调 | 🔴 L1 (退款原路/限额执行机制 0→1 启用) + 🟡 L2 (限额调整) | 用户(逐项拍板确认) | 经退款引擎只读体检 + TRC20 校验 bug 修复后,启用退款护栏总开关 **`nezha_refund_control_status` 0→1**,退款机制②(原路锁定 + 限额 + 留痕≥5年)正式对所有真实退款生效(此前退款裸奔:无原路、无限额)。**同步上调限额**(用户逐项拍板,均改 `business_settings` 设置项、不改代码):单笔 40000→**400000֏**、单日累计 120000→**800000֏**、单日笔数 5→**10**、退款窗口 7→**3天**(澄清:本系统超窗口=转人工审核非自动拒;3天贴合外卖行业惯例——餐食即时消费、窗口本就短,窗口内丝滑自动退、超期=顾客找人工申诉);USDT 链上自动校验保持 **1(开)**。**启用时影响面≈0**:现网 0 笔 paid 订单 / 0 退款流量,翻开无即时影响、仅对今后真实退款生效,且属"变更安全"(从无护栏→有护栏)。**活体验证(开关开后 check_limits 不再短路)**:金额 300000/400000 `pass`、500000 命中 `single_limit`、900000 命中 `single_limit+daily_total`、交付5天前 命中 `window`;`enabled()=true`;原路锁定 + 链上校验(BSC/TRC) 同批已实测 `verified`/反查==链上from。**回退**:总开关改回 `0` 即恢复无护栏行为(原值 0 已记);限额原值 40000/120000/5/7 已记录于本条。⚠️ 公共节点免密钥,TRC/BSC 退款量大前建议补 `nezha_refund_trongrid_api_key`/`nezha_refund_bscscan_api_key`(否则限流时 verify 降级人工核)。**多窗口/汇率说明**:限额按用户「约 $1000/$2000」意图设为整数德拉姆 400000/800000(设定时 USD≈400֏);设定期间另一窗口将 `nezha_rate_usd_to_amd` 更新为 368.12,故现值实际 ≈$1087/$2174(略宽~9%)。**用户确认保留整数德拉姆值不重算**(与「初创期避免客户不好体验」的宽松取向一致);限额存绝对德拉姆值、不随汇率波动,功能无碍。ADMIN_GUIDE 4.2/4.6.1 同步更新。提交见同批 commit |
| 2026-06-15 | 退款引擎② TRC20 链上自动校验 bug 修复(开退款护栏前置体检发现) | 🟢 L3 bug修复(退款引擎·不改 L1 行为) | 用户 | **背景**:准备启用退款护栏 `nezha_refund_control_status` 前做只读活体体检,发现 `NezhaRefundControl::verify_refund_tx` 对 **TRC20(波场)** USDT 退款链上自动校验**恒返回 `failed:未找到USDT转账事件`**。**根因**:`tron_hex_to_base58()` 开头无条件 `strtolower($hex)`;TronGrid `/v1/transactions/{hash}/events` 把 USDT 合约地址返回为 **base58(大小写敏感)** 形式 `TR7NHqjeKQ…`,被 lowercase 成 `tr7nh…` 后与常量 `TRC_USDT` 比对永不相等→跳过事件→判失败。**修复**:改为仅当入参确为 hex(`ctype_xdigit`)时才 lowercase 转换,已是 base58 则原样返回(保留大小写);非 hex 返回原值。**性质·不碰 L1**:此前 bug **失败朝安全侧**(只把 TRC 退款转人工核,绝不会误判"已验证"放行,无资金/合规漏洞);USDT **原路锁定**(真正的 L1-2/L1-3 红线,走 `reverse_lookup_from_address`)两条链本就正常,本次不动。**实测(真实链上交易)**:① 修复前 TRC verify=`failed:未找到事件`;② 修复后 TRC 正确解析 `to`/`amount` 并在地址+金额命中时返回 `verified`(txid d460d598… to=TTaJTS6M… amt=10→verified);③ BSC 不受影响:正向 `verified`、错地址 `failed`;④ 两链 `reverse_lookup` 反查地址==链上 from。⚠️ **诚实边界**:TronGrid/BSC 公共节点免密钥(`nezha_refund_trongrid_api_key`/`nezha_refund_bscscan_api_key` 为空),连发请求会触发限流→verify 安全降级 `manual`(待人工核);量大前建议补 API key。`php -l` 通过、备份 `NezhaRefundControl.php.bak.202606151517pretronfix`。提交见同批 commit |
| 2026-06-15 | 商家入驻线索 merchant_leads PII 到期清理 | 🔴 L1-7 (PII到期删) 强化 | 标准授权(强化红线·不碰资金) | 落实 data-protection.md「merchant_leads 暂随业务保留,后续可纳入到期清理」待办。**另起独立命令** `nezha:purge-merchant-leads`(每日 03:50 调度):与举报/支付凭证不同,入驻线索**不能按登记时间一刀切删**(会误删未跟进的真实潜在商家)——故触发条件=**线索已结案**(`status`=2已完成/3无效)**且**距 `updated_at`(≈结案时间)超过保留期,抹除 contact_name/phone/wechat/address/note(置空),**保留 行/店名/品类/状态 供审计**;`status` 0待跟进/1跟进中【绝不触碰】。保留期 `business_settings.merchant_leads_retention_days`(默认 90 天,自结案起算,与支付凭证 90 天对齐)。`--dry-run` 同覆盖。**实测**(5条边界用例):已完成/无效+结案100天前→命中清PII;刚结案1天(未到期)/待跟进200天/跟进中150天→均正确跳过、电话微信原样保留;清后核对 PII 已抹、店名/状态保留;删测试行(表回0行)+删脚本;`php -l` 通过。诚实细节:contact_name/phone 为 NOT NULL 列,清除落为空串(非NULL),仍不含PII。提交见同批 commit |
| 2026-06-15 | 术语统一:商家「保证金」→「预存佣金」 | 🟢 L3 文案(仅措辞,机制零改动) | 用户 | 经核查 `VendorController::rechargeDeposit` 逻辑实为「商家先预存款→佣金按单从中扣→余额不足停接单」,本质是**预存佣金**而非押金式保证金,旧称「保证金」词不达意。**仅改措辞、机制/资金流/扣佣逻辑完全不动**:① 面向商家/运营处(后台收款信息页 Blade、`messages.php` 中文、Toastr、ADMIN_GUIDE/MERCHANT_GUIDE、代码注释)统一为**「预存佣金」**;② 本目录合规正本(business-flow/AML-policy/INVARIANTS)首次出现处并列标注**「预存佣金（履约保证金）」**——因该机制兼具预付与履约担保双重性质;③ **数据库字段 `deposit_balance`、设置键 `nezha_deposit_mode_status` 等英文标识符不改**(改 schema 无收益且有风险)。⚠️**法律最终用词以专业律师审定为准**(已在 business-flow.md 术语说明中注明)。`php -l` 全过、0 残留「保证金」(备份/StackFood vendor 目录除外)。提交见同批 commit |
| 2026-06-15 | 本地生活顾客举报 detail PII 到期清理 | 🔴 L1-7 (PII到期删) 强化 | 标准授权(强化红线·不碰资金) | 落实 data-protection.md「local_life_reports.detail 暂随业务保留,后续可纳入到期清理」待办。**扩展现有命令** `nezha:purge-locallife-pii`(非新建,沿用每日 03:40 既有调度):在 UGC 帖 contact_info 清理后追加一段——清 `local_life_reports` 中 `created_at` 早于保留期的行的 `detail`(置 null),**保留 行/reason/status 供运营审计**。保留期 `business_settings.locallife_report_retention_days`(默认 180 天),后台「护栏与文案设置」页新增输入框可调(留空保持原值)。`--dry-run` 同覆盖新逻辑(只报告)。**实测**:dry-run 无 180 天前数据命中 0;回填一条 `created_at`=200 天前测试举报→dry-run 命中 1→实删后核对 `detail`=null 且 `reason`/`status`/行均保留→删测试行(表回 0 行干净)+ 删 bootstrap 脚本;`php -l`/blade 编译通过。**merchant_leads(电话/微信 PII)本次不做、仍暂随业务保留**——入驻线索留存逻辑不同(需留到联系/入驻完成),按 created_at 自动删可能误伤真实业务,留待用户拍板(届时独立命令+独立保留期)。提交见同批 commit |
| 2026-06-15 | 本地生活 后台「护栏与文案设置」页 | 🟢 L3 实现(法律文本编辑入口) | 标准授权 | 新增后台 `/admin/local-life/settings`(列表页右上角入口):运营自助编辑违禁词 `locallife_banned_words`、免责短提示 `locallife_disclaimer`、《信息发布规则》全文 `locallife_terms`、三个反滥用阈值(每日发帖/举报上限、最小发帖间隔),不再需改数据库。规则全文编辑区带⚠️提示(法律文本慎改、与 `docs/legal/local-life-terms.md` 正本同步、重大修改请律师过目);阈值留空保持原值、最小间隔填0=关。文案类允许清空(清空则顾客端回退代码内默认)。实测 round-trip(真实调 settingsSave 写入/读取/复原)通过+blade 编译+路由注册 OK(收尾发现并清掉测试残留 daily_limit 键,还原代码默认5)。ADMIN_GUIDE 8.5 同步指向本页。提交见同批 commit |
| 2026-06-15 | 本地生活《信息发布规则》+免责声明 中文正式版上线 | 🟢 L3 内容 + 🔴 L1-1/L1-7 口径确认 | 用户(语言范围/联系方式/是否上PII通知 三问已拍板) | 将用户提供的《合规文案包》中文版清理为可上线文本(删研究综述 Part0/对照表 Part4/律师待办 Part5 等**非展示内容**及"须律师审"元注释),上线到 `business_settings.locallife_terms`/`locallife_disclaimer`(前端规则抽屉+免责短提示渲染,无需 build)。§7 投诉渠道=站内「举报」+ noctis201914@gmail.com、§10 管辖=亚美尼亚共和国法院(原占位 `____` 已填)。新增**法务正本** `docs/legal/local-life-terms.md`(git 留档/审计/将来协议页同源复用)。**L1-1**:§1.2 明列平台不收/代收/代付/担保、不参与交易;**L1-7**:§5 联系方式仅登录详情可见、列表永不展示、30天清除、静态加密(与 data-protection.md 一致)。⚠️**暂未上线(已记 docs/legal 待办)**:俄/英/**亚美尼亚语**版(亚语文档明标须母语律师审校;亚美尼亚消费者法要求强制信息用亚语)、《个人数据处理通知》(需公司全称+注册地址+前端第二个 consent)、Part5 的15条待当地律师确认项。Playwright 真机验证规则抽屉渲染全文(第1条/「不收取代收代付」不碰钱条款/邮箱/第11条),无横向溢出、console 0 错。提交 00e2f51 |
| 2026-06-15 | 微信支付独立成单独收款方式(原微信/支付宝共用一张人民币码) | 🟢 L3 实现 + 🔴 L1-1 不碰钱 / 🔴 L1-7 PII 确认 | 标准授权(扩展既有已合规字段,未触红线) | 此前结算页「人民币」一张共用码同时给微信/支付宝扫;现拆分:`restaurants` 新增列 `wechat_qr_image`(独立于 `rmb_qr_image`),后台「收款信息」tab 拆成「支付宝收款」「微信收款」两张卡分别上传;离线支付方式由共用「人民币支付（微信/支付宝）」**重命名为「支付宝」** + **新增「微信」方式**(method_fields 同为付款截图必填),结算页前端按方式名(含微信/wechat 且非支付宝)选显微信码 or 支付宝码。**L1-1 不碰钱**:微信码与支付宝码同性质,均为**商家本人收款码**,顾客直付商家、平台不归集不代收,未引入任何代收/担保/下单字段。**L1-7 PII**:`wechat_qr_image` 列落在已 `ENCRYPTION='Y'` 的 restaurants 表内(已验证)、自动随全表静态加密(at-rest);属商家**常驻展示码**(非顾客一次性付款凭证),**不在** `nezha:purge-payment-proofs` 90天清除范围(与 rmb_qr 一致)。**微信外部唤起诚实边界**:经查微信官方/社区,普通手机浏览器(非微信内置)**无法深链唤起微信到指定收款页**(`weixin://scanqrcode` 仅唤起实时相机扫码、传不进指定图,Android 多被拦),故顾客端走「长按二维码保存到相册→微信扫一扫从相册选图」可靠路径,另附尽力而为「打开微信」(`weixin://`,失败无动作,不伪造跳转)。两条迁移可逆(down 改回原名/删列/删微信方式)。提交见同批前后端 commit |
| 2026-06-15 | 本地生活 UGC 加固(窗口⑤):违禁词过滤 + 顾客举报 + 反滥用限流 + 平台免责声明 + **L1-7 加密缺口修复** | 🟢 L3 实现 + 🔴 L1-7 PII 修复 + 🔴 L1-1 不碰钱确认 | 用户(发帖总开关 `locallife_ugc_enabled` 仍默认关,需用户拍板开放) | 在窗口④ UGC 发帖基础上补四层上线护栏:**①违禁词过滤**——发帖(及后台录入)扫 title+description+contact_info,命中即拒(422「内容含违规词」,不回显命中词防试探),词库 `business_settings.locallife_banned_words`(换行/逗号分隔,后台可增删,种子覆盖涉黄/赌博/诈骗洗钱/毒品违禁/外站强引流,`mb_stripos` 不区分大小写);**②顾客举报**——新表 `local_life_reports`,接口 `POST /api/v1/local-life/posts/{id}/report`(**auth:api 禁匿名**),理由白名单6项(「其他」必填说明 max500),同用户对同帖未处理举报去重(友好提示不报错),每用户每日上限 `locallife_report_daily_limit`(默认20);后台列表举报数徽章 + 举报处理页(下线帖 status→2 并把待处理举报→已处理 / 驳回举报→2 帖不动);**③反滥用**——最小发帖间隔 `locallife_ugc_min_interval_sec`(默认60s,429)+ 24h 内同标题重复拦截(422),沿用④每日上限5;**④免责声明**——列表/详情底部常驻短提示 + 发帖必勾「我已阅读并同意《本地生活信息发布规则》」(不勾→提交按钮灰显 disabled),短提示/全文 `business_settings.locallife_disclaimer`/`locallife_terms` 可配。**🔴 L1-7 加密缺口修复**:核查发现 `local_life_posts`(窗口④建,含 contact_info PII)`CREATE_OPTIONS` 为空 = **实际未加密**(contact_info 明文落盘),与 data-protection.md 此前「随全表静态加密」声称不符(MySQL 5.7 新建表不继承全库加密)→ 本窗口迁移内 `ALTER TABLE local_life_posts ENCRYPTION='Y'` 补加密;新建 `local_life_reports`(detail 可能含 PII)显式 `ENCRYPTION='Y'`;两表均已确认 `ENCRYPTION="Y"`。**L1-1**:违禁词/举报/免责全程不引入任何支付/押金/代收/下单/担保字段,仅信息墙。**总开关仍默认关**=现网不对外开放发帖,护栏齐备后由用户后台手动开。实测:违禁词422 / 正常发帖200 / 最小间隔429 / 举报200 / 重复举报去重200 / 「其他」无说明422 / Playwright 三态(免责提示·规则抽屉·勾选灰按钮·违禁词toast·举报抽屉) 全 PASS,console 无 JS 异常。提交见同批前后端 commit |
| 2026-06-15 | 本地生活 UGC 用户发帖 + PII 到期清除(窗口④) | 🟢 L3 实现 + 🔴 L1-7 PII / 🔴 L1-1 不碰钱 确认 | 用户(开关默认关,需用户拍板开放) | 顾客可在 H5「本地生活」自助发帖(标题/分类/价格/图片/联系方式),默认 `status=待审核`,后台「通过」才公开、「驳回」带理由。**L1-1**:发帖表单/接口**不含**任何支付/押金/代收/下单/担保字段,仅信息墙撮合。**L1-7 PII**:`contact_info` 列表永不返回、详情仅登录顾客可见、游客置 null;随全表静态加密;发帖设 `expires_at=+30天`,新建命令 `nezha:purge-locallife-pii`(每日03:40)到期抹 contact_info + 删上传图,保留帖子行/状态供审计(--dry-run 实测命中0,无过期数据)。**发帖入口总开关 `locallife_ugc_enabled` 默认关(0)** = 现网不对外开放发帖,前端 FAB 显示"即将开放",需用户验收满意后手动开(真实影响开关);每用户每日上限 `locallife_ugc_daily_limit`(默认5)防刷。顺带修 nginx `fastcgi_intercept_errors`(API vhost 关闭),使 Laravel 404/502 JSON 错误响应不再被 nginx 错误页替换、保留 CORS 头(原致前端跨域拿到 Network Error)。违禁词过滤/举报/公开总开关顺延窗口⑤。实测:游客发帖→登录门、登录→填表+传图→提交→待审核、后台通过/驳回、422 校验、PII 仅登录可见 全 PASS。提交见同批前后端 commit |
| 2026-06-14 | 离线支付付款截图文件上传(端到端)落地 | 🟢 L3 实现 + 🔴 L1-7 PII 覆盖确认 | 用户 | 补齐顾客离线支付**截图文件上传**(上一轮只通文本哈希,文件字段被刻意跳过)。前端 OfflinePaymentForm + 订单详情编辑表单渲染文件输入并改 multipart 提交(POST + `_method=PUT` 方法伪装命中原 PUT 路由,因 PHP 不解析 PUT 的 multipart);File 不入 redux(避免 redux-persist 序列化丢失),经表单 ref 在下单后随 FormData 提交。后端 `offline_payment()`/`update_offline_payment_info()` 对 `input_type==='file'` 字段用 `Helpers::upload('offline_payment/')` 存盘,`payment_info` 记完整相对路径 `offline_payment/xxx.webp`;编辑时不传新图则保留原图。**截图扩展名守卫**(仅 png/jpg/jpeg/webp/gif)从源头杜绝 purge/展示判定盲区。**PII(L1-7)覆盖确认**:截图随全表静态加密(at-rest);90 天 `nezha:purge-payment-proofs` 已验证连同截图文件一并清除(--dry-run 回填 120 天实测命中 `offline_payment/...webp`)。顾客端/后台/商家端订单详情均可点开查看截图。实测:USDT(可选)+ 人民币(必填)两态——必填缺图 403、非图扩展名 403、正常 200 入库 + 文件存盘 + track API 返回 `file_url`(200 image/webp)。提交见同批前端/后端 commit |
| 2026-06-14 | 退款机制② 原路锁定 + 限额限笔 落地 | 🔴 L1 (L1-2/3/4 退款原路) | 用户 | 退款接入「原路锁定 + 限额」: USDT 退款目标 = 由原始付款 tx 反查的原 from 地址(链上校验金额+地址, 公共节点免密钥 + 降级人工核), 法币退还原付款人(政策+凭证版), 金额强制 ≤ 原订单, 禁退第三方; 单笔/单日累计/单日笔数/退款窗口 限额(后台「风控设置→退款控制」可调), 超限转 admin 审核不直接拒(放行后该订单豁免); 全程留痕 nezha_refund_records(≥5年, 免于PII自动清除). 新增后台「退款留痕/审核」页(USDT 登记商家退款哈希做链上校验/凭证上传/超限审核). 总开关 nezha_refund_control_status **默认关(0)** = 现网退款行为不变, 需测试单验证 + 用户批准再开. 实测: 真实链上 tx 反查+转账校验/限额命中/留痕/grace 全 PASS. 提交 04da684 / f1c5a8c / 2814589 / 5f5042d |
| 2026-06-14 | L1-1 护栏: 直付订单禁走平台钱包退款 | 🔴 L1 (平台不碰钱) | 用户 | Admin/OrderController 退款分支: offline_payment(直付)订单**永不**走 wallet_add_refund 平台钱包退款(退到平台钱包 = 平台替顾客记账/欠款 = 碰钱), 一律原路由商家退顾客, 平台仅留痕. 堵住"钱包开关被开即违规"的潜在地雷(当前 wallet_status=0 本就睡着, 此为防御性硬化). 提交 f1c5a8c |
| 2026-06-14 | 修复离线支付凭证采集(退款前置) | 🟢 L3 (bug修复) | 用户 | offline_payment 提交原从 null 的 method_informations 读字段名 → 顾客交易哈希/截图全被丢弃(memory 已记之已知bug); 改为从 method_fields.input_field_name 读 + 前端表单同步修(顾客输入框/绑定). 这是 USDT 退款原路反查的**数据前提**(无原始 tx 则无从反查 from 地址). 真实流量 0, 属填坑非救火. 实测: 提交后 payment_info 含交易哈希 + 前端 USDT 哈希框正常. 提交 e70cac9(前端) / 6b82c5c(后端) |
| 2026-06-14 | 商家入驻意向表 (merchant_leads) | 🟡 新增 PII 类别 | 用户 | H5「我的→商家入驻」MVP 人工入驻线索:新表 merchant_leads 收店名/联系人/电话/微信/地址/品类/备注.电话属 PII,沿用全表静态加密(见同日条目)+ MasksSensitiveAttributes(demo 模式脱敏);提交即发邮件到运营邮箱.平台此步不碰钱,正式开店仍人工.留存:暂随业务保留,后续可纳入到期清理.详见 data-protection.md |
| 2026-06-14 | PII 静态加密 + 加密备份③ 实装 | 🔴 L1 (PII加密) | 用户 | MySQL 表空间加密(全部150张 InnoDB 表 `ENCRYPTION='Y'` + keyring_file/early-plugin-load),覆盖全部 PII 字段,验证读写正常+API200;keyring 在数据&备份目录之外.每日 AES-256 加密备份(留14份,密钥 /root/.nezha/backup.key,明文不落盘).诚实边界:防磁盘/备份被盗,非防服务器被攻破;5.7 的 redo/undo/binlog 未覆盖(binlog 已关).一次 MySQL 重启~2s.属 DB/配置层变更,repo 仅同步本文档 |
| 2026-06-14 | 支付凭证 90 天留存自动清除③ | 🔴 L1 (PII到期删) | 用户 | 命令 nezha:purge-payment-proofs + 每日计划任务,抹除超期 offline_payments 的 PII 字段及截图,保留行/状态供审计;链上/交易记录不动(另留≥5年).天数后台 nezha_payment_proof_retention_days 可调(默认90).提交 a31d25c |
| 2026-06-14 | Admin 后台 TOTP 两步验证③ | 🟡 访问控制加固 | 用户 | 零依赖手写TOTP(RFC6238已验证),opt-in默认关,含8个一次性恢复码+SSH应急关闭命令.E2E验证通过.提交 aa9fa45 |
| 2026-06-14 | 代码库密钥泄露扫描③ | — (核查) | 用户 | 前后端 git 全历史核查: .env从未入库, .gitignore已屏蔽密钥/凭据, 已知机密0命中. 无需修复 |
| 2026-06-14 | PII 静态加密方案评估③ | 🔴 L1 (待批准) | — 待用户 | 应用层加密会破坏搜索/派单功能,不适用;建议改 MySQL 表空间加密(透明,搜索照常).涉MySQL重启,待用户批准后实施 |
| 2026-06-12 | 建立合规文档体系 + 机制评级清单 | — | 用户 | 新建 INVARIANTS.md + docs/compliance/(AML/业务流/数据保护/本记录),用于合规留档与防止后续误改核心机制 |
| 2026-06-12 | 上线交易红旗风控① (后端+后台) | 🟡 新增机制 | 用户 | 5规则交易监控(单笔/单日/频次/金额特征)+人工审核队列+阈值后台可调;总开关默认关,待菜单美元化后启用 |
| 2026-06-10 | 拔除二清打款腿 | 🔴 L1 | 用户 | 禁用 RestaurantDisbursement,直付订单不记平台对商家负债,平台不持币 |
| 2026-06-10 | 确立 B 方案(顾客直付商家,平台不碰钱) | 🔴 L1 | 用户 | 规避二清/帮信/外汇风险;平台靠保证金收佣 |
| 2026-06-15 | 本地生活 多语言条款 + 《个人数据处理通知》同意采集**骨架**(默认关) | 🔴 L1 (对外法律文本 + 新增同意采集，**未生效**) | 用户(暂无注册实体/未律师审校→保持默认关) | 为将来合规上线预置骨架，**当前不改变任何线上行为**。① **多语言条款/免责**：后端按 `X-localization` 返回 `locallife_{terms,disclaimer}_{en,ru,hy}`，留空回退中文；后台「护栏与文案设置」加各语种编辑框。② **《个人数据处理通知》PII 同意采集**：新增 `locallife_pii_notice{,_en,_ru,_hy}` + 独立开关 `locallife_pii_consent_enabled`(**默认 0/关**)；发帖接口加 `agree_pii` 守卫、H5 发帖表单加第二个必勾同意项，**门控「开关开 且 正文非空」两道门**。当前全空/关 → **API 返回逐字节同旧、H5 不出现第二勾项、不采集任何新同意**(Playwright 真机:默认态无第二勾项+中文条款正常、console=0;mock 开启态:两勾项渲染+提交按钮正确门控;API:`ugc_pii_required=false`)。🔴 **正式开启前置**:① 亚美尼亚注册实体(数据控制者全称+地址);② 律师审校(亚语须母语法律译者)。**用户决定:俄/亚语文案未审校全部先不上、暂无主体故 PII 通知先不上**。详见 `legal/local-life-terms.md` + `data-protection.md`。提交见同批前后端 commit |

## 待实施(实施时在此追加记录)
- ② USDT 通道: ✅ **退款地址锁定**(由原始付款 tx 反查 from 地址 + 退款 tx 链上校验金额/地址)已落地 2026-06-14(见上表); ✅ **退款链上记录留存 ≥5年**(nezha_refund_records 表) 已落地; ⏳ **仍待**: 付款来源地址 OFAC SDN/黑名单筛查(命中拒收下单) — 🔴 L1
- ③ 剩余项:RBAC、审计日志、自动安全更新、密钥轮换、泄露应急流程 — 🔴 L1 / 🟡
  - ✅ 已完成: 支付凭证90天自动删、Admin 2FA、git密钥扫描、服务器加固(key-only/fail2ban/限速)、后台nginx Basic Auth、**PII静态加密(MySQL表空间加密)**、**每日加密备份(AES-256,留14份)**
