# 哪吒外卖 — 合规变更记录 (Compliance CHANGELOG)

> 每次改动合规相关机制(风控、资金流、数据保护、退款、留存)都在此记一笔。
> 格式: 日期 | 变更 | 等级 | 批准人 | 说明。等级定义见 `../INVARIANTS.md`。

| 日期 | 变更 | 等级 | 批准人 | 说明 |
|---|---|---|---|---|
| 2026-07-04 | **denied 凭证争议流 R1-R4 全实装(dormant·开关 nezha_refund_dispute_status=0) + R4 逾期计时锚点消费** | 🔴 L1-2 邻(退款义务判定·平台不碰钱) | 业主 2026-07-03 批准 §7(争议流) | 商家对「待退款(段B·凭证在案先核后退)」可发起争议(R1 数据模型+守卫·refund_record_id UNIQUE=单记录争议上限 1 次结构墙); disputed 排除出 STATUS_NEEDS_ACTION=争议期暂停逾期催办/停接单(R2a); 商家端发起 UI(R2b); 超管「退款留痕 → 退款争议裁决」队列 + 裁决闭环(R3): upheld(维持退款义务→回 pending_merchant_refund·置 overdue_anchor_at=裁决时刻) / closed_no_payment(核实未收款→留痕关闭终态·非删除)。**R4 逾期计时锚点消费**: 全站逾期"几小时/是否达阈值/筛选/排序"收口 NezhaRefundRecord::OVERDUE_SINCE_SQL=COALESCE(overdue_anchor_at, created_at) + overdue_since accessor(RefundOverdueSweep 选择+时长 / SystemController 浮窗 / overdue() 排序 / overdue.blade 显示 五处同源); upheld 复活单从裁决时刻重算逾期(不再拿原始 created_at 立刻误判逾期误催商家) + 清该记录 nezha_refund_overdue_events 幂等账本行=催办重周期。🔴 平台全程不碰钱、只改留痕/审计状态、L1 退款路由(原路退)不变。验证: php-l 全绿·进程内 COALESCE 三态(fresh anchor 不选中=宽限期重启核心修复) + accessor 两态 + 真实 sweep/orderByRaw 查询执行 + blade 编译 + 探针渲染 HINT。技术就绪·真开前须业主批准 + 亲测整条争议链。正本代码 fb8cc17(R3) + 本次 R4。 |
| 2026-07-02 | **L1 变更(实装): L1-8③ 制裁门由「读入驻旧 screen_status 列」改为「审批放款时实时 RE-run screen_names」+ step4-4/step5 商家退出结算暴露(未部署·dormant)** | 🔴 L1(变更 L1-8③ 判定源·更 fail-closed) | 业主明确批准("批准，走完整 L1 流程") | 缘由: 入驻当刻写死的 vendor_kyc_profiles.screen_status 静态列, 制裁名单每日刷新后不重算=空转, 退款给商家时可能已过期漏放行。改为退出审批放款前用【当前】nezha_sanction_names 实时 RE-run NezhaKycScreen::screen_names([legal_name,beneficial_owner_name])(NezhaOffboard::rescreenSanctions): clear→置 sanction_rescreen_at 供 approve() 放行; hit→拒+工单 rejected+恢复营业+record_risk; possible/未决→fail-closed 不置位+转人工 AML。approve() 4 道 fail-closed 放款门(status=applied·冷静期·sanction_rescreen_at·holder_verified)+户名三方核对(§D3, 超管勾选 legal_name==KYC 户名==缴纳凭证付款人)+审批闸 H(高额净额≥nezha_offboard_high_amount_amd 默认 500000֏ 强制 approved 后 T+1·TG 二次告警·审计双时间戳)。同步 step4-4 暴露层: 商家端对账中心底部申请/撤回退出(前置门 §E3: 自家非终态订单硬挡·待核实纠纷软提示不硬挡[防举报武器化]·冷静期 20 天)+超管审批/放款 UI(admin/nezha-offboard·module:deposit)+ KYC review 回流状态机(onKycApproved/onKycRejected)+「待退出核验」队列+超期红旗(被动查询无 cron)。总开关 nezha_offboard_status 默认 0=关(服务端在 open() 控制器强制, 部署≠启用, 运行时急停)。断言同步: NezhaL1RedlineTest 加 step5 结构守卫(screen_names 实时调用/approve 4 门/开关默认关);三腿隔离 INV-1 + net 公式守卫 step4-3 已在。资金正确性由 staging 下单 harness 验收(CI 测不了资金闭环)。⚠️**本次仅 commit+push, 未部署**(随 offboard 批次由业主协调 nzdeploy-api.sh)。正本 docs/DESIGN_merchant_offboard.md(v3) + docs/PLAN_merchant_offboard.md §8。 |
| 2026-07-02 | **商家退出结算 step1 schema(DESIGN §A): 押金/结算/来路/KYC 指纹 列+新表 landed(纯 schema·未激活)** | 🟢 L3(结构·实装 L1-8 已批准机制的载体) | 用户授权(step1 执行范围) | 全 additive 迁移(2026_07_02_050000): restaurant_wallets.guarantee_balance / restaurant_deposit_transactions.currency+original_amount+original_ref(模型 encrypted) / restaurants.onboard_source+offboard_status+guarantee_tier(存量店 default 回填 unknown/active/NULL) / vendor_kyc_profiles.account_holder_name(encrypted)+id_doc_fingerprint(HMAC 明文索引) / 新表 restaurant_offboard_settlements(uq_active(vendor_id,active_uniq) 5.7 部分唯一=幂等根)。🔴 L1-8⑤ 留存: RDT 与新表均资金/合规留存≥5年、免 PII 清除, 全库 purge 任务零触及(2026-07-02 grep app/Console 已核实)。**本迁移不激活任何资金/合规行为**(纯 schema; 抽佣/退出/缴纳/制裁逻辑尚未接)。staging(nezha_staging)实证: migration up/down + 10 列/表齐 + 存量店回填 + guarantee_balance 可填 + original_ref 落库加密(prefix eyJ≠明文)+模型解密回读 + 结算 casts(bool/float/Carbon) + uq_active 2nd active 拒1062/多条 NULL 可并存, 8 项全绿。migration + 3 模型 fillable/casts + 新模型 RestaurantOffboardSettlement(见本次 commit); ⚠️随下次 nzdeploy-api.sh 自动 migrate 上线。 |
| 2026-07-02 | **双扣佣底座(task_fb41eea8): order_transactions.order_id 唯一约束 + create_transaction 幂等 catch** | 🟢 L3(结构完整性墙·守护资金正确性, 同 task9; DESIGN_merchant_offboard A6/C5 前置) | 用户授权(step0 执行范围) | create_transaction(OrderLogic:83) 对每单记一条 order_transaction 并在(直付单+开抽佣时)从 deposit_balance 扣 commission_deduction; 并发结算(顾客确认 settle_delivered / 超时兜底 cron / 商家 status())可能同时越过 exists() 幂等闸(check-then-insert 无锁)→ 双记流水+双扣佣金。加 UNIQUE(order_id) 作 DB 底座(后到者 insert 撞 1062 → create_transaction 内新增 catch 幂等 return false, 不进扣佣/改钱包)。现网核实(prod sql_api_nezha_am): order_transactions 11 行/0 重复 + commission_deduction 0 行(抽佣开关 nezha_deposit_mode_status=0 从未真扣)→ 加约束零数据清理、零资金影响、纯预防。staging(nezha_staging)harness 实证: 无索引=双扣(deposit −900)/有索引=单扣(−450)且 2nd create_transaction return false/原始重复 insert 拒 1062; migration up/down 通过; NezhaCommissionTest+NezhaL1RedlineTest 17 passed(37 断言)。migration database/migrations/2026_07_02_040000_* + OrderLogic.php(见本次 commit); ⚠️随下次 nzdeploy-api.sh 自动 migrate(:54)上线, 本次未单独触发部署。 |
| 2026-07-01 | **L1 变更: 新增 L1-8(押金持有+商家退出结算退还) + 制裁门(呼应 L1-6) + 押金法币-only** | 🔴 L1(记录·机制待实装) | 业主明确批准("L1变更——批") | 组④⑤押金账户+退出结算方案经 /debate 三路红队核验(资金/合规/防薅, verdict 全🔴阻断)后业主拍板并记入 INVARIANTS(新增 L1-8): ①**制裁门(呼应 L1-6)**——退出结算退款给商家前须 KYC 制裁筛查未命中(vendor_kyc_profiles.screen_status∉{hit,possible}), 命中即挡+转人工 AML; 缘由:"退钱给商家"首次让平台可能主动向受制裁主体付款(现有 KYC 命中仅 Toastr 软告知不硬拦, NezhaKycController.php:110)。②**资金隔离处理**——退出结算合并三账户结清时未结佣金/罚款只从 deposit_balance 抵, ad_balance/guarantee_balance 各自独立全额退、不跨账户挪用(守 ad 资金隔离)。附政策: 押金定**法币-only、平台不持 USDT**(强化 L1-1; 删原"平台自有 USDT 地址+托管"——合规红队指出平台托管 USDT=首次持币突破"平台不碰钱"基石)。正本 docs/PLAN_merchant_offboard.md §4/§8。⚠️机制**尚未实装**: 5 工程阻断项(净额竞态锁/三腿原子性幂等/来路溯源/退出门防滥用)+设计硬化未完成前不写代码; 实装时须同步 tests/Feature/NezhaL1RedlineTest.php 断言(加制裁门+法币-only)。本次仅记录 L1 决策于 INVARIANTS/CHANGELOG, 无代码改动。 |
| 2026-07-01 | **广告竞价 Slice C0: 首页推广条移除付费位 merit 标签「精选好店」+ 全去标** | 🟢 L3(前端呈现层·删标签不碰资金/计费路径; 修复的是 L1 邻近合规红线) | 业主拍板(截图点头后上线) | 首页「商家推广」横滑广告条(AdStrip, 早于 47849df 上线)部署代码带 merit 标签「精选好店」= 付费位吹捧, 违「付费位不得贴虚假 merit 标签」唯一红线; 业主决定全不打标。删「推广」角标 +「商家推广/精选好店」头部(含喇叭 CampaignRoundedIcon 图标) + 未用 import, 保留推广条与卡片(店名/评分/图)。approved_total=0 顾客零可见变化(条现不渲染, 一次广告审核即上线故现修)。⚠️留痕: 全去标同时移除了原「推广」角标(兼具广告披露作用, 老注释载明), 已向业主明示 merit 吹捧(必删)与中性广告披露(可选)为两事并经确认全不打标; 若目标市场广告法要求付费位可识别再评估补中性「广告」标。前端 commit d658315 / release BUILD xO5Zl7fg7rEyG1zxIU8RH; 本地 mock 三态 Playwright 验 + served bundle grep 三标签串=0。C1(后端防刷)/C2(前端计费触发)为独立任务, 开关 nezha_ad_auction_status 仍默认 0=关。方案 docs/PLAN_ad_auction.md §11。 |
| 2026-07-01 | **数据完整性墙(task9): 价格≥0 + 防孤儿外键 + 状态非空白 + food.name NOT NULL** | 🟢 L3(表结构/实现细节·结构性加固, 顺带强化 L1-4 订单留存) | 用户授权(本窗口拍板范围 + 逐墙 staging 验证后上线) | 给 StackFood 松字段加结构约束。MySQL 5.7 静默忽略 CHECK + App strict=false 非严格连接静默 coerce → 用触发器 SIGNAL + 外键落地(这套栈唯一能真拒绝坏数据的机制)。W2 价格≥0 触发器(food/add_ons/variation_options/item_campaigns/order_details); W1 外键 ON DELETE RESTRICT(order_details→orders / food→restaurants / orders→restaurants, 使订单记录不能被"删店"误删 = 结构性强化 L1-4 留存); W3(宽松) orders 状态非空白触发器(严格词表墙经代码审计判高破坏×低价值已弃, 完整词表交应用层 in: 校验); W4 food.name NOT NULL。配套 VendorController::destroy 友好拦截(有订单店不可删/无订单先清菜品)。staging(nezha_staging)验证下单不破 + 负值/孤儿/删带单店/空白态被拒 + 每墙可回滚; prod 审计 0 违规后经 release 部署上线(commit a05ce34), 4 migrations DONE + 健康门 config/zone/login 200。 |
| 2026-07-02 | **多级满减 折扣账务定性: 商家自掏折扣 100% 记 vendor·平台 0 出资(消除 折扣×佣金率 记 admin 的虚假补贴)** | 🔴 L1(账务定性·平台不出资;新增 INVARIANTS L1-9) | 用户授权(本窗口明确批准"修·全局都记vendor") | OrderLogic::create_transaction 的 vendor 折扣分支原把 折扣×佣金率(amount_admin)记 admin_expense(expenseCreate created_by=admin·type=discount_on_product)→ ReportGeneratorTrait `net_profit=admin_earning-admin_expense` 从平台净利再扣一次 + 报表"平台支出明细"虚显平台补贴, 与"平台不出资"对外陈述冲突。改为 amount_admin=0 + 删除 admin 拆分行 → 商家自掏折扣(满减/POS)100% 记 vendor、平台 0 出资;佣金仍按减后额计(平台"少收"D×率佣金=让利分摊, 属少收非支出, 已被 NezhaCommissionTest 锁定)。真金流动=0(直付+虚账), 多级满减仍灰度关(nezha_tiered_discount_status=0)线上零影响;同时生效于现有 POS 商家折扣(同属商家自掏)。断言: NezhaL1RedlineTest::test_L1_9_* 结构守卫(禁 amount_admin 拆分恢复)。 |
| 2026-07-02 | **商家广告竞价 CPC 第二期(商家侧 Slice B): 商家竞价推广 3 旋钮面板** | 🟢 L3(商家自助 UI·写 advertisements 配置行·不碰资金路径) | 用户授权(本窗口拍板设计+部署) | 商家后台「广告→竞价推广」: 推广开关 + 想多靠前(低/中/高) + 每天最多花; 只读展示 广告余额/今日已花/今日点击。一店一条 cpc advertisements 行 upsert(pricing_model=cpc/slot=list_top/status approved⇄paused), tier→出价跟随后台 floor/max_per_click 自动换算、出价数字对商家全程隐藏; 自助即时生效(status=approved, 业主拍板无需超管审核); restaurant_id 一律服务端 Helpers::get_restaurant_id() 取(IDOR 安全); CPT 广告列表/计数已过滤 cpc 行防混淆; 商家侧栏「竞价推广」入口跟随总开关(nezha_ad_auction_status 关时隐藏)。总开关仍默认 0=关、且 ad_balance=0 时 RecomputeAdAuction 不物化 → 零 live 影响。验证: 进程内真渲染面板 + savePromotion upsert 20/20(建行/高档 bid=封顶/低档 bid=保底/日预算/开关⇄status/同行不重复/事务 rollback 零残留) + 门控 4/4(关隐藏·开显示) + route:list。不碰资金原子路径(第一期已死亡测试)。方案 docs/PLAN_ad_auction.md §8/§10。 |
| 2026-07-02 | **商家广告竞价 CPC 第二期(超管 UI): 竞价参数页 + 广告余额充值页上线·开关仍默认关** | 🟡 L2(平台广告服务费·纯超管 UI·不碰顾客钱、非二清) | 用户授权(本窗口拍板设计+部署) | 第一期后端核心(64414ed)之上补超管操作面: ①「广告→竞价参数设置」(admin/advertisement/auction-settings) 编辑 9 个 L2 参数(总开关/保底价/单次封顶/日预算上限/前N自然位保留/单店占位上限/去重窗口/计费身份下单史阈值/重算间隔), 守卫 floor>0·单次封顶≥floor·日预算上限≥floor; 总开关翻转写 AdminAuditLog。②「广告→广告余额充值」(admin/advertisement/ad-recharge) B2B 预付记入商家 ad_balance, 抽出单一真相源 App\CentralLogics\AdBalanceLogic::credit(原子 UPDATE ad_balance+? + ad_recharge 流水, INV-1 只动 ad_balance 永不碰 deposit, INV-2 不用 save 整行), 只充值不扣减(冲正走 CLI)+二次确认+审计日志。总开关 nezha_ad_auction_status 仍默认 0=关, 纯超管后台 UI, 对顾客/商家零 live 影响。资金路径与第一期同构、不升级定级。验证: 进程内真渲染两页 22/22(字段齐+隔离证伪 deposit 纹丝不动+事务 rollback 零残留)+route:list 四路由注册。方案 docs/PLAN_ad_auction.md §8/§10。 |
| 2026-07-02 | **商家广告「实时竞价」CPC 第一期(后端核心)上线·开关默认关** | 🟡 L2(平台广告服务费, 不碰顾客钱、非二清) | 用户授权(本窗口拍板 DDL+部署) | 新增 CPC 按点击计费+首价清算+近实时物化竞价(每5分钟)+**独立 ad_balance 子余额(只扣它、永不碰 deposit_balance, 从结构根除「买广告把自己店买下线」)**+服务端可信计费身份(登录+真实下单史)+难刷质量分(完单率/差评率/出餐时长)。流水类型 **ad_click_fee**(区别于 CPT 的 advertisement_fee, ad_balance 隔离不混 deposit 对账)。总开关 nezha_ad_auction_status 默认 0=关(关时全链路零行为变化, 排序退化到现行 CPT)。验证: 死亡测试 PHPUnit 9/9(首价/原子封顶/隔离证伪自残/可信身份/dedup/物化/质量分/对账) + 真并发30·50路零超扣·无死锁·对账一致·零残留。二清三要件未齐(广告费不经顾客钱/平台收自有费进自己账/不再分发), 与 CPT 同构资金路径, 不升级定级。方案 docs/PLAN_ad_auction.md。 |
| 2026-07-01 | **阶段D 双向客服: 人工客服／商家沟通经 Telegram 中转 — 隐私政策第3条(7)披露 + 数据保护说明** | 🟢 L3(数据保护披露·只增透明度) + 🔴L1-7相邻(新增第三方/跨境数据流) | 用户授权(本窗口拍板"按此发布") | 阶段D Telegram 双向客服: 顾客"转人工"/与商家沟通时, 消息内容可能经 Telegram(境外第三方)转交超管/商家便于回复。隐私政策 `privacy_policy`(DataSetting id=154)第3条加 (7) 披露(第三方+跨境+去标识化+提示勿发敏感PII), 版本 1.0→1.1, 旧值备份 `_privacy_policy_backup_*.html`; `data-protection.md` §6 加同条记录。**最小化**: 仅必需范围转交, 面向超管/运营消息经 redactPii(屏蔽电话/邮箱/钱包); **不碰钱(L1-1 不变)**。配套代码=③绑定改"发码自动绑"(`d746951`, 全程 gate 在 `nezha_cs_tg_webhook_secret`); ④ setWebhook 激活日起真正中转(可 deleteWebhook 回滚)。验证: 前端 API `/api/v1/privacy-policy` 返回含 Telegram 条款 + 版本 1.1。 |
| 2026-06-25 | **前端登录态安全: 登出服务端吊销 + 分析脚本ID净化 + CSP Report-Only** | 🟢 L3(安全加固·数据保护·只收紧) | 用户授权(分阶段·每步真机验) | 接当日登录审计的前端遗留项。**①前端XSS扫**: 顾客端 React 前端无用户/商家可控 HTML sink——全部 dangerouslySetInnerHTML=admin/config 可控(法务CMS页读data_settings + analytics ID注入), 商品描述顾客端不渲染HTML, 评价 {comment}/{reply} 走React转义=纯文本, URL参数同样转义, 无反射型XSS。WEB-1/6e84746那类存储型XSS在顾客前端无同类。**②纠正前条记忆/CHANGELOG**: "Passport token无过期"有误——实测 oauth_access_tokens(283行) null_expiry=0、expires=created+正好1年, **token固定1年寿命非永久**。用户拍板维持1年(personal access token无refresh, 缩短=周期性强制重登)、**不迁httpOnly cookie**(~30处localStorage读登录态要改+跨子域CSRF面+Google/PWA回归高, 当前XSS面近零=性价比差)。**③登出服务端吊销**: 新增 POST /api/v1/customer/logout(auth:api → token()->revoke()), 原**无登出端点**(登出仅前端removeItem token, 服务端有效到1年); 前端3处登出(AccountPopover/DrawerMenu/profile hub)清本地token前先best-effort调该端点(失败/超时静默不阻断)。端到端实测: 登录200→带token /customer/info 200→/logout 200→同token再访问 **401**。commit 后端14fde9b/前端39ab54c→nzdeploy。**④分析脚本ID净化**: _document 注入8家像素ID进inline script前净化为[A-Za-z0-9_-](commit 5c9310e), 堵后台被钓时ID断字符串逃逸。**⑤CSP 已切 enforce**: nginx前端proxy conf先加 Report-Only, 再 Playwright 8 页面态(home/餐厅/checkout/map-picker/登录modal/profile/local-life × 登录+游客)观察=0违规且实触发跨域源全覆盖(Google/Apple登录·maps·firebase·fonts), 补 youtube+media-src 后 flip 为强制 Content-Security-Policy; enforce 后复跑同8页=0阻断+全资源仍加载+截图渲染正常+console 0错。enforce 后又真机驱动稀有流程(用户要求别把风险留客户): 真实登录态 UI 加购→去结算→真带商品 checkout→地图选址→付款抽屉(TRC20/支付宝/BEP20+USDT地址+QR码+凭证上传)全开 0 阻断, 聊天消息中心 0 阻断; 唯一未触发=analytics像素(本环境未配ID, 已预白名单); script-src 仍 unsafe-inline/eval(Next.js需,故挡不了inline XSS,价值在connect/frame限token外泄); token TTL维持1年未缩。⚠️CSP在宝塔生成proxy conf,面板改代理会覆盖,改后须复查。 |
| 2026-06-25 | **客户端登录鉴权审计 + 修复2条中危(reset爆破·账号预劫持)** | 🟢 L3(安全加固·只收紧) | 用户授权 | 全面审计顾客登录(实查生产: 仅开 manual账密 + Google; APP_MODE=live 故测试万能码123456后门未激活; 全局限流实测5次/60s/IP即429有效; Google aud 校验在位; token存localStorage+Passport无过期). **修复#1**: 密码重置 submit 端原先无 token TTL + 无 per-account 锁定(verifyToken 的锁定可被跳过), 6位码永久有效→可分布式爆破; 加 10分钟 TTL + 镜像 verifyToken 锁定。live 实测: 20分钟旧token被拒(400)/新鲜token成功(200)/成功后删token。commit 87a1a4d→nzdeploy。 **修复#2**: 注册不验邮箱 + Google 自动合并 → 账号预劫持(攻击者用受害者邮箱预注册设密码, 受害者日后 Google 登录合并进该既有账号但旧密码仍在→攻击者保留手动登录权); 改为 Google 合并进"邮箱从未验证"的既有账号时作废其旧密码, 真owner 用 Google 或"忘记密码"重设。live 反射实测: 合并后攻击者密码失效 + 已验证账号无回归。commit ca4bf79→nzdeploy。 **潜在地雷(未触发, 记录备查)**: OTP 自动建号密码=手机号(otp_login_status=0 时前端不暴露+0个此类账号, 不可利用; 开 OTP 前必先改); APP_MODE=test 后门(当前=live)。 **待办**: 前端 token 存 localStorage + Passport 无过期 + 登出无服务端吊销, 留作另开"前端 XSS + token 生命周期"审计。 |
| 2026-06-24 | **反馈收集 对抗审计修复: PII脱敏加固 + 埋点防刷 + 时区/归纳准确性** | 🟢 L3 + 🔴L1-7相邻(收紧数据保护, 不放松) | Claude多实例对抗审计(8确认/10误报) + 用户授权 | 对方案A/B/C上线后做多实例对抗审计, 确认并修8条(均L3可逆不碰资金): ①PII redactPii 收敛单一口径(NezhaUsageLog::redactPii, 日报复用防漂移)并补带空格电话/银行卡(先压缩数字间分隔再抹≥7位串), 修埃里温分段写法(+374 99...)与空格卡号此前漏脱敏后被每日喂第三方AI(DeepSeek)的缺口; ②日报搜索词在写库counts+喂AI前再脱敏一次(读取端纵深防御); ③nezha_search_misses 加唯一索引+原子upsert去重消竞态+zone_id入库前Zone::exists校验, 封死匿名缺词表被刷爆/zone枚举放大; ④时区: created_at按埃里温墙钟存储而绑Carbon对象会转UTC(带Z)致日报窗口偏-4h, 改绑墙钟字符串对齐昨天整日; ⑤日报顾客发客服消息排除admin/AI回复(按user_infos.admin_id过滤)防污染归纳; ⑥orders加复合索引加速purge回填。IDOR/越权/XSS/CSRF/批量赋值经对抗复核未发现可利用问题(10误报/info)。已nzdeploy(8c21867), 工作树验证全过。 |
| 2026-06-24 | **反馈收集体系(方案A/B/C): AI反馈日报 + 商家反馈入口 + 两轻量埋点** | 🟢 L3 + 🔴L1-7相邻(新增含PII表+保留期清理; 新增"脱敏后喂第三方AI"的批量数据流) | 用户(要求 A→B→C 全做) | 主动收集顾客/商家反馈三件套。A(AI反馈日报): nezha:feedback-digest(每日06:00)把窗口内 评价/差评原文+退款原因(reason_note)+客服高频问题 聚合, 复用既有已上线AI客服管线(nezha_cs_ai_*, 喂DeepSeek前一律 redactPii 抹电话/邮箱/钱包)生成摘要, 存 nezha_feedback_digests(只存AI产出摘要+确定性统计数字, 不存原始评价/聊天原文——原文仍在原表按既有purge到期删); 总开关 nezha_feedback_digest_status 默认0关; 性质=把既有已脱敏管线从单条客服扩到批量反馈, 未新开对外PII口子。B(商家反馈入口): 新表 vendor_feedback 存商家求助文本(可能含商家自填联系方式=中敏感PII, 表 ENCRYPTION=Y 静态加密符L1-7; 暂未挂自动清除, 列为后续待办)。C(轻量埋点): nezha_search_misses(搜索无结果词·不存user_id·入库前脱敏·天然匿名·冷词180天清) + nezha_cart_events(加购事件·含user_id为PII·30天清, 由 nezha:purge-analytics 每日03:25 离线回填转化+按保留期删, 不改下单/资金逻辑)。🔴 全程不碰钱/退款/链上(L1-1~6不变); 埋点对用户零打扰且失败安全(try/catch)。验证: 三方案 php -l/迁移DONE/内部派发渲染OK/dry-run真出DeepSeek摘要(713tokens)/埋点生产真实搜索路径端到端记录, 测试数据已清。同步 MERCHANT_GUIDE; ADMIN_GUIDE 因别窗占用(WIP)暂缓待补。 |
| 2026-06-22 | **逾期未退款 T2 升级为自动停接单 + 退款后自动恢复 + Telegram 提醒超管 + 后台手动干预** | 🟡 L2(风控处置流程升级; 严守 L1-1 零资金不变) | 用户(本窗口明确要求"T2 从建议手动停接单改为: 自动停接单+退完自动恢复+超管提醒+后台查看页") | 背景: 原 T2(逾期≥停接单阈值, 默认36h)仅发 escalate_t2 告警「建议运营手动停接单」, 商家长期不退全靠运营盯后台手动停, 抓手弱。改动: RefundOverdueSweep T2 改为幂等动作 auto_suspend——① 自动调 NezhaRefundOverdue::suspend() 置 restaurants.nezha_order_suspended=1(接单闸 is_suspended 在 Api/V1/OrderController order_validation_check 拦截下单, 文案「该商家暂时停止接单」) ② \App\CentralLogics\Helpers::sendTelegramToAdmin() 提醒超管(复用 nezha_risk_admin_chat_id, 未配自动静默降级不抛错) ③ 运营邮件留痕措辞由「建议停接单」改「已自动停接单」。自愈(已在既有代码, 本次未改): 商家在「订单→待退款」点「标记已退款」(merchant_refunded_at)→Vendor/OrderController::mark_refunded 调 lift_suspend_if_clear 自动解除挂起(该店无其它逾期留痕时); 运营仍可后台「风控中心→逾期未退款」手动停/解除/人工核实(overdueSuspend/Unsuspend/Resolve)。幂等: 复用 nezha_refund_overdue_events 唯一键(refund_record_id, action=auto_suspend)保证每留痕只自动停一次。🔴 L1-1 零资金全程不变: nezha_order_suspended 专属退款逾期、与履约保证金互相独立不扣减, 平台不碰钱/不代退/不给顾客打钱。总开关 nezha_refund_overdue_status 仍默认 0(关), 开=用户拍板生效日才自动停(且 cron 从 api-deploy/current 跑, 已 nzdeploy 上活路径)。同步 ADMIN_GUIDE 第19章 + MERCHANT_GUIDE 逾期章。验证: php -l 过; 内部派发 suspend/is_suspended/unsuspend 往返事务回滚无残留(before=0 after=0); admin 逾期页内部派发 HTTP 200 含新文案「自动停接单/系统自动停接单」。⬜ 未验(需上线日开关验): cron 真扫到逾期单→自动停一个真实商家 + Telegram 实际投递(避免开关开后旧码真发催办邮件给商家, 未在本次翻开关跑端到端)。 |
| 2026-06-22 | **退款逾期催办阈值调整: 催办 12h→4h, 建议停接单 72h→36h** | 🟡 L2(风控阈值; 严守 L1-1 零资金不变) | 用户(本窗口明确要求"催办调到4小时·停接单调到36小时") | 背景: 外卖即时消费退款要更快, 缩短逾期容忍窗口。改动: business_settings nezha_refund_overdue_remind_hours 12→4(逾期满4h: 记风控refund_overdue+催办邮件商家+告警运营), nezha_refund_overdue_suspend_hours 72→36(逾期满36h升级告警「建议停接单」, 仍运营手动停非自动)。🔴 全程零资金(L1-1平台不碰钱/不代退/不扣保证金赔顾客 均不变), 仅缩短阈值。总开关 nezha_refund_overdue_status 仍 0(关), 线上零即时行为变化(开关开后才按新阈值生效)。读取经 NezhaRefundOverdue::thresholdHours() 直查 BusinessSetting(非缓存)实测返回 4/36。同步 ADMIN_GUIDE / MERCHANT_GUIDE / INVARIANTS L2 阈值描述。 |
| 2026-06-22 | **修复: KYC 姓名筛查记录的处置审计文案 KYC 化(原复用订单语境)** | 🟢 L3(审计留痕措辞, 不改状态流转/机制; 因属 L1-6 制裁审计字段故留痕) | QA(E 轴发现)+集成窗修 | 背景: 94cb17a 给风控队列 KYC 记录拆了按钮(核实无误·解除/确认命中·拒绝)但「approve/reject 动作逻辑零改动」, 致 NezhaRiskController::approve/reject 给 KYC(无订单)记录仍写订单语境 disposal_result(「放行·顾客可在宽限期内重新下单付款」/「清退该订单」)+Toastr——在 L1-6 AML 审计留痕(≥5年)里读着不对。修: approve/reject 加 isKycRecord(hit_rules 含 sanction_kyc_name) 分支, KYC 记录改写 disposal_result=「核实无误·解除疑似标记(已人工核对非同一人)」/「确认命中制裁名单(法人·受益人姓名命中 OFAC SDN, 拒绝入驻 L1-6)」+ 对应 KYC 语境 Toastr; **订单记录文案/状态流转零改动**(已验订单分支仍原文案)。验证: 内部派发真 POST approve/reject KYC 记录→disposal_result 落 KYC 文案、订单记录仍订单文案; php -l 过, 测试数据已清。纯文案 L3, 不触状态机/资金/退款。 |
| 2026-06-22 | **商家回传配送链接(Yandex): admin 后台订单详情可见 + 到期自动清除(数据最小化)** | 🟢 L3(admin 只读显示) + L1-7相邻(PII 数据最小化, 收紧不放松) | 用户批准("这两个改") | 背景: 商家回传的 Yandex 配送追踪链接此前仅商家端/顾客端可见, admin 后台看不到(处理"我没收到餐"客诉时无从核查商家是否叫车); 且该链接本质是"顾客位置的实时链接", 终态后无保留价值却长期留库。改动: ① admin 订单详情(admin-views/order/order-view.blade)新增「配送链接(Yandex)」行——仅配送单显示, 贴了显示可点链接/顾客已催未回传/未回传三态, 供平台核查。② 新增 nezha:purge-yandex-links(每天03:55, 接 bootstrap/app.php->withSchedule): 把终态(delivered/canceled/refunded/failed)且 updated_at 超保留期的订单的 yandex_tracking_url + delivery_link_reminded_at 两字段置空, 【只清这两个字段, 保留订单行与全部财务/状态】(订单是财务记录, L1-4/合规另行留存 ≥5年)。保留天数 business_settings.nezha_yandex_link_retention_days 默认10, 开关 nezha_yandex_link_purge_status 默认1开。🔴 不碰钱/不碰退款/不碰链上记录, 仅 admin 只读显示 + 单字段数据最小化。验证: 事务回滚仿真直接跑命令——A(终态+40天前)CLEARED / B(终态+5天内)KEPT / C(配送中+40天前)KEPT, A 的 order_status/order_amount 财务字段 intact, 零落库; dry-run + admin blade 编译通过。 |
| 2026-06-22 | **(补记) 商家KYC基础模块上线(轻量·方案B): 新表 vendor_kyc_profiles 存核验结论** | 🔴 L1-7相关(新增加密PII表 + AML/CDD ≥5年留存豁免PII清除) | 用户批准(同 commit 11a25a3 基础模块) | 补记 commit 11a25a3 的合规留痕(此前仅在代码注释/commit message, 未入本表)。新表 vendor_kyc_profiles 存运营当面/视频核验【结论】: legal_name/id_doc_number/bank_account/contact_phone 等为 PII, 模型层 encrypted cast + 表 ENCRYPTION='Y'(静态加密, 符 L1-7 加密); 默认不上传证件扫描件以降 PII 负债。🔴 留存例外: 作为 AML/CDD 记录须留存 ≥5 年, 故 vendor_kyc_profiles 明确**不进 PII 清除任务**——此为对 L1-7「PII 到期删」的合规豁免(反洗钱法定保存义务优先), 建议在 INVARIANTS L1-7 与 data-protection.md 正文显式登记(本次仅补 CHANGELOG, 正文改红线措辞待用户确认)。后台 NezhaKycController(index/edit/save/review)+4 路由+风控中心侧栏入口; 总开关 nezha_kyc_required_status 默认 0 关, 不影响存量商家。验证(2026-06-22 KYC后台处置专项): index/edit/queue 内部派发均 HTTP 200; 三处置按钮(保存转待审核/审核通过/拒绝 + 风控队列 核实无误·解除/确认命中·拒绝/去KYC处置)真实渲染可见; 制裁筛查 clear/hit/possible 三态 tinker 实测通过; QA 数据已清(profile+risk record 删除归零)。 |
| 2026-06-22 | **退款体验大改: 信任闭环通知 + 顾客「催一下」+ 文案反转(平台主动态/敬语您) + 催办引擎改小时级** | 🟢 L3(展示/通知/入口; 平台不碰钱零改动) + 🟡 L2(催办阈值天→小时) | 用户定调"MVP先这样" | 外卖即时消费顾客退款心态="立刻要", 但B方案平台不碰钱给不了即时到账(托管escrow=改根基碰二清, MVP明确不做)。解法=平台立刻替顾客催+商家尽快退+文案不晾人: ①Piece1信任闭环(c2bd561): 商家「标记已退款」→顾客站内信+FCM; 顾客/商家/系统触发待退款留痕→邮件超管(admin不重复发) ②Piece2催一下(3088a73): POST customer/order/nudge-refund(对象级鉴权防IDOR+6h Cache防刷+商家站内信&Telegram), 追踪页待退款卡置顶醒目实心按钮"催一下商家·请尽快退款" ③文案反转(5370630/b92dff7): 删一切"超过X天"等待语→"我们已通知商家立即为您原路退款"+全部敬语"您"(对客户与商家) ④Piece3(a3fb420): 逾期催办引擎按天改小时(remind 12h/suspend 72h=3天, cron dailyAt→hourly每小时, 新键*_hours优先缺则回退旧*_days×24兼容), 总开关nezha_refund_overdue_status仍默认0关。零资金(L1-1平台不碰钱/L1-2,3只原路退 均不变), helpers单测+dry-run(15h单T1触发T2未触发零写入)验证, 测试数据已清。⬜前端(催一下按钮/文案反转)待主窗口build。 |
| 2026-06-22 | **商家KYC制裁名字筛查上线(阶段1): 入驻法人/受益人姓名比对 OFAC SDN 人名名单** | 🔴 L1-6(制裁筛查,新增能力收紧不放松) | 用户批准("批准,按方案做") | 缺口: 既有 nezha:sync-sanction-list 解析 OFAC SDN XML 时只取数字货币地址(nezha_sanction_addresses),人名/别名/实体名全丢→入驻「制裁名单命中即拒」对人名空跑。补法: ①新表 nezha_sanction_names(公开名单非PII不加密)②扩展 SyncSanctionList 多解析 firstName/lastName/akaList+sdnType+programList,失败安全(解析0条保留旧名单),实测落库 43,576 条 ③新 NezhaKycScreen::screen_name() 规范化(去音标/大写/去标点/压空白)精确比对→hit;token子集或≥2重叠→possible转人工(写 nezha_risk_records 复用风控①队列/日志,rule含sanction)④挂 NezhaKycController::save()(录入法人+受益人名筛,写回 screen_*)+ VendorController::store() 自注册非阻断筛(命中/疑似进风控队列,商家本就 status=0 待审)。开关 nezha_kyc_sanction_screen_status 默认1开。实测 hit/possible/clear 三态+全链+风控页渲染均通过,测试数据已清。🔴局限: 仅精确+词重叠,漏音译变体(Mohammed/Muhammad、Aleksandr/Alexander、中文不同拼音),不做模糊/语音/编辑距离/商业名单→非完备制裁合规,仅入驻初筛+人工复核线索,完整方案待后续。留存: 人名表为公开名单缓存,无PII不进清除任务。 |
| 2026-06-21 | **每日加密备份增异地副本(Cloudflare R2 off-site)** | 🟢 L3(强化L1-4留存可靠性,收紧不放松) | 用户批准(选R2方案) | 此前每日加密备份(DB+storage/app+.env)仅在本机同盘,丢机/丢盘/勒索即丢含≥5年留存的 nezha_refund_records。新增:备份成功后 rclone 推一份加密件到 Cloudflare R2(异地对象存储),copy 只增不删防级联删,远端独立保留30天;出站强制 --bind IPv4 178.105.216.158 匹配 R2 token IP白名单(双栈默认IPv6会403)。失败双告警:备份脚本即时邮件+nzwatch 每5min 查 offsite_last_ok>26h 告警。实测:真跑一次→R2收到→从R2下载解密解压得完整有效 MySQL dump(端到端可恢复)。R2上是加密副本,backup.key 仍须离线另存否则副本=废。 |
| 2026-06-21 | **隐私政策§3加(6):在线客服/翻译经第三方AI(DeepSeek)处理+数据跨境披露** | 🔴 L1-7相关(数据出境披露) | 用户批准("这个处理掉") | 配合 ea2d179 AI客服:翻译/运营助手会把顾客消息发DeepSeek(服务器在中国)→个人信息跨境传输。正本docs/legal/privacy-policy.md+线上data_settings(id=154,旧值备份key _privacy_policy_live_backup_202606211819)同步加第3条(6)项:披露第三方AI处理+跨境+运营统计已去标识化+提示勿发非必要敏感信息。清data_settings缓存,nginx 60s自动过期。 |
| 2026-06-21 | **AI客服运营助手喂模型前抹PII(电话/邮箱)** | 🟢 L3(强化L1-7,收紧不放松) | Claude实施(用户"全部都修") | adminAssistant 把顾客原话/差评原文发DeepSeek(数据出境中国)做运营归纳,顾客消息可能含电话/邮箱PII。新增 redactPii() 在喂模型前正则抹电话(含亚美尼亚号)/邮箱。注:①翻译功能因需译原文无法脱敏;②地址类PII正则难尽抹;③「客服/翻译消息可能经第三方AI处理、数据出境」的隐私政策披露仍待用户+律师确认。 |
| 2026-06-21 | **风控通道收口服务端(子项B补2): 复评不信任客户端 payment_channel** | 🟡 L2(风控行为:仅改通道如何判定,阈值/开关未变) | 用户批准 | 缺口: build_context 读客户端自由串 payment_channel 选通道阈值可伪报换松阈值(线下具体通道下单后才提交凭证,下单时服务端无法权威得知)。修法: evaluate_server_authoritative 由 order->payment_method 权威判定——非线下用 other(法币阈值); 线下通道未定则对 rmb/usdt 两套阈值取最严(reject>review>pass),伪报换不到更松阈值; 阈值相同(现 rmb=usdt)则零行为变化,分化自动 fail-closed。新增 NezhaRiskChannelTest(3测)接入 composer test:redlines+pre-push门防回归(现9测19断言)。
| 2026-06-21 | **风控入参堵漏(子项B补): place_order 按服务端权威金额复评风控** | 🟡 L2(风控行为变更:仅改评估用哪个金额,阈值/开关未变) | 用户批准("修了") | 堵上一行登记的待批缺口: build_context 早评读客户端 order_amount,顾客低报可绕单笔/大额风控。修法:服务端金额重算定型后(save前·事务内)用权威金额复评,命中即 rollBack+记审计(record 在 rollBack 后调用)+返回 risk_reject/risk_review。真单实测:低报1+真实112500>单笔110437→risk_reject·未建单·审计持久;正常4500→下单成功不误伤。
| 2026-06-21 | **安全架构化三件套(IDOR结构守卫 / 金额前端值不读 / L1红线CI断言)** | 🟢 L3(强化合规,不改L1机制) + 🟡 L2待批1项 | 用户已批准方案·Claude实施 | A(IDOR根治): 新增 `App\Traits\OwnedByCustomer::forCustomer`(owner为null即抛异常,绝不退化成全表无过滤)+ `tests/Feature/NezhaIdorGuardTest`(扫顾客控制器,裸by-id查归属模型且方法内无任何归属过滤即fail,防回归;负向植入探针验证能抓)+加固 set_default_address 裸update。评估结论:共享模型被admin合法跨查/cron无登录态,Global Scope会误伤后台与扫单,故用forCustomer+CI守卫而非Global Scope。B(金额前端值不读): place_order删除`$order->order_amount=$request['order_amount']`早赋值(原本就被服务端按DB商品价重算无条件覆盖),校验改nullable;真实下单谎报order_amount=1→响应/存库均4500验证客户端值被丢弃。C(L1红线CI化): `tests/Feature/NezhaL1RedlineTest`把L1-1平台不碰钱(digital_payment/wallet_status/add_fund_status须全0)、L1-2/3退款只原路(lock_route仅$order参数+禁止退第三方)、L1-5二清腿已拔(if(!$is_direct_pay)守卫包住total_earning)、L1-6制裁命中即拒(匹配器命中/miss正负对照)写成自动断言;`composer test:redlines`+git`pre-push`门(红则拒推,本仓所有窗口受门)。**待批L2缺口**: `NezhaRiskController::build_context`仍读`$request->order_amount`做风控阈值,顾客低报可绕单笔/大额风控(既有缺口非本次引入,L2);修法(重算后复评/早算服务端金额喂风控)待用户拍板,改前留痕告知。 |
| 2026-06-21 | **新增 USDT·BEP20 收款 + BEP20 链上核验修稳** | 🟢 L3(新增收款链路展示+只读核验; 平台不碰钱零改动) | 用户(要求加币安BEP20便宜链; 经澄清「币安内部转账无链上哈希不可自动核验」后定「加BEP20链上USDT·顾客自选便宜链」) | **背景**: 波场TRC20 手续费略高(~1USDT), 顾客想要更便宜的链; BEP20(币安智能链)手续费更低(~0.3–0.8USDT)。**改动**: ① restaurants 加 usdt_bep20_address(usdt_address 保持 TRC20); 收银台三块砖(支付宝/USDT·波场TRC20/USDT·BEP20)按商家配了哪个地址条件显示, 顾客自选便宜链。② 复用停用的微信支付方式槽位 id=3 → USDT·BEP20。③ store 端点按所选方式分流网络+应收地址再链上核验。④ 后台商家收款页拆两地址+说明(两码区别/原理/手续费差异/务必填自有钱包别填交易所充值地址)。⑤ **修稳 BEP20 核验**: NezhaRefundControl::bsc_rpc 改 6 个公共 BSC RPC 多节点 failover(单点 bsc-dataseed 会限流/节点滞后)——顺带修好现有退款/制裁的 BSC 核验; NezhaChainVerifier 按链规范化哈希 0x(BSC 回执查询需 0x 前缀)。**关键定性**: BEP20 与 TRC20 **同样只读链上核验真到账**(非「商家自己验」, 已实测), 不移动资金、不自动放款。**真免手续费的币安内部转账(Binance Pay)因链下无哈希不可自动核验, 本次未做(留商户 API 另立项)**。**验证**: 真实 BEP20 USDT 交易端到端 HTTP 提交→nezha_auto_check.usdt_network=BEP20 chain.status=verified(到账20USDT/to_match/confirmed/explorer=bscscan); 商家页渲染 HTTP200 含「链上已核验到账」+bscscan 链接; TRC20 回归仍 verified。⬜ 前端三块砖真机渲染待构建(他窗 TrackingPage WIP 未提交, 暂未构建)。 |
| 2026-06-21 | **离线收款 USDT 核验改为复用现有链上设施（去重复）** | 🟢 L3(实现复用; 机制/契约/资金零改动) | 用户(指出「之前已办过验 USDT 的 key」、要求改为复用) | **背景**: 当日新加的 NezhaChainVerifier 自引了 Tronscan(keyless) 查链, 但哪吒早有一套链上设施——NezhaRefundControl(退款反查 + 制裁筛查 OFAC)已用 TronGrid(/v1 events)+BSC RPC, 且 business_settings.nezha_refund_trongrid_api_key 已配一把 36 位 TRON-PRO-API-KEY。属重复造轮子(违「先查复用」)。**改动**: NezhaChainVerifier::verifyUsdt 改调 NezhaRefundControl::verify_refund_tx(网络映射 TRC20→trc20 / BEP20→bsc), 读其 detail.to/amount 后按本类 3% 容差与中文状态判定; 删掉自引的 Tronscan 调用与官方合约常量。**收益**: 复用已配 key(无需另办)、白捡 BSC(BEP20)核验、复用已踩平 base58 大小写坑的成熟解析、全站查链统一一套设施一把 key。**不变**: offline_payments.nezha_auto_check 结构、商家订单页徽章展示、L3 只读不碰钱定性全不变。**验证**: 经复用路径(真用已配 TronGrid key)对真实 USDT-TRC20 交易重测 5 分支全过(verified 到账7000USDT·to_match / 地址不符 mismatch / 金额不足 mismatch / 格式错 invalid_hash / 查无 not_found)。 |
| 2026-06-21 | **顾客接单后申请取消(申请→商家裁决) + 商家主动拒单/无法接单** | 🔴 L1边界(订单状态机 + 退款触发时机; 严守 L1-1 平台不碰钱) | 用户(就阶段/整套方案两问拍板: confirmed+processing 可申请 / 批准按方案做) | **背景缺口**: 顾客原只能 pending 自助取消, 商家一接单(confirmed)顾客就无任何取消入口、只能线下找商家; 商家也无正经拒单能力(canceled_by_restaurant 默认关 + confirmed 拦死, 只能打回凭证或晾到超时)。**改动(三条路共用统一收尾 OrderLogic::finalize_cancellation)**: ① 顾客 PUT /customer/order/cancel-request: 仅 confirmed/processing 可申请(handover/picked_up 已出餐/配送中一律拒, 遵 ORDER_TIMEOUT_RULES D/E), 必填理由, 防刷(已申请不重复 / 被拒10min节流), 只登记 nezha_cancel_request=requested + 通知商家、**不改 order_status**(订单继续履约); ② 商家裁决 PUT /vendor/order/cancel-request-decision: approve→finalize_cancellation(canceled_by=customer) / reject→nezha_cancel_request=rejected + 存商家理由 + 订单回原状态继续履约 + 通知顾客被拒+原因; ③ 商家主动拒单 PUT /vendor/order/reject: 仅 pending/confirmed(handover/picked_up 绝不可), 必填理由, finalize_cancellation(canceled_by=restaurant)——**不开全局 canceled_by_restaurant 开关**(避免商家可取消任意阶段单); ④ orders 加 nezha_cancel_* 5 列(迁移); ⑤ track 下发 nezha_cancel meta 供前端显示申请入口/申请中/被拒卡; ⑥ 补 Vendor status:541 取消已付直付单的 record_direct_pay_refund_pending **留痕缺口**(原只写 canceled_by/reason 漏建留痕)。**🔴 L1-1 守住**: finalize_cancellation 对已付直付单只 mark offline_payments=canceled + 生成 nezha_refund_records(pending_merchant_refund) 留痕 + 通知顾客「联系商家原路退」, 平台**绝不自动退款/不碰钱**; reject(继续履约)不产生任何退款留痕。**验证**: 后端 25 项断言全过(真调 api/vendor 控制器方法: 顾客申请→200/防刷→403/handover→403; 商家 approve→canceled+canceled_by=customer+pending_merchant_refund 留痕+零平台 Refund 行+payment_status 未自动退; reject→订单不变(processing)无留痕; 主动拒单 pending paid→canceled+canceled_by=restaurant+留痕; 拒单 handover→拦截不动), 测试单全清+food 计数还原; 前端 Playwright iPhone 真机三态(申请入口/申请中/被拒)+弹层提交流 console0; live route 返回 401(已 wired)。提交: 后端 d040747(共享index并发被他窗一并提交)+229ea64(补 cancel_request 方法体), 前端 6b02277。 |
| 2026-06-21 | **商家逾期未退款兜底约束(记风控+催办+告警+运营手动停接单)** | 🟡 L2(风控流程/阈值; 严守 L1-1 平台不碰钱、不代退、不扣保证金赔顾客) | 用户(就停接单方式/阈值默认/总开关三问拍板: 运营手动停 / 催3天停7天 / 总开关默认关) | **背景缺口**: B方案点对点直付, 平台取消/退款直付单后只生成 nezha_refund_records(status=pending_merchant_refund)留痕+通知商家原路退, 但无任何机制约束商家真去退; 商家不退也不点「待退款」mark_refunded, 顾客的钱一直卡着、平台无强制力。**改动(全部非资金)**: ① 新增 artisan `nezha:refund-overdue-sweep`(每天09:30, 接 bootstrap/app.php->withSchedule): 扫 pending_merchant_refund 且 created_at 超阈值天数仍未 merchant_refunded 的留痕, 经新账本表 nezha_refund_overdue_events(唯一键 refund_record_id+action, 仿 nezha_order_timeout_events)幂等保证每留痕每动作恰好一次——逾期 remind_days(默认3): 写 NezhaRiskRecord(rule=refund_overdue, action=review, 进商家风控档案)+催办邮件商家(NezhaRefundOverdueMail, 引导原路退+去后台标记)+告警运营 support@; 逾期 suspend_days(默认7): 升级告警「建议停接单」。② 停接单走「与钱无关的接单挂起标记」(restaurants 新增 nezha_order_suspended/nezha_suspend_reason/nezha_suspended_at): 复用 Api/V1/OrderController::order_validation_check 两处接单闸各加一条 match 臂(紧邻既有 nezha_deposit_low 臂, status_code=403+中性中文文案不泄露退款信息), 由运营在后台「风控中心→逾期未退款」手动一键停接单(留人工复核口子防误伤已退忘标记的商家); 商家在「订单→待退款」mark_refunded 或运营「人工核实已退款」后, 若该店已无逾期留痕则自动解除挂起(NezhaRefundOverdue::lift_suspend_if_clear)。③ 新逻辑全隔离进 app/CentralLogics/NezhaRefundOverdue.php(suspend/unsuspend/lift/is_suspended), 后台 NezhaRefundController 加 overdue/overdueSuspend/overdueUnsuspend/overdueResolve + 视图 admin-views/nezha-refund/overdue.blade + 侧栏入口。**🔴 L1 红线守住**: 全程零资金操作——不从保证金扣钱赔顾客、不代退、不向顾客打钱; 停接单与保证金相互独立不扣减; 实际退款永远商家原路退。**开关**: nezha_refund_overdue_status 默认 0(关), 线上零行为变化, 测试+本次拍板后仍保持关、待用户决定何时开。**验证**: 造一条回填8天的 pending_merchant_refund 测试留痕(挂 demo 店6, 测试期临时清空店6/商家邮箱防发真催办)→ --dry-run 命中4动作零写入 → 真跑写账本4行+风控记录1条(rule=refund_overdue), 幂等再跑0动作; 零资金确认(本测试订单 restaurant_deposit_transactions=0、留痕金额未动); 接单闸 order_validation_check 真调用返回 status_code=403+中文「该商家暂时停止接单」; 自动解除两分支(店6有别的pending→守卫不解除 / 店7无pending→suspend后lift解除); 测后全清(删测试留痕/账本/风控记录、恢复邮箱、店6/7无挂起、开关回0、临时key删净); 6 个被改 PHP 文件 php -l 全过。 |
| 2026-06-21 | **离线收款自动核验: USDT 上链核验 + 法币实付金额比对 + 凭证图片软门** | 🟢 L3(只读核验辅助展示; 资金机制/平台不碰钱零改动) | 用户(就图片把关/金额核对/USDT上链三问拍板: 软提示+仍可交 / 顾客填实付比对 / 现在就做上链核验) | **背景**: B方案下顾客直付商家、平台无支付回调, 商家此前只能靠看截图+自查账户判断到账, 截图可伪造。**改动**: ① 新增 NezhaChainVerifier(只读查 Tronscan, 官方USDT-TRC20合约写死): 顾客填交易哈希→核对收款地址=商家usdt_address、币种USDT、金额≥应付(容差3%)、区块已确认, offline_payments 加 nezha_auto_check(JSON)存结果; 哈希防重用(别单用过=可疑)。② 商家订单页显示「本单应收 ֏X(≈¥/≈USDT)」大字 + 交易哈希跳 Tronscan + 核验徽章(绿:链上已核验到账 / 黄:待区块确认 / 红:不符/查无/哈希无效)。③ 顾客端: USDT 哈希格式前端校验(64位hex)、支付宝实付金额选填输入与应付软比对、付款截图 canvas 软门体检(过小/接近空白/疑似模糊→软提示仍可交, 标记 nezha_image_flags 给商家)。**关键定性**: 全程只读核验、**不移动资金、不自动放款/自动确认**(商家仍须人工点确认收款)、任何核验异常都吞掉不阻断下单; 图片/金额核对全为软提示**永不误伤真实顾客**。不构成代收/二清, L3。**验证**: NezhaChainVerifier 对真实 USDT-TRC20 交易实测 5 分支(verified/地址不符/金额不足/格式错/查无); 真实 HTTP 提交端到端→nezha_auto_check.chain.status=verified(到账7000USDT/to_match/confirmed); 商家订单页内部派发渲染 HTTP200 含全部徽章文本; 前端 next build 通过+部署健康门200。⬜ 顾客抽屉新增控件真机渲染待用户亲测(无头结算页因地址门空态白页, 非本改动 bug)。 |
| 2026-06-20 | **广告计费 T1: 综合排序注入付费曝光权重** | 🟡 L2(排序行为; 只影响排序、不伪造数据, 非资金) | 用户(广告计费方案v2锁定 T1; 常规推进长期授权) | **背景**: 综合排序(home filter=all)综合分 = 0.45距离 + 0.30评分 + 0.25销量(RestaurantLogic.php::get_restaurants orderByRaw)。T1 给付费广告加曝光权重。**改动**: 综合分 + nezha_ad_boost_weight × EXISTS(该餐馆有 is_paid=1 + 审核通过 + 投放期内 广告)。boost 钳到 [0,1.0] 防排序失真(综合分上限≈1.0), 设 0 即关闭加权(weight>0 才注入 EXISTS 子查询, 关时零开销)。只改排序、不伪造数据/不改卡片内容。**⚠️ 营业优先**: orderBy(open desc) 是主排序, 加权只在营业(open=1)组内重排——打烊店的广告在打烊时段不会被顶到营业店之上(避免顾客点进已关门的店), 等其开门加权再生效。**零行为变化保证**: 计费总开关关时无广告会 is_paid=1(扣费命令不跑) → EXISTS 恒 0 → 排序完全不变(线上当前 0 广告即如此)。**验证**: 真跑 get_restaurants(filter=all, zone[3,2]): 基线[6,9,8,7,10,11,12] → 给营业组最靠后店#11 建付费广告 → 升至#1[11,6,9,8,7,10,12]; weight=0 时顺序==基线(可关); 删广告回基线; 店#12(打烊)始终垫底验证营业优先; 6 断言全过, 测后还原(权重回0.5/删广告)。**待办**: T1b 前端广告位组件 / T4 INVARIANTS+ADMIN_GUIDE+MERCHANT_GUIDE 文档。 |
| 2026-06-20 | **广告计费 T4 文档落地: INVARIANTS/ADMIN_GUIDE/MERCHANT_GUIDE 补广告服务费** | 🟡 L2(仅文档, 机制/开关零改动) | 用户(常规推进长期授权) | 广告计费方案 v2 收尾的 T4 文档项(T2/T3/T3细化 已上线见下几行)。**改动(纯文档, 不动任何代码/机制/开关)**: ① INVARIANTS.md L2 表新增「广告计费总开关 nezha_ad_billing_status(默认0) + 单价/曝光加权/平台下架退费 三参数」, 注明 advertisement_fee 为保证金新扣款类型、平台收自有广告服务费非二清; ② ADMIN_GUIDE.md 新增第18章「商家广告计费」(广告计费设置页四开关含义 / 审核不扣费 / 起始日定时扣费 / 在保证金流水看 advertisement_fee / 「下架并退费」按比例退 / L2合规口径); ③ MERCHANT_GUIDE.md 新增「投广告买曝光」章(按天计费 / 从预存佣金保证金扣 / 扣费后不可取消不可退原文确认框 / 余额不足不投放充值 / 平台下架才按比例退)。**口径对齐**: 总开关默认 0(线上仍免费投放、零行为变化), 扣费机制与 T3 实装一致(单价×天数、is_paid 幂等、平台下架按未投放天数比例退、商家自停不退)。属纯文档无需功能验证。 |
| 2026-06-20 | **广告计费 T3 细化: 暂停不退费/下架并退费分离 + 商家编辑已扣费广告守卫** | 🟡 L2(退费触发条件收窄; 仍 advertisement_fee 冲正, 非二清) | 用户(明确要求: 后台给「暂停不退费」的区分 + 补商家编辑守卫·不允许已扣费广告改期) | **背景**: T3 初版让超管「暂停」一律退费+解付(is_paid=0), 但运行中广告在后台只有「暂停」按钮、超管日常暂停≠想终止退费, 会误退; 另商家 update() 可把已扣费广告改回 pending 并改 end_date 实现免费延期(只守了取消/删除未守编辑)。**改动**: ① 退费触发从「status∈{paused,denied}」收窄为「显式 refund=1 且 status∈{paused,denied}」: 普通「暂停」(Admin@status 无 refund 参数)不退费、保持 is_paid=1 可恢复; 仅后台新增「下架并退费」按钮(details.blade, 仅 is_paid=1 广告显示, 提交 status=paused&refund=1)触发按未投放天数比例退。② 商家 Vendor@update 加守卫: is_paid=1 直接返回错误不可编辑(防改期免费延期/改内容绕审核), 未扣费仍可编辑; 已扣费如需变更请新建广告。③ 商家自停/Vendor 路径仍永不退(不受影响)。**验证**: demo 店9 真跑 17 断言全过: R1 普通暂停不退费(余额不动/is_paid=1/无退费流水) / R2 refund=1 按比例退 2000(=3000×2/3)+is_paid=0 / R3 二次下架幂等不重退 / 商家编辑已扣费广告被拦(返回 errors+end_date 未改+status 未回 pending); 测后还原余额+删数据+开关还原。blade compileString 通过。**未改**: 资金机制/扣费命令/起始日扣费逻辑不变; 总开关 nezha_ad_billing_status 仍默认 0。 |
| 2026-06-20 | **新增平台广告服务费扣款引擎(广告计费 T3)** | 🟡 L2(保证金新扣款类型 advertisement_fee; 平台收自有广告服务费, 非二清) | 用户(广告计费方案v2已锁定, T2已上线 1444e24/6ec0bed/d1407fc; 常规推进长期授权) | **背景**: 广告计费方案 v2(docs/PLAN_advertisement_billing.md)已锁定, T2(price/paid_at/deposit_transaction_id 字段 + 4 配置 + 商家提交页不可退确认框)已上线; 本次 T3 做扣款引擎。**改动**: ① 新建 artisan `nezha:charge-ad-on-start`(每小时调度, 接入 bootstrap/app.php->withSchedule, Laravel12 后 Kernel::schedule 已失效): 到投放起始日(Asia/Yerevan)对 status=approved 且 paid_at IS NULL 且 price>0 的广告, 从商家保证金 restaurant_wallets.deposit_balance 扣全额(单价×天数), 写流水 type='advertisement_fee'(新类型, 与 commission_deduction/recharge/refund_reversal 并列, amount=-price, commission=price, balance_after 快照), 置 is_paid=1 + paid_at=now + deposit_transaction_id。事务内 lockForUpdate 防并发 lost-update(仿 OrderLogic 佣金扣款样板), paid_at IS NULL 为唯一幂等防重闸 + 事务内二次复查。② 余额不足: 跳过不投放 + 发 DepositLowBalanceMail 提醒充值(不做宽限重试)。③ 商家取消守卫(Vendor AdvertisementController@destroy): is_paid=1 锁死不可取消/删除; 未扣费(is_paid=0)仍可免费取消。④ 平台/超管强制下架退费(Admin AdvertisementController@status 的 paused/denied 分支): is_paid=1 且 nezha_ad_refund_on_platform_takedown=1 时按未投放天数比例退回保证金(正数 advertisement_fee 冲正流水), 商家自停永不退; 退后置 is_paid=0 防二次退, paid_at 保留作永久'曾扣费'标记(扣费命令要求 paid_at IS NULL, 永不会被重扣)。**为何非二清(L1-1)**: 平台收取的是自家广告服务费(平台向商家收), 从商家'预存保证金'扣, 全程不归集/不经手顾客资金, 不存在'代收再分发', 故定 L2 非 L1。**开关**: 总开关 nezha_ad_billing_status 默认 0(关), 关时命令直接返回零扣费、线上零行为变化; 本次验证后已还原为 0。**验证**: 全程用 demo 店 6/7/8/9 真跑(测完精确还原 4 钱包余额 + 删测试广告/测试流水 + 开关还原 0, 线上回测前基线): ①余额充足扣款(余额-3000 / 流水 -3000 / is_paid=1 / paid_at / dtid, 9 检查) ②余额不足跳过不投放(4) ③幂等连跑 2 次只扣 1 笔(2) ④并发双进程只扣 1 笔且余额不扣穿成负(5) ⑤平台下架按比例退 2000(=3000×未投放2天/3天) + 二次下架幂等不重退(10) + destroy 守卫已扣费拦截/未扣费放行(4); 共 43 断言 PASS=43 FAIL=0。**待办(同任务链)**: T1 综合排序加曝光权重 / T1b 前端广告位组件 / T4 ADMIN_GUIDE+MERCHANT_GUIDE 手册。 |
| 2026-06-20 | **三份法律页顶部新增可见「最后更新 · 版本」展示行** | 🟢 L3(CMS展示行; 资金/退款/机制零改动) | 用户(就#2「加可见最后更新日期/版本」拍板: 执行) | **背景**: 《用户协议》§12/《隐私政策》§10 均声明"更新后公示并标注更新日期、继续使用视为接受更新", 但线上页面无任何可见版本/日期, 举证"用户何时接受了哪一版"乏力。**改动**: 在 data_settings(type=admin_landing_page) 的 terms_and_conditions/privacy_policy/about_us 三值首部各插一行淡灰小字 `<p style="color:#9a9a9a;font-size:13px">最后更新：<日期> · 版本 1.0</p>`——条款/隐私=2026年6月16日(取各自正本『内容上次实质变更』日, 非装饰改动当日)、关于我们=2026年6月20日; 旧值备份至 /root/.nezha/legal_datefooter_backup_<ts>.json; 清 data_settings_*_<lang> 缓存。顺带修正 docs/legal/terms-and-conditions.md / privacy-policy.md 头注"渲染来自 business_settings"→实为 data_settings(经 ConfigController::get_settings_data)。**未改**: 协议/政策正文一字未动; 资金/退款/风控机制零改动。**验证**: /config(zh/en) 三键首部均含展示行; Playwright iPhone 真机 /terms-and-conditions /privacy-policy /about-us 顶部渲染该行、无横向溢出、console 0 错误。 |
| 2026-06-20 | **发布《关于我们》页 + 修复《用户协议》渲染瑕疵** | 🟢 L3(CMS文案; 资金/退款机制零改动) | 用户(三问拍板: 批准发布关于我们 / 退款仅靠用户协议§5不单独开页 / 修星号) | **背景**: about_us 在 data_settings 无内容致关于页空白; 已上线 terms_and_conditions §4.1/§5.1 残留 markdown `**` 经 dangerouslySetInnerHTML 字面显示星号。**改动**: ① 发布《关于我们》到 data_settings(key=about_us,type=admin_landing_page), 按 B 方案口径(信息撮合+配送协调·平台不碰钱·不夸大自有配送·德拉姆计价直付商家), 清 data_settings_about_us_<lang> 缓存。② 修 terms §4.1/§5.1 的 `**X**` → `<strong>X</strong>`(2处), 旧值备份至 /root/nezha-legal-backups。③ 新增正本 docs/legal/about-us.md(已发布)/refund-policy.md(草稿·未发布, 退款条款以用户协议§5为准)。**未改**: 资金/退款/风控机制零改动; refund_policy 内容与 refund_policy_status 保持空(退款页仍关闭)。**待办**: 运营主体全称/注册地址占位待填; 多语言(en/ru/hy)译文待补; 正式商用前经亚美尼亚律师审校。**验证**: /config API(zh-CN/en) about_us 2567B·terms 星号0·strong×2; Playwright 真机 /about-us /terms-and-conditions 渲染非空·字面星号0·无横向溢出·console 0 错误。 |
| 2026-06-20 | **评价区 Phase2: 顾客评价图上传(UGC) + 审核门控 + 举报** | 🟡 L1-7 相关(引入 UGC 评价图; 表加密 + 人工审核剔 PII; 资金流/L1-1 零改动) | 用户(就审核范围/审核方式/图留存三问拍板: **只带图评价进审核 / AI预审+人工兜底 / 评价图永久保留+审核时人工剔含PII图**) | **背景**: 后端 submit_product_review 早收 attachment 但前端无上传入口、无审核, 裸开 UGC 图触 L1-7。**改动**: ① reviews 加 reject_reason/reviewed_at + 新建 `nezha_review_reports` 举报表(显式 `ENCRYPTION='Y'`, detail 视为 PII)。② **带图评价进审核**: submit 时有图→`status=3` 待审核(active 作用域只取 status=1 故不公开), 纯文字→`status=1` 即时公开; **带图评价的评分聚合在过审时才计入**(submit 跳过), 防未过审先影响均分。③ **违禁词过滤**复用 `locallife_banned_words` 词库(命中 422 不回显)。④ **图片类型校验**仅 png/jpg/jpeg/webp/gif + 上传全失败防御(不创无图僵尸待审单)。⑤ **审核**(admin /foods/reviews 待审核筛选 + 通过/驳回): 过审 3→1 并补计聚合; 驳回 →4 + **删除未过审评价图(防 PII/违规图留存)**、评价行保留供审计; AI 预审走交互额度(读待审队列逐条核, 同本地生活)。⑥ **举报** `POST /restaurants/reviews/{id}/report`(auth:api 禁匿名, 理由白名单+其他必填+去重)。⑦ 前端写评价加上传控件(≤3张/≤2MB) + **PII 上传前提示**(请勿上传身份证/人脸/他人隐私) + 评价卡举报入口。**留存定性(与本地生活不同)**: 评价图=菜品/到店产品内容(社会证明), **非联系方式那类必删 PII**, 故**永久保留**(不进 30 天 purge); 含 PII 的图靠上传前提示 + 审核环节人工剔除。**合规依据**: reviews/举报表均加密(L1-7); 资金流/OrderLogic/L1-1 零改动。**残留义务**: 运营/AI 须定期清待审核队列(status=3), 发现含身份证/人脸/他人隐私的图即驳回(自动删图)。**验证**: 真 API 实测——违禁词→422、带图→待审核不公开、纯文字→即时公开、过审→展示+计数+1、驳回→删图、举报→入库; admin 页渲染含通过/驳回按钮; 真机写评价页上传控件+PII提示渲染、评价卡举报弹层 console 0。 |
| 2026-06-20 | **商家钱包/提现 B方案合规审计 + 提现入口硬闸(默认禁用) + admin 侧栏提现菜单隐藏** | 🟡 L2(新增控制开关 nezha_withdraw_enabled 默认禁用=防御纵深; 资金流/账务零改动) | 用户(就"做 ABD"明确批准, B 项涉资金机制代码) | **审计结论**: StackFood 原生「钱包余额+提现+admin审批」整套仍在代码里但结构性空转——直付订单(payment_method=offline_payment)被 OrderLogic::create_transaction 的 is_direct_pay 守卫跳过 total_earning 入账(:213), RestaurantWallet::getBalanceAttribute total_earning<=0 即返 0, 故 web(WalletController::w_request) 与 移动端API(Api/V1/Vendor/VendorController::request_withdraw) 提现均被 balance>=amount 门挡死; 即便有请求, admin VendorController::withdrawStatus 审批"通过"仅改账本(total_withdrawn/pending_withdraw)+发通知, **全函数无任何支付网关/打款调用**=平台从不向商家打款。**DB实据**: 7个商家钱包 total_earning/withdrawn/pending/collected_cash 全0; withdraw_requests/disbursement_details/disbursement_withdrawal_methods 均0行(从未使用); 1034笔直付单 delivery_charge 全0、self_delivery 餐厅0家(自配送入账侧门 OrderLogic:206 确认从不触发); 保证金 deposit_balance(SUM=1600) 为独立列, 提现 balance 访问器不读它=与提现严格隔离。**定性**: 二清三要件(无牌主体+自有账户归集+再分发)归集✗·再分发✗, 不构成二清; 当前无 L1 实违。**加固改动(本次)**: A) admin 侧栏「提现列表/提现方式」菜单加 false&& 隐藏(镜像商家端做法, 消除误导运营), 路由未动; B) w_request/request_withdraw/withdrawStatus(approved==1) 三处加硬闸——读 business_settings.nezha_withdraw_enabled, !=1(默认/不存在=禁用)即拒绝, 即使 total_earning 哪天异常>0 也走不通, 迁移持牌分账时设=1 恢复; D) 查证 delivery_charge 恒0(无需改码)。**合规依据**: 强化 INVARIANTS L1-1(平台不碰钱)/L1-5(打款腿已拔), 从"靠空数据摁住"升级为"代码硬闸主动拒绝"。**验证**: 3控制器 php -l 通过; admin 侧栏 Blade::compileString+编译产物 php -l 无错; 闸条件当前求值=blocked(flag=NULL); 真实商家/admin 登录态实跑因图形验证码无会话标⬜需用户亲测(代码+DB强支持走不通)。 |
| 2026-06-19 | **订单超时下发扩展到配送阶段(handover 出餐后未配送 / picked_up 配送中未送达), 经验值 45/90 分钟从前端迁入后端集中配置** | 🟢 L3(展示层; L1-1/L1-2 资金流与动作层零改动) | 用户(就"把配送超时判断从前端经验值迁到后端统一下发"拍板开工) | **背景**: 出餐后未配送/配送中未送达的「偏久」提示原是前端 TrackingPage.jsx 本地硬编码 45/90 分钟经验值判断, 依赖客户设备时间、阈值散落前端, 易误判。**改动**: ① NezhaOrderTimeout::phase()/clockStart()/describe() 新增 D(handover, 仅 delivery)/E(picked_up) 阶段, **仅展示层、无动作层**——sweep 任务硬编码只扫 pending/confirmed/processing, **绝不对配送中的单自动取消**(钱已付、饭已出/在配送, L1 风险最高); ② 时钟起点**只认状态切换真实时间列** order.handover/order.picked_up, **无可靠记录返回 no_time_record 诚实告知, 绝不退用 created_at/updated_at 臆造超时**; ③ 阈值 nezha_timeout_handover_min(45)/nezha_timeout_picked_min(90) 进 business_settings 后台可调; ④ 下发字段含 severity/title/next_step/contact_hint(联系入口)/refund_method(退款责任=联系商家原路退, 平台不经手), **不声称第三方配送在重试/不伪造骑手/不给虚假 ETA**; ⑤ take_away 的 handover=可取餐非配送延迟, 不计超时。**前端**: 删 TrackingPage.jsx 本地 45/90 longWait 判断, 只消费后端 nezha_timeout, 无 timeout 数据保留正常诚实默认文案。**合规依据**: 资金流/退款/动作层零改动, 纯展示层把"偏久"判断从客户端经验值改为后端统一时间源; 退款责任表述仍是联系商家原路退、平台不碰钱(L1-1/L1-2 不变)。**验证**: 后端 tests/Feature/NezhaDeliveryTimeoutTest.php 13 测试 58 断言 PASS(阈值前不超时/刚好达到及超过/缺状态时间/handover·picked_up·delivered·canceled 边界/take_away 豁免/文案无虚假表述; 内存 Order 实例零入库、DatabaseTransactions 回滚、绝不 RefreshDatabase 因连生产库) + Playwright route-mock iPhone13 两超时场景+正常场景回归。前端 commit 见同批、构建 BUILD_ID 见提交。 |
| 2026-06-19 | **配送申诉「没有收到餐」专用入口 + 留痕(不自动退款) + 完成页状态来源/收据脱敏** | 🟡 L1-1/L1-2 相关(资金流代码零改动; 申诉仅留痕不触发退款) | 用户(就"状态展示层区分 + 专用申诉流"两推荐方案拍板) | **背景**: 生产订单 #10 已置 delivered 但「第三方配送」节点无骑手/取餐记录(平台无 Yandex 轨迹), 且完成后顾客缺乏申诉能力(只有通用退款)。**改动(后端 commit 5c99368)**: ① OrderLogic::completion_meta 如实暴露「状态来源」(顾客确认/系统兜底/商家标记, 读 delivery_settled 留痕) + has_delivery_event + legacy_no_track, 配送订单缺事件不伪造节点; ② OrderLogic::receipt_meta 收据脱敏(长数字串掩码 + 截图不进摘要), 收款方=商家; ③ 新增 customer/order/delivery-appeal 端点 + nezha_delivery_appeals 表: 「没收到餐」专用申诉, 仅创建留痕 + 写 logs(delivery_appeal_opened), **绝不触发自动退款 / 不碰 digital_received / 不走平台钱包**, 真实退款仍走商家原路退回; 申诉窗口/SLA/文案走 business_settings(L2 可调)。**合规依据**: L1-1(平台不碰钱)/L1-2(退款原路)资金流代码零改动; 申诉是「留痕 + 引导联系商家」, 不是退款执行器。**残留义务**: 申诉目前无自动通知, 运营须主动查 nezha_delivery_appeals(status=open)跟进; 前端两抽屉(DeliveryAppealDrawer/PaymentReceiptDrawer)已提交(commit ff8599d)但待 Codex 的 TrackingPage/OrderDetails 重写提交后接入 + 真机验证。**验证**: track 接口真机 HTTP 返回新字段诚实(订单 #10: source=customer / has_delivery_event=false / 骑手轨迹标不可得); 申诉端点真机提交 + 去重 + 窗口校验通过(测试记录已清)。 |
| 2026-06-18 | **对外联系邮箱统一到 support@nezha.am(发信/收信/法律文案全域官方域名)** | 🟢 L3(对外联系方式/品牌,不改任何机制·红线;因涉隐私政策/条款文本故留痕) | 用户(就"法律页联系邮箱用 support@"拍板:用 support@、改) | **背景**:全平台邮件域名化改造——发信走 Resend(发件人 哪吒外卖<noreply@nezha.am>)、客诉收信走 Cloudflare 邮件路由(support@nezha.am 转发至运营 Gmail noctis201914@gmail.com)、全局回信地址 Mail::alwaysReplyTo(support@nezha.am)、「联系我们」收件箱 email_address 改 support@nezha.am。收尾排查发现 3 处面向顾客的法律/政策文案仍残留个人邮箱:隐私政策 privacy_policy(nezhadelivery@gmail.com×3)、本地生活条款 locallife_terms(noctis201914@gmail.com×2,暴露私人 Gmail)、风控联系 nezha_risk_contact_info(nezhadelivery@gmail.com×1)。**改动**:三处 DB 设置 + 法律正本 docs/legal/privacy-policy.md / local-life-terms.md 正文联系邮箱统一改为 support@nezha.am(本地生活条款保留「批准人」审批留档邮箱不动);清 cache。**合规依据**:仅替换对外联系地址,L1-1(平台不碰钱)/L1-7(PII 加密)等任一不变量及法律实质条款零改动;support@nezha.am 经 Cloudflare 邮件路由真实转发至运营信箱(已端到端验证可收)。**验证**:DB 三键替换后旧邮箱残留=0、support@ 计数=1/2/3 正确;docs/legal 替换后 privacy 残留 nezhadelivery=0、local-life 残留 noctis201914=1(=批准人留档);发信 noreply@ + 收信 support@ + 回信 support@ 三向均真机/反射验证;Resend 免费档 100 封/天 上限为后续盯防项。 |
| 2026-06-18 | **本地生活类目系统化(8 新类目, 后台可增删)+「移民/签证/按摩」敏感类目加强审核+扩违禁词** | 🟡 L1-1 相关(信息墙内容治理; 资金流/L1-1 不变量代码零改动) | 用户(就敏感类目处理拍板: **8 个全保留 + 加强审核**) | **背景**: 本地生活金刚区类目原写死在前后端常量; 用户要求换成 8 个类目(租房合租/包车出行/本地旅游/移民/签证/二手闲置/美容美发/按摩)并要求后台可自助增删、商家发帖选类目。其中**移民/签证=受监管法律咨询服务(虚假『包过』易引诈骗投诉)、按摩=行业内极易被涉黄广告利用**。**改动**: ① 新建 local_life_categories 表(后台 CRUD, 含 is_sensitive 旗标) + 8 类目播种(移民/签证/按摩 标记 is_sensitive=1); ② 发帖/建帖类目白名单改读 DB(API categoryNames() = DB 启用类目 ∪ 旧常量, 兼容历史帖); ③ **敏感类目加强审核落地**: 敏感类目下的帖在后台帖子列表标红「敏感」徽章提醒运营重点审核(UGC 发帖本就默认进待审核, status=3, 人工通过才公开——双保险); ④ **扩违禁词**(Api+Admin 两处 DEFAULT_BANNED_WORDS 同步): 签证移民诈骗词(包过签/保证过签/100%过签/拒签全退/包入籍/包拿绿卡/黑户洗白/假学历证/假资产证明/假银行流水) + 按摩涉黄词(大保健/莞式/楼凤/一条龙服务/性服务/裸聊/裸体按摩), 命中即拒不转待审核; ⑤ 演示帖按 8 类目重生成(24 条), 移民/签证/按摩文案严格保守(仅『咨询/正规代办/理疗放松』, 无任何包过/保证/特殊服务表述), 带整表备份+一键回退。**合规依据**: L1-1 仍是纯信息墙——平台不碰钱/不代收/不担保, 资金流代码与 OrderLogic 零改动; 类目仅是展示分类。敏感类目经『违禁词拦截 + 默认人工审核 + 免责声明』三重把关。**残留义务**: 移民/签证/按摩 帖上线后运营须在待审核队列重点核验持牌/资质真实性, 发现『包过/特殊服务』即驳回。**验证**: Playwright 真机 /local-life 8 类目齐、按摩筛选只出理疗帖、console 0、无横向溢出; 后台 3 页内部派发 HTTP 200; 类目 API 返回 8 项。后端工作树即生产(已 live), 前端构建 BUILD_ID=1v23lIS7l3PxD4BIJdHmO 上线 local-life=200。 |
| 2026-06-17 | **移除顾客侧「平台不经手资金」提醒(支付面板/订单详情/下单须知 3 处)** | 🟡 L1-1 相关对外披露(资金流代码零改动, 仅 UI 文案) | 用户(本人, 经说明合规影响后批准) | **背景**: 结算支付面板/订单详情/下单须知向顾客显示"平台不经手资金, 请直接付款到本店收款账户"。口吻负面("出事不管")伤下单转化。**改动**: 三处删除该提醒句; 保留标题「请向商家付款」+「收款人: 商家名」承载"顾客直付商家本人"的合规提示; OrderNotice 仅删资金半句, 保留 PII「不保留客户隐私信息」(L1-7)。**合规依据**: ① 资金流代码(OrderLogic 直付分支/收款面板)零改动, L1-1 不变量本身未动; ② 正式披露仍在《用户协议》正文(含 撮合/平台不碰/直付/直接支付/不代收/不经手 等 7 处表述) +《隐私政策》(直付), 顾客「提交订单即表示同意用户协议」隐式同意链路完整。**验证**: Playwright 真机确认 3 处"(平台)不经手资金"文案消失、标题/收款人/应付金额/凭证段仍在、console 0。前端 commit fb0bc23。 |
| 2026-06-17 | **配送单收尾闭环: 顾客「确认收货」(A) + handover 超24h 自动兜底(C)** | 🟡 L2(订单收尾/佣金结算触发路径; L1-1 平台不碰钱经事务回滚仿真验证未变) | 用户(就 A/B/C 拍板: A+C, 兜底24h) | **背景**: B方案平台不配送(顾客自叫Yandex/自取), 配送单(order_type=delivery, 7家店 self_delivery=0)无骑手点送达、商家端被 you_can_not_delivered_delivery_order 拦, 顾客端只能取消无"确认收货"→ 订单永卡 handover → create_transaction 永不跑 → 佣金永不结算 + 顾客端永远"未送达"+ 取餐卡卡3天TTL。**改动**: ① 新增 OrderLogic::settle_delivered(置 delivered+paid, 仅当 order_transaction 不存在时调 create_transaction, 排除订阅单); ② 顾客端 PUT customer/order/confirm-delivery (V1/OrderController::confirm_delivery, 轴A对象级鉴权仅能确认本人单, 仅 handover/picked_up 可确认); ③ nezha:auto-finalize-handover 命令 + bootstrap/app.php 每小时调度: handover 超 nezha_auto_finalize_handover_hours(默认24) 无人确认自动收尾, 开关 nezha_auto_finalize_handover_status(默认1, 已seed)。**幂等/恰好一次**: 所有收尾路径(商家Web/App/骑手/Admin/顾客确认/自动兜底)同以"order_transaction 行是否存在"为闸先到先记余者跳过; confirm_offline_payment 不结算(已验0次调用)。**验证**: 事务回滚仿真(deposit_mode 临时置1)13项全PASS零落库——deposit_balance 恰好扣1笔佣金(10000×10%=1000, 100000→99000)、commission_deduction 流水1条链式一致、vendor total_earning 与 admin digital_received 均不变(L1-1)、二次收尾被闸拦不重复扣、回滚后零残留。**真实影响**: 自动兜底默认开、对真实配送单生效; 上线时线上0单卡 handover>24h(dry-run实测)不误伤现有单。 |
| 2026-06-17 | **容灾/备份恢复 QA + 备份覆盖扩展(图片/凭证/.env)** | 🟢 L3(运维/数据保护加固,不改机制/红线;属数据保护故留痕) | 用户(就异地/密钥/5年留存三问拍板:异地暂缓·补全覆盖·密钥离线另存) | **容灾QA**:实测每日加密备份可解密恢复(DB 155 表逐数对齐 / 「Dump completed」正常收尾 / 152 表 ENCRYPTION=Y)。**发现并修**:此前每日备份只 dump 数据库,`storage/app`(19MB 含商品/餐厅图、banner、会话附件、本地生活UGC、**offline_payment 支付凭证**)与 `.env`(DB密码/Firebase/Maps Key/邮件凭证)**完全没进备份**,丢盘即灭失。改 `/root/nezha-backup/nezha-encrypted-backup.sh` 增第二份加密备份 `nezha-files-<时间戳>.tar.gz.enc`(同密钥/同 14 份滚动),实跑+解密演练通过(308 条目,含 .env 与全部上传目录)。**密钥离线**:backup.key + keyring.master.backup 打包下载到站长本机(此前只在服务器同机,丢机即两份备份都解不开)。**5年留存核查**:L1-4 ≥5年留存载体=生产库 `nezha_refund_records` 表(免于 PII 清除、长期累积),非 14 天滚动备份;不需单独长期归档,但物理可靠性当前=单机,异地暂缓意味着丢机会同时丢掉该留存(已在 data-protection.md 记诚实边界)。README-RESTORE.txt / ADMIN_GUIDE 6.3 / data-protection.md §2 同步。**注**:备份脚本在 `/root/nezha-backup/`(非 git repo)不随本仓提交,原脚本/README 已留 .bak。 |
| 2026-06-17 | **新增「AML/制裁 人工审核 SOP 初稿」(docs/compliance/AML-manual-review-SOP.md)** | 🟢 L3(操作流程文档,不改任何机制/红线;因属合规材料故留痕) | 起草:AI助手;**待用户+持牌律师/合规顾问复核定稿** | AML-policy §3/§7「交易监控与可疑交易处理」的**操作落地**:把"转人工审核"拆成审核员可逐步照做、可留痕、可审计的动作。覆盖**三类人工场景**——下单风控①(daily_amount/frequency/large_amount→放行/退款/清退)、退款超限②(pending_admin→放行回订单重执行/拒绝)、制裁筛查未决③(sanction_inconclusive→「核实放行+确认收款」须填结论,系统重筛命中仍拒)。含 速查表+五步通用流程+分场景SOP+决策矩阵+留痕要求+红线自检(🔴不得人工放行真命中制裁单/不得退第三方或超原单)。**两处缺口已显式标注待补**:① 外部可疑交易上报(STR→亚美尼亚央行 FMC)为【规划中·待法律确认】,当前仅内部审核留痕、无对外报送机制;② SLA时限为建议值待负责人拍板;③ 单人操作期以留痕+月度抽查补偿职责分离(maker-checker)缺口。版本 0.1,补齐 §8 外部上报义务+§10 SLA 并经律师复核后升 1.0 生效。 |
| 2026-06-17 | **启用邮箱找回密码 + 开启邮件总开关 config(mail.status)** | 🟢 L3(登录辅助功能,非 L1 红线;因涉全平台邮件总闸故留痕) | 用户(就"启用邮箱找回 vs 隐藏入口"二选一拍板:启用邮箱找回) | **背景**:登录弹窗"忘记密码"走兜底死胡同——根因 config(mail.status)=NULL(StackFood config/mail.php 无 status 键、后台 mail_config 空)→前端 is_mail_active=false→ForgotPasswordNumberForm 渲染 support 兜底页+gotosupport.png 1x1 绿块。B 方案登录为邮箱+Google+Apple,理应支持邮箱找回。**合规核对**:邮箱本为既有登录方式,用邮箱找回不引入新 PII 处理,不碰 L1-1~L1-7 任一红线(经查 INVARIANTS.md)。**改动**:① config/mail.php 加 'status'=>env(MAIL_STATUS,true)(保留 .env Gmail SMTP 的 mailers.smtp 不动,仅补缺失 status 标志);② business_settings forget_password_mail_status_user=1(顾客找回邮件子开关);③ EmailTemplate id=28(user/forget_password)改中文/OTP 文案/哪吒品牌(原英文+StackFood 2023 旧品牌+"点链接"错配);清 business_settings_all_data + config/view cache。**爆炸半径(已逐条核)**:开总闸后顾客批量邮件(下单/注册/退款)仍不发(各 per-type 子开关全 null);新激活仅低频事件邮件——商家/管理端找回密码、联系表单回复(无害/属改进)。注册仍无 OTP(email/phone_verification_status=0,B 方案不变)。**验证**:线上 /config is_mail_active=true;Playwright 真机走通 登录→忘记密码→邮箱表单(非兜底)→获取验证码→200+toast"验证码已发送至 no***@gmail.com"→OTP 页;console error=0;邮件渲染全中文;真实发信至 noctis201911@gmail.com(站长自有)。**待办**:前端 line 138 英文副标题(模板字面量插值丢 t() 恒英文)待修+构建(暂受他窗 WIP 阻塞)。 |
| 2026-06-17 | **制裁筛查② 审核队列「核实放行+确认收款」一键按钮**(未决单人工复核闸口) | 🟡 L3/运营便捷(在 L1-6 之上加人工复核 UI; 不放松红线) | 用户(要求把 hold 暂挂单的处置做成审核队列一键) | **背景**:开 hold 后"反查不出来源"的 USDT 单暂挂、进审核队列, 原需人工回订单页重新确认, 不便。**改动**:① `OrderLogic::confirm_offline_payment` 加第4参 `$allow_inconclusive=false`(仅 true 时越过"反查不出→暂挂"; **reject 真命中分支不受影响、永远照拦**); ② Admin\NezhaRiskController 新增 `release_inconclusive($id)`——仅对 `sanction_inconclusive` 待复核记录、要求填核实备注(合规留痕), 以 allow_inconclusive=true 重新走 confirm: 重新链上筛查若此刻**命中制裁名单→仍自动拒收**(捕获异常、记录转 rejected、订单 denied), 否则放行确认收款、记录转 approved(reviewed_by/at/备注); 订单已被其它途径处理则仅归档记录; ③ 路由 `POST admin/nezha-risk/release-inconclusive/{id}`; ④ 审核队列页对 inconclusive 行显示「核实放行+确认收款」(必填核实备注)+「清退」, 非 inconclusive 行保持原放行/退款/清退。**安全性质**:人工只能放行"查不出", **永不能放行"已命中"**(真命中即使点放行也会被重新筛查拦下); 命中即拒(status=auto)的单本就不进审核队列。**验证(DB 事务、真实数据零残留, 9/9 断言)**:① hold 下无 tx USDT 单 allow=false 暂挂(订单 pending)、allow=true 放行(订单 confirmed); ② 真命中(真实 BSC tx, from 在名单)即使 allow=true 仍抛异常拒收+订单 pending+offline_payments=denied+写 reject 记录; ③ queue.blade 编译; 还原 inconclusive_action=hold、TEST_VERIFY 残留 0。php -l + route:list 确认。ADMIN_GUIDE 4.7.4/4.7.5(并诚实订正: TronGrid key 影响 TRC20 反查、BscScan key 代码未用不影响筛查)同步。提交见同批 commit |
| 2026-06-17 | **制裁筛查② 来源反查不出时改默认 fail-closed(拦截待人工) + 后台可调** | 🔴 L1-6(制裁筛查处置策略, 收紧为更稳健默认) | 用户(就"反查不出是否自动放行"提问后拍板:做成后台可调开关、默认拦截待人工) | **背景**:b19c63e 初版对"来源地址反查不出(无 tx / 链上 API 不可达)"采 fail-open(自动放行+留痕)。经与用户确认: 制裁筛查通用准则是 fail-closed(查不出即不放行), 遂改为**默认拦截、且后台可调**。**改动**:① 新增设置 `nezha_sanction_inconclusive_action`(hold/allow, **默认 hold**; 新 migration 2026_06_17_160000 种入, 已存在不覆盖); ② `NezhaSanctionScreen::inconclusive_action()` 读策略; ③ `confirm_offline_payment` inconclusive 分支: hold→写复核记录(去重)+抛 SanctionScreenException 中止确认(**订单保持 pending、offline_payments 不标 denied、可在来源核实/API 恢复后重新确认**, 非拒收), allow→留痕后放行(原行为); ④ record_inconclusive 加去重(同单已有 pending sanction_inconclusive 则复用, 防 hold 下反复点确认刷队列); ⑤ 后台「制裁名单筛查」卡加「来源地址反查不出时」下拉(hold/allow) + cfgKey。**为何保留 allow 选项**:未配 TronGrid/BscScan API key 时公共端点易限流→更常"查不出", hold 可能误挂合法 USDT 单; 运营可据实际切 allow 或去免费申请 key。**真实影响**:默认 hold 即对线上生效——查不出来源的 USDT 单不再自动出餐、转人工。误挂的是"查不出"而非"已确认无问题"的单, 可重试/人工放。**验证(DB 事务、真实数据零残留, 13/13 断言)**:默认=hold; hold 下无 tx 的 USDT 单 confirm→抛异常+订单仍 pending+payment_status 仍 unpaid+offline_payments 仍 pending(非 denied)+写 1 条 sanction_inconclusive 复核记录; 再点一次确认→复核记录仍仅 1 条(去重); 切 allow 后同单→不抛异常+订单 confirmed+仍留 1 条复核记录; settings.blade 编译通过; 验证后 inconclusive_action 还原 hold。命中即拒(reject)路径不受本次影响。ADMIN_GUIDE 4.7.1/4.7.4、AML-policy §4 同步。提交见同批 commit |
| 2026-06-17 | **L1-7 加密缺口修复**: `merchant_leads` + `nezha_refund_records` 补静态加密 | 🔴 L1-7(PII 静态加密) | 用户(资金合规QA后明确「全做」批准) | **资金合规QA 实跑**全库扫描 information_schema CREATE_OPTIONS 发现 154 张 InnoDB 表中 `merchant_leads`(含 contact_name/phone/wechat/address 明文 PII, 1 行真实数据)+`nezha_refund_records`(含 USDT locked_to_address, ≥5年留存, 0 行)**未加密**——MySQL5.7 新表不继承全库加密(同 local_life_posts 老坑第三次复发), 与 data-protection.md「全表加密」声称不符。**修复**: `ALTER TABLE ... ENCRYPTION="Y"` 两表; 改前后行数快照不变(1/0)+验 CREATE_OPTIONS=`ENCRYPTION="Y"`+全库复扫剩 1 张=`nezha_sanction_addresses`(L1-6 公开 OFAC SDN 数据·非 PII·不构成缺口·未动)。透明加密、应用零感知。data-protection.md §2 同步订正。**遗留🟡**(本次未改, 因属另一窗口热点函数): admin 确认收款路径 `confirm_offline_payment` 缺幂等守卫(vendor 已守); 建议下沉守卫到共用实现。提交见同批 commit |
| 2026-06-17 | **风控单笔阈值 $200→$300**(法币+USDT 单笔均 73625֏→110437֏) | 🟡 L2(业务参数, 阈值类用后台设置项调, 不改代码) | 用户(明确要求: 拼单采购可能达 $300) | **背景**:拼单/集采订单可能合法达 $300, 原 $200 单笔上限误拦真实大单. **改动**:nezha_risk_single_order_limit 73625→110437 + nezha_risk_usdt_single_limit 73625→110437 (=300×368.1236 ֏/USD 现行汇率; 前端弹窗显示 ֏110,437≈$300, limit 由后端返回不硬编码). **未动**:单日累计 800000֏/频次/大额/退款限额不变. **生效**:cfg() 直读 DB 无缓存即时生效. **验证(2026-06-17 线上 risk-check API)**:$250(92031֏)/$300(110437֏)放行, $350(128843֏)法币+USDT均拒单 limit=110437; 测试记录已清. **影响/回退**:放行上限上调=每笔风险容忍度提高($300内不再拦)属业务取舍; 后台风控设置可改回. |
| 2026-06-16 | **制裁名单筛查② 实装 + 正式启用**（L1-6 命中即拒收）+ OFAC SDN 名单已载入并生效 | 🔴 L1-6（制裁筛查命中即拒收，由"待"→"已实施"；含"真实影响"启用） | 用户（已批准建，属 L1） | **背景**:INVARIANTS L1-6 此前标"(待)②地址筛查"未实装。本次落地 USDT 付款来源地址的 OFAC SDN 制裁筛查。**新增**:① 表 `nezha_sanction_addresses`(addr_kind evm/tron/other + 规范化 address + source/sdn_uid/currency_type/added_at/last_seen_sync) + 3 个 business_settings(总开关 `nezha_sanction_screen_status` **默认1开** / 名单源 `nezha_sanction_source_url` / 同步状态 `nezha_sanction_last_sync`); ② 引擎 `App\CentralLogics\NezhaSanctionScreen`(地址规范化: EVM 小写吸收 EIP-55 大小写、Tron base58 大小写敏感原样; screen_address 比对; screen_order **复用 NezhaRefundControl 的链上反查/通道识别/tx 提取**); ③ 命令 `nezha:sync-sanction-list`(每日 04:30, 从 OFAC 公开 SDN.XML 拉"Digital Currency Address"入库, 带护栏: 取数/解析0条则保留旧名单不动=失败安全; --dry-run/--file 选项; 注册于 bootstrap/app.php withSchedule); ④ 异常 `App\Exceptions\SanctionScreenException`; ⑤ 模型 `NezhaSanctionAddress`。**筛查点**:`OrderLogic::confirm_offline_payment`(确认收款=放行出餐闸口, admin+vendor 共用单一来源)在任何状态变更前筛查——USDT→反查 tx 的 from 地址→比对名单→**命中即 deny_offline_payment(拒收, 订单不确认/不放行) + 写 nezha_risk_records(rule=sanction, action=reject, status=auto, 留痕 from/tx/链/订单号) + 抛 SanctionScreenException**(两端控制器捕获并提示); 非 USDT 不筛; 来源地址反查不出(无tx/API不可达)**不硬拦**, 写 review 记录待人工复核(命中才拒, 查不出≠命中)。**后台**:风控设置页新增「制裁名单筛查」卡片(开关/源URL/名单数/上次同步状态), 风控日志新增「制裁命中」筛选。**🔴 真实影响启用**:筛查开关默认开, 且本次**已运行一次正式同步载入 OFAC 现行名单 780 条(EVM 97/Tron 127/其它 556)**, 故 L1-6 即刻对真实 USDT 确认收款生效。误伤真实顾客风险≈0(仅 OFAC 在册来源被拒)。**回退/暂存**:后台「风控设置→制裁名单筛查」将开关置 0 即停用; 名单可清空。**验证(2026-06-17 独立复现; 真实 OFAC 地址 + 真实 BSC 链上交易, 公共 RPC 免密钥; 全程 DB 事务回滚, 真实数据零残留; 逻辑层 25/26 + 端到端 13/13 断言通过)**:① 真实 OFAC EVM/Tron 地址命中 + EVM 大小写规范化命中 + 干净地址放行 + 链隔离(EVM 地址不按 Tron 命中); ② 端到端 confirm_offline_payment 真实合成订单(克隆订单行 + 合成离线 USDT 付款携真实 BSC tx, user_id 取不存在 id 故不误发推送/邮件): 取最新块真实 tx → reverse_lookup_from_address 反查 from(与块内 from 一致)→ 塞入名单 → **命中即抛 SanctionScreenException + 订单 order_status 仍 pending、payment_status 仍 unpaid、confirmed 未写(未放行出餐) + offline_payments=denied + 写入 nezha_risk_records(rule=sanction, action=reject, status=auto, 快照含 from/tx/chain)**; ③ 反证: 同交易地址移出名单后 screen_order=pass(证 from 真实反查有效, 非 inconclusive 假放行); ④ 失败兜底: --file 空/坏数据 → 名单 780 条原样不动 + 命令成功退出(绝不清空、绝不放行一切); ⑤ 真实 OFAC 源 dry-run 连通解析 780 条(EVM 97 / Tron 127 / 其它 556)、正式同步幂等入库、last_sync=ok。**观察**: Tron 地址比对在 MySQL _ci 排序规则下大小写不敏感 —— 仅令匹配更宽(不可被大小写规避漏放)、实务无误伤(Base58 改大小写即非法地址), 不削弱筛查; PHP normalize() 保留 Tron 原样仍为正确防御。改动文件 php -l 通过。提交见同批 commit |
| 2026-06-16 | 风控单笔阈值 $2000→$200等值(736240→73625֏,法币+USDT); INVARIANTS L2开关当前值对齐线上(风控/退款护栏=1开) | 🟡 L2(阈值+文档对齐) | 用户(明确指示$200) | 客户下单超$200拒单(配套前端弹窗提示由新窗口做); 单日/频次/大额未动 |
| 2026-06-16 | 取消已确认直付单的退款路径补 L1-1 护栏(F-4a, refund_before_delivered) | 🔴 L1-1(平台不碰钱) | 用户(明确「现在就改+记CHANGELOG」) | 资金闭环QA发现: OrderLogic::refund_before_delivered(admin 取消订单时调用)对 payment_status=paid 的订单会 ①冲减平台 adminWallet.digital_received ②若 wallet_add_refund 开则用平台钱包退款给顾客。直付单(offline_payment)顾客的钱直接进商家账户、平台从未收款, 故①是错误冲减(平台从没加过 digital_received)、②是平台垫钱退顾客=碰钱=违反 L1-1。对比 refund_order(退款路径)已有 !isDirectPay 护栏, refund_before_delivered(取消路径)缺失, 两路径不一致。修复: 在 refund_before_delivered 开头加 offline_payment 直付 no-op(return true), 使直付单平台侧不动账、钱由商家直接退原付款人, 与 refund_order 对齐; 对 COD/线上支付单零影响。验证: 事务回滚仿真, 最坏情况强开 wallet_add_refund=1 下, 直付单 digital_received 不变(修前会-300)+无平台钱包退款产生+COD 对照仍返回 false, 零落库。注: 顾客自助取消路径(API customer cancel)经查只能取消 pending 单(此时 payment_status=unpaid、已付 sum=0)故不触达, 无需改。遗留(非本次): 取消直付单后"商家需直接退原付款人"目前无 NezhaRefundRecord/通知商家留痕, 建议后续补。提交见同批 commit |
| 2026-06-16 | 3个每日 PII 清除任务接回每日调度(L1-7, 修复 L12 调度休眠) | 🔴 L1-7(PII到期删, 恢复应有的自动删除) | 用户(明确「接回去」) | 承接上条发现: purge-payment-proofs(每日03:30)/purge-locallife-pii(03:40)/purge-merchant-leads(03:50) 原只定义在 L12 后已失效的 app/Console/Kernel.php, 自升级起一直未运行。本次迁入 bootstrap/app.php 的 withSchedule 正式接回每日调度。**接回前三任务 --dry-run 实测均命中 0**(平台尚新、无数据超期, 此刻接回=零删除=最安全时机), 无「一次性删积压」风险; 趁数据将于约 2026-09(支付凭证90天)、30天内(UGC contact_info)起陆续到期之前接好。schedule:list 已确认 4 个任务齐(含 sync-fx-rate)。各任务只抹超期 PII 字段+关联文件, 保留行/状态供审计, 不动订单/交易/链上记录。提交见同批 commit |
| 2026-06-16 | 结算汇率每天自动对齐市场价(新增 nezha:sync-fx-rate 定时任务) | 🟡 L2(自动化既有结算汇率参数; L1-1/L1-3 经确认未碰) | 用户(要求每周自动对齐实际汇率, 选「自动写入+护栏」「贴市场中间价不加缓冲」) | **背景**: 结算汇率 nezha_rate_usd_to_amd/nezha_rate_cny_to_amd 是顾客↔商家直付的事实结算汇率, 手动维护易放旧→商家按过期价收钱。**动作**: ①新增命令 nezha:sync-fx-rate, 从公开汇率源 open.er-api.com(现有汇率即取自此源)拉市场中间价, 算 1USD=?֏ 与 1CNY=?֏(=AMD÷CNY)写回两设置项; ②护栏(任一触发则不改+保留旧值+记录告警): 取数失败/内容异常、数值越界(usd∉[250,550]/cny∉[35,80])、单次变动>8%; ③结果(ok/skipped)写入 nezha_fx_last_sync, 收款信息页汇率卡下方展示(上次对齐时间/红字「已暂停等确认」); ④按用户选贴市场中间价不加缓冲。**顺带重大发现+修复**: 升级 Laravel 12 后 app/Console/Kernel.php::schedule() 已不被框架调用(bootstrap/app.php 无 withSchedule、commands 注释), schedule:list 为空 → 既有每日 PII 清除任务(purge-payment-proofs/locallife-pii/merchant-leads, L1-7)实际自升级起一直未运行; 本次在 bootstrap/app.php 用 withSchedule 接回调度(先只挂 sync-fx-rate)。3个 PII purge 任务的重新挂入留待用户确认后单独处理(首次运行会一次性不可逆删除超期 PII 积压, 建议先 dry-run)。【已于同日接回, 见本表最新条】**L1-1 平台不碰钱/L1-3 无换汇提现入口**: 仅刷新公开换算率, 未引入资金归集/兑换/提现, 经确认未变。**验证**: dry-run 不写值、真实运行写 368.1236/54.3604、突变护栏 PASS(临时设420偏离12%→跳过未覆盖+记skipped+自恢复)、blade 编译 OK、schedule:list 排每天04:10(按用户要求改每天)。提交见同批 commit |
| 2026-06-16 | 本地生活 UGC 用户发帖对外开放（`locallife_ugc_enabled` 0→1） | 🟡 L2（运营决策上线既有功能；🔴 L1-7 PII 边界经验证守住） | 用户（指派本任务「开本地生活发帖开关」，明确要求翻 0→1） | **背景**:本地生活 ①-⑤ 全部 UGC 代码 + 四道护栏(违禁词/举报/限流/免责)早已完成并验证，本次为运营决策"开闸"。**动作**:仅翻 DB 业务设置 `locallife_ugc_enabled` 0→1（等价后台「开放发帖入口」按钮 `ugcToggle()`，**无代码改动**）。**🔴 L1-7 PII 边界守住（已验证）**:`locallife_pii_consent_enabled` 保持默认关(NULL)+通知正文空 → API `ugc_pii_required=false`、发帖表单**只 1 个同意勾选(信息发布规则)、无《个人数据处理通知》勾选**(Playwright 实证)，**未采集任何个人数据处理同意**；顾客 contact_info 仍随表静态加密(ENCRYPTION=Y)+30天 `nezha:purge-locallife-pii` 自动清；登录后可见防爬。**未碰**律师审校前禁开的 PII 同意键。**上线验证**:护栏 API E2E 7 项全过(违禁词422/缺字段422/正常200/最小间隔429/重复标题422/举报200/其他无说明422)；Playwright iPhone 真机三态(空📭/假数据/登录态发帖)+登录门(游客点发帖弹登录抽屉)+违禁词toast+举报抽屉全过，console error=0、无横向溢出。测试帖(id11)/举报/临时验证token 已清，平台帖4条完好。**🟠 进行中义务**:顾客发帖进"待审核"**不自动公开**，需人工(或 AI 预审 ADMIN_GUIDE 8.6)审待审队列 + 看举报。**回退**:`locallife_ugc_enabled` 置回 0 即关闭(已发布帖不受影响)。ADMIN_GUIDE 8.4/8.5 已记开关与护栏。 |
| 2026-06-16 | 商家自营「确认收款」上线（替代 admin 核验为主路径，admin 保留后备）—— 闭合上一条遗留 | 🟡 L2（订单状态机·谁来核验+可见性；🔴 L1-1 经验证守住） | 用户（指派本任务并给出实现要点） | **背景**:闭合上一条 CHANGELOG 遗留的「商家自营确认收款」。此前 `Order::scopeNotDigitalOrder`（仅商家侧用）排除"pending+offline_payment"单，致顾客离线单**对商家完全隐藏=掉单**，只能走 admin 后台核验。business-flow §4 写的是「商家确认收款」，本次把核验权交回商家。**改动（仅商家侧可见性+新增动作，不动 Report\*）**:① `Vendor/OrderController::list` 新增 `offline_pending`「待确认收款」视图（`where order_status=pending & payment_method=offline_payment & whereHas offline_payments.status=pending`），且**仅该视图**跳过 `NotDigitalOrder`，其余视图行为不变；② `::details` 放开本店 `offline_payment` 单（等价 NotDigitalOrder 但 orWhere offline，digital+pending 仍排除；已按 id+restaurant_id 限本店）；③ 新增 `PUT order/confirm-offline-payment/{id}`、`deny-offline-payment/{id}` + 控制器方法（强校验 restaurant_id==本店 & offline_payments.status==pending，防越权/重复确认）；④ `order-view.blade` 加「确认收款」「拒收/打回」按钮+付款截图预览+拒收备注弹窗（中文）；⑤ 侧栏加「待确认收款」红徽标 tab。**抽共用逻辑**:核验动作抽到 `OrderLogic::confirm_offline_payment` / `deny_offline_payment`（含顾客通知+留痕），**admin `offline_payment()` 重构为调用同一方法**（删原私有 `sent_notification_on_offline_payment`），admin/vendor 单一来源不漂移；admin 路径保留为后备。**留痕**:确认/拒收写 `logs` 表（by_type=admin/vendor、by_id、前后状态、时间）。**🔴 L1-1 守住（已验证）**:只改"谁核验+可见性+状态"，钱全程不经平台、收款人=商家本人、收款信息仍后台代录（商家无自助改码，business-flow §8）。**验证**:确定性后端测试（合成 pending+offline 单）—— 可见性查询命中/旧 NotDigitalOrder 确认隐藏/详情可加载；confirm→order_status=confirmed·payment_status=paid·offline_payments=verified·留痕落库；**`restaurant_wallet.total_earning` 0→0 未变、`RestaurantDisbursement` 已拔(类不存在)→平台不碰钱/二清腿(L1-5)守住**；deny→denied+备注、订单仍 pending；全部 Blade 编译通过；测试脏单已清。商家面板真机 UI 截图验证另附。**回退**:revert 6 文件即恢复（admin 路径独立可用）。ADMIN_GUIDE §3 / MERCHANT_GUIDE「确认收款」章 同步。提交见同批 commit |
| 2026-06-16 | order_confirmation_model: 'deliveryman' → 'restaurant'（端到端走查修复掉单根因） | 🟡 L2（业务参数·影响全部订单流转） | 用户（端到端资金流走查后拍板） | **背景**:2026-06-16 商家端+顾客↔商家端到端资金流首次走查,发现"从没真跑通过"根因之一:`order_confirmation_model` 原为 `'deliveryman'`,该模式下商家端订单列表(`Vendor/OrderController::list` 的 pending/all 视图)**排除 `order_type='delivery'` 的 pending 单**;哪吒用 Yandex 配送、**无自有骑手**,delivery 单在 deliveryman 模式下无人确认→**永久卡 pending、商家永远看不到=掉单**。**改动**:`business_settings.order_confirmation_model` 'deliveryman'→**'restaurant'**(商家直接看到/确认订单,匹配 business-flow §4「商家确认收款」),仅改设置项、不改代码。**影响**:现已生效于全部真实订单的商家端列表过滤。**回退值**:'deliveryman' 已记录。**遗留(本次未解决)**:`Order::scopeNotDigitalOrder`(仅商家侧用)仍排除"pending+offline_payment"订单,故顾客离线单需经 **admin 后台离线核验**(`Admin/OrderController::offline_payment`)转 confirmed 后商家才可见;本次走查已用 admin 核验路径**验证端到端跑通**(顾客下单#100004→admin核验→商家接单/出餐→顾客取餐码3143,金额֏2200全程一致、收款人=商家本人、平台不碰钱)。**「商家自营确认收款」**(去商家侧离线单隐藏+加商家端确认动作)作为独立任务留后续(改订单状态机,谨慎)。**L1 守住**:平台全程不碰钱、收款人=商家本人,本改动只改"谁确认+可见性",未引入归集/代收。预存佣金按单扣佣(开关 `nezha_deposit_mode_status`)同次走查验证:停接单(余额<阈值→下单403拦)+按单扣减(佣金10%写 commission_deduction 流水)均通过,测试后开关已关回0、测试数据已清。**无代码改动,仅本 CHANGELOG 留痕**。 |
| 2026-06-16 | 下单风控阈值调整（启用当日按客单价/客服打通微调） | 🟡 L2（业务参数 · 含"真实影响"拒单线调整） | 用户（逐项拍板：大额$1000 / 频次10min8·24h20 / 拒单线抬$2000 / 客服打通） | 在同日启用基础上调整（均改 `business_settings`、不改引擎代码；前端为展示层改动）：① **大额审核线** `nezha_risk_large_amount_threshold` 32000→**368120֏**($1000@368.12)；② **单笔硬拒单线**(法币 `nezha_risk_single_order_limit` + USDT `nezha_risk_usdt_single_limit`) 200000→**736240֏**($2000)——因大额审核线$1000 高于原拒单线$543 会使审核永不触发（单笔超限命中即拒、优先级最高），抬拒单线形成**三段带**：<$1000 通过 / $1000–$2000 转客服审核 / >$2000 直接拒；③ **频次** 10min `nezha_risk_freq_10min_count` 2→**8**、24h `nezha_risk_freq_24h_count` 5→**20**（原 24h=5 会压住 10min=8 使其形同虚设，一并放宽，仅登录用户计）；④ **客服打通**：风控拒单/审核时，结算支付页风控拦截面板(`OfflinePaymentForm` reject/review 块)新增「联系客服」按钮，点击直达平台客服会话深链 `/chat?type=admin&direct=1`(复用 Chat 组件既有能力：登录顾客直接打开 admin 会话、游客弹登录)；另在 `CheckoutPage` 下单 mutation 的 onError(order/place 兜底路径)对 `risk_reject`/`risk_review` 码也弹带「联系客服」的品牌 toast(新增 `RiskBlockedToast` 组件)。**诚实更正**：原拟修 `onSingleErrorResponse` 的 undefined toast bug，实查 order/place 的 onError 本就遍历 `errors[]` 显示消息、且顾客主路径是 risk-check 预检的内联面板(非 toast)，故无此 bug、未改该函数。**活体复验**(总开关已开)：三段带 pass/review/reject 逐档命中——367000 `pass`、368120/700000/736240 `review[large_amount]`、736241 `reject[single_order_limit]`、USDT 700000 `review`/800000 `reject`。**影响面≈0**(现网 2 单/1 paid)。**回退**：阈值原值已记（大额 32000 / 单笔 200000 / 10min 2 / 24h 5）。ADMIN_GUIDE 4.2 同步。前端提交见 nezha-frontend 同批 commit |
| 2026-06-16 | 下单风控① 正式启用（总开关 `nezha_risk_control_status` 0→1） | 🟡 L2（业务参数开关启用 · "真实影响开关"） | 用户（复核报告后拍板"现值直接开"） | 启用交易红旗风控①（5规则：单笔上限→拒单 / 单日累计·频次·大额特征→转人工审核）。**阈值保持当前线上值、不改代码**：单笔上限 200000֏（法币/USDT 同）→拒单不可放行、单日累计 800000֏→审核、大额特征 32000֏→审核、频次 >5单/24h 或 >2单/10min→审核、放行宽限 60min（键未持久化、引擎默认）。**汇率说明**：现汇率 368.12 下 200000/800000/32000֏ ≈ $543/$2173/$87（文档原标 $500/$2000/$80 基于旧汇率~400，本次已校准 ADMIN_GUIDE 4.2 美元标注）；阈值存**绝对德拉姆值、不随汇率波动**（同退款护栏处理）。**影响面≈0**：启用前现网仅 2 单（1 paid）、真实流量≈0，属"变更安全"（从无风控→有风控），翻开无即时影响、仅对今后超限订单生效。**活体验证**（开关开后 evaluate() 不再短路）：正常小单 1500 `pass`；大额 40000 命中 `large_amount`→`review`；单笔 250000 命中 `single_order_limit`→`reject`；USDT 250000→`reject`；游客大额 40000→`review`；`record()` 落库 status=`pending` 进审核队列、查得后**立即删除测试记录**（队列归 0，无脏数据）。`enabled()`=true。下单流程兜底接入点 OrderController L131-142（reject→risk_reject/review→risk_review，均返 403 且不创建订单）。**回退**：总开关改回 `0` 即恢复无风控（原值 0 已记）。**诚实边界**：① 大额审核阈值 32000֏($87) 偏低，多人拼单可能进审核队列（仅 review 非 reject），量大可后台上调；② `nezha_risk_support_contact` 为空且代码未引用，拒单/审核提示为固定中文文案、不渲染具体联系方式（文档 4.2 已诚实标注，如需展示需改提示模板）。ADMIN_GUIDE 4.2/4.4 同步更新。提交见同批 commit |
| 2026-06-15 | 退款护栏② 正式启用 + 限额按客单价上调 | 🔴 L1 (退款原路/限额执行机制 0→1 启用) + 🟡 L2 (限额调整) | 用户(逐项拍板确认) | 经退款引擎只读体检 + TRC20 校验 bug 修复后,启用退款护栏总开关 **`nezha_refund_control_status` 0→1**,退款机制②(原路锁定 + 限额 + 留痕≥5年)正式对所有真实退款生效(此前退款裸奔:无原路、无限额)。**同步上调限额**(用户逐项拍板,均改 `business_settings` 设置项、不改代码):单笔 40000→**400000֏**、单日累计 120000→**800000֏**、单日笔数 5→**10**、退款窗口 7→**3天**(澄清:本系统超窗口=转人工审核非自动拒;3天贴合外卖行业惯例——餐食即时消费、窗口本就短,窗口内丝滑自动退、超期=顾客找人工申诉);USDT 链上自动校验保持 **1(开)**。**启用时影响面≈0**:现网 0 笔 paid 订单 / 0 退款流量,翻开无即时影响、仅对今后真实退款生效,且属"变更安全"(从无护栏→有护栏)。**活体验证(开关开后 check_limits 不再短路)**:金额 300000/400000 `pass`、500000 命中 `single_limit`、900000 命中 `single_limit+daily_total`、交付5天前 命中 `window`;`enabled()=true`;原路锁定 + 链上校验(BSC/TRC) 同批已实测 `verified`/反查==链上from。**回退**:总开关改回 `0` 即恢复无护栏行为(原值 0 已记);限额原值 40000/120000/5/7 已记录于本条。⚠️ 公共节点免密钥,TRC/BSC 退款量大前建议补 `nezha_refund_trongrid_api_key`/`nezha_refund_bscscan_api_key`(否则限流时 verify 降级人工核)。**多窗口/汇率说明**:限额按用户「约 $1000/$2000」意图设为整数德拉姆 400000/800000(设定时 USD≈400֏);设定期间另一窗口将 `nezha_rate_usd_to_amd` 更新为 368.12,故现值实际 ≈$1087/$2174(略宽~9%)。**用户确认保留整数德拉姆值不重算**(与「初创期避免客户不好体验」的宽松取向一致);限额存绝对德拉姆值、不随汇率波动,功能无碍。ADMIN_GUIDE 4.2/4.6.1 同步更新。提交见同批 commit |
| 2026-06-15 | 退款引擎② TRC20 链上自动校验 bug 修复(开退款护栏前置体检发现) | 🟢 L3 bug修复(退款引擎·不改 L1 行为) | 用户 | **背景**:准备启用退款护栏 `nezha_refund_control_status` 前做只读活体体检,发现 `NezhaRefundControl::verify_refund_tx` 对 **TRC20(波场)** USDT 退款链上自动校验**恒返回 `failed:未找到USDT转账事件`**。**根因**:`tron_hex_to_base58()` 开头无条件 `strtolower($hex)`;TronGrid `/v1/transactions/{hash}/events` 把 USDT 合约地址返回为 **base58(大小写敏感)** 形式 `TR7NHqjeKQ…`,被 lowercase 成 `tr7nh…` 后与常量 `TRC_USDT` 比对永不相等→跳过事件→判失败。**修复**:改为仅当入参确为 hex(`ctype_xdigit`)时才 lowercase 转换,已是 base58 则原样返回(保留大小写);非 hex 返回原值。**性质·不碰 L1**:此前 bug **失败朝安全侧**(只把 TRC 退款转人工核,绝不会误判"已验证"放行,无资金/合规漏洞);USDT **原路锁定**(真正的 L1-2/L1-3 红线,走 `reverse_lookup_from_address`)两条链本就正常,本次不动。**实测(真实链上交易)**:① 修复前 TRC verify=`failed:未找到事件`;② 修复后 TRC 正确解析 `to`/`amount` 并在地址+金额命中时返回 `verified`(txid d460d598… to=TTaJTS6M… amt=10→verified);③ BSC 不受影响:正向 `verified`、错地址 `failed`;④ 两链 `reverse_lookup` 反查地址==链上 from。⚠️ **诚实边界**:TronGrid/BSC 公共节点免密钥(`nezha_refund_trongrid_api_key`/`nezha_refund_bscscan_api_key` 为空),连发请求会触发限流→verify 安全降级 `manual`(待人工核);量大前建议补 API key。`php -l` 通过、备份 `NezhaRefundControl.php.bak.202606151517pretronfix`。提交见同批 commit |
| 2026-06-15 | 商家入驻线索 merchant_leads PII 到期清理 | 🔴 L1-7 (PII到期删) 强化 | 标准授权(强化红线·不碰资金) | 落实 data-protection.md「merchant_leads 暂随业务保留,后续可纳入到期清理」待办。**另起独立命令** `nezha:purge-merchant-leads`(每日 03:50 调度):与举报/支付凭证不同,入驻线索**不能按登记时间一刀切删**(会误删未跟进的真实潜在商家)——故触发条件=**线索已结案**(`status`=2已完成/3无效)**且**距 `updated_at`(≈结案时间)超过保留期,抹除 contact_name/phone/wechat/address/note(置空),**保留 行/店名/品类/状态 供审计**;`status` 0待跟进/1跟进中【绝不触碰】。保留期 `business_settings.merchant_leads_retention_days`(默认 90 天,自结案起算,与支付凭证 90 天对齐)。`--dry-run` 同覆盖。**实测**(5条边界用例):已完成/无效+结案100天前→命中清PII;刚结案1天(未到期)/待跟进200天/跟进中150天→均正确跳过、电话微信原样保留;清后核对 PII 已抹、店名/状态保留;删测试行(表回0行)+删脚本;`php -l` 通过。诚实细节:contact_name/phone 为 NOT NULL 列,清除落为空串(非NULL),仍不含PII。提交见同批 commit |
| 2026-06-15 | 术语统一:商家「保证金」→「预存佣金」 | 🟢 L3 文案(仅措辞,机制零改动) | 用户 | 经核查 `VendorController::rechargeDeposit` 逻辑实为「商家先预存款→佣金按单从中扣→余额不足停接单」,本质是**预存佣金**而非押金式保证金,旧称「保证金」词不达意。**仅改措辞、机制/资金流/扣佣逻辑完全不动**:① 面向商家/运营处(后台收款信息页 Blade、`messages.php` 中文、Toastr、ADMIN_GUIDE/MERCHANT_GUIDE、代码注释)统一为**「预存佣金」**;② 本目录合规正本(business-flow/AML-policy/INVARIANTS)首次出现处并列标注**「预存佣金（履约保证金）」**——因该机制兼具预付与履约担保双重性质;③ **数据库字段 `deposit_balance`、设置键 `nezha_deposit_mode_status` 等英文标识符不改**(改 schema 无收益且有风险)。⚠️**法律最终用词以专业律师审定为准**(已在 business-flow.md 术语说明中注明)。`php -l` 全过、0 残留「保证金」(备份/StackFood vendor 目录除外)。提交见同批 commit |
| 2026-06-15 | 本地生活顾客举报 detail PII 到期清理 | 🔴 L1-7 (PII到期删) 强化 | 标准授权(强化红线·不碰资金) | 落实 data-protection.md「local_life_reports.detail 暂随业务保留,后续可纳入到期清理」待办。**扩展现有命令** `nezha:purge-locallife-pii`(非新建,沿用每日 03:40 既有调度):在 UGC 帖 contact_info 清理后追加一段——清 `local_life_reports` 中 `created_at` 早于保留期的行的 `detail`(置 null),**保留 行/reason/status 供运营审计**。保留期 `business_settings.locallife_report_retention_days`(默认 180 天),后台「护栏与文案设置」页新增输入框可调(留空保持原值)。`--dry-run` 同覆盖新逻辑(只报告)。**实测**:dry-run 无 180 天前数据命中 0;回填一条 `created_at`=200 天前测试举报→dry-run 命中 1→实删后核对 `detail`=null 且 `reason`/`status`/行均保留→删测试行(表回 0 行干净)+ 删 bootstrap 脚本;`php -l`/blade 编译通过。**merchant_leads(电话/微信 PII)本次不做、仍暂随业务保留**——入驻线索留存逻辑不同(需留到联系/入驻完成),按 created_at 自动删可能误伤真实业务,留待用户拍板(届时独立命令+独立保留期)。提交见同批 commit |
| 2026-06-15 | 本地生活 后台「护栏与文案设置」页 | 🟢 L3 实现(法律文本编辑入口) | 标准授权 | 新增后台 `/admin/local-life/settings`(列表页右上角入口):运营自助编辑违禁词 `locallife_banned_words`、免责短提示 `locallife_disclaimer`、《信息发布规则》全文 `locallife_terms`、三个反滥用阈值(每日发帖/举报上限、最小发帖间隔),不再需改数据库。规则全文编辑区带⚠️提示(法律文本慎改、与 `docs/legal/local-life-terms.md` 正本同步、重大修改请律师过目);阈值留空保持原值、最小间隔填0=关。文案类允许清空(清空则顾客端回退代码内默认)。实测 round-trip(真实调 settingsSave 写入/读取/复原)通过+blade 编译+路由注册 OK(收尾发现并清掉测试残留 daily_limit 键,还原代码默认5)。ADMIN_GUIDE 8.5 同步指向本页。提交见同批 commit |
| 2026-06-15 | 本地生活《信息发布规则》+免责声明 中文正式版上线 | 🟢 L3 内容 + 🔴 L1-1/L1-7 口径确认 | 用户(语言范围/联系方式/是否上PII通知 三问已拍板) | 将用户提供的《合规文案包》中文版清理为可上线文本(删研究综述 Part0/对照表 Part4/律师待办 Part5 等**非展示内容**及"须律师审"元注释),上线到 `business_settings.locallife_terms`/`locallife_disclaimer`(前端规则抽屉+免责短提示渲染,无需 build)。§7 投诉渠道=站内「举报」+ noctis201914@gmail.com、§10 管辖=亚美尼亚共和国法院(原占位 `____` 已填)。新增**法务正本** `docs/legal/local-life-terms.md`(git 留档/审计/将来协议页同源复用)。**L1-1**:§1.2 明列平台不收/代收/代付/担保、不参与交易;**L1-7**:§5 联系方式仅登录详情可见、列表永不展示、30天清除、静态加密(与 data-protection.md 一致)。⚠️**暂未上线(已记 docs/legal 待办)**:俄/英/**亚美尼亚语**版(亚语文档明标须母语律师审校;亚美尼亚消费者法要求强制信息用亚语)、《个人数据处理通知》(需公司全称+注册地址+前端第二个 consent)、Part5 的15条待当地律师确认项。Playwright 真机验证规则抽屉渲染全文(第1条/「不收取代收代付」不碰钱条款/邮箱/第11条),无横向溢出、console 0 错。提交 00e2f51 |
| 2026-06-15 | 微信支付独立成单独收款方式(原微信/支付宝共用一张人民币码) | 🟢 L3 实现 + 🔴 L1-1 不碰钱 / 🔴 L1-7 PII 确认 | 标准授权(扩展既有已合规字段,未触红线) | 此前结算页「人民币」一张共用码同时给微信/支付宝扫;现拆分:`restaurants` 新增列 `wechat_qr_image`(独立于 `rmb_qr_image`),后台「收款信息」tab 拆成「支付宝收款」「微信收款」两张卡分别上传;离线支付方式由共用「人民币支付（微信/支付宝）」**重命名为「支付宝」** + **新增「微信」方式**(method_fields 同为付款截图必填),结算页前端按方式名(含微信/wechat 且非支付宝)选显微信码 or 支付宝码。**L1-1 不碰钱**:微信码与支付宝码同性质,均为**商家本人收款码**,顾客直付商家、平台不归集不代收,未引入任何代收/担保/下单字段。**L1-7 PII**:`wechat_qr_image` 列落在已 `ENCRYPTION='Y'` 的 restaurants 表内(已验证)、自动随全表静态加密(at-rest);属商家**常驻展示码**(非顾客一次性付款凭证),**不在** `nezha:purge-payment-proofs` 90天清除范围(与 rmb_qr 一致)。**微信外部唤起诚实边界**:经查微信官方/社区,普通手机浏览器(非微信内置)**无法深链唤起微信到指定收款页**(`weixin://scanqrcode` 仅唤起实时相机扫码、传不进指定图,Android 多被拦),故顾客端走「长按二维码保存到相册→微信扫一扫从相册选图」可靠路径,另附尽力而为「打开微信」(`weixin://`,失败无动作,不伪造跳转)。两条迁移可逆(down 改回原名/删列/删微信方式)。提交见同批前后端 commit |
| 2026-06-15 | 本地生活 UGC 加固(窗口⑤):违禁词过滤 + 顾客举报 + 反滥用限流 + 平台免责声明 + **L1-7 加密缺口修复** | 🟢 L3 实现 + 🔴 L1-7 PII 修复 + 🔴 L1-1 不碰钱确认 | 用户(发帖总开关 `locallife_ugc_enabled` 仍默认关,需用户拍板开放) | 在窗口④ UGC 发帖基础上补四层上线护栏:**①违禁词过滤**——发帖(及后台录入)扫 title+description+contact_info,命中即拒(422「内容含违规词」,不回显命中词防试探),词库 `business_settings.locallife_banned_words`(换行/逗号分隔,后台可增删,种子覆盖涉黄/赌博/诈骗洗钱/毒品违禁/外站强引流,`mb_stripos` 不区分大小写);**②顾客举报**——新表 `local_life_reports`,接口 `POST /api/v1/local-life/posts/{id}/report`(**auth:api 禁匿名**),理由白名单6项(「其他」必填说明 max500),同用户对同帖未处理举报去重(友好提示不报错),每用户每日上限 `locallife_report_daily_limit`(默认20);后台列表举报数徽章 + 举报处理页(下线帖 status→2 并把待处理举报→已处理 / 驳回举报→2 帖不动);**③反滥用**——最小发帖间隔 `locallife_ugc_min_interval_sec`(默认60s,429)+ 24h 内同标题重复拦截(422),沿用④每日上限5;**④免责声明**——列表/详情底部常驻短提示 + 发帖必勾「我已阅读并同意《本地生活信息发布规则》」(不勾→提交按钮灰显 disabled),短提示/全文 `business_settings.locallife_disclaimer`/`locallife_terms` 可配。**🔴 L1-7 加密缺口修复**:核查发现 `local_life_posts`(窗口④建,含 contact_info PII)`CREATE_OPTIONS` 为空 = **实际未加密**(contact_info 明文落盘),与 data-protection.md 此前「随全表静态加密」声称不符(MySQL 5.7 新建表不继承全库加密)→ 本窗口迁移内 `ALTER TABLE local_life_posts ENCRYPTION='Y'` 补加密;新建 `local_life_reports`(detail 可能含 PII)显式 `ENCRYPTION='Y'`;两表均已确认 `ENCRYPTION="Y"`。**L1-1**:违禁词/举报/免责全程不引入任何支付/押金/代收/下单/担保字段,仅信息墙。**总开关仍默认关**=现网不对外开放发帖,护栏齐备后由用户后台手动开。实测:违禁词422 / 正常发帖200 / 最小间隔429 / 举报200 / 重复举报去重200 / 「其他」无说明422 / Playwright 三态(免责提示·规则抽屉·勾选灰按钮·违禁词toast·举报抽屉) 全 PASS,console 无 JS 异常。提交见同批前后端 commit |
| 2026-06-15 | 本地生活 UGC 用户发帖 + PII 到期清除(窗口④) | 🟢 L3 实现 + 🔴 L1-7 PII / 🔴 L1-1 不碰钱 确认 | 用户(开关默认关,需用户拍板开放) | 顾客可在 H5「本地生活」自助发帖(标题/分类/价格/图片/联系方式),默认 `status=待审核`,后台「通过」才公开、「驳回」带理由。**L1-1**:发帖表单/接口**不含**任何支付/押金/代收/下单/担保字段,仅信息墙撮合。**L1-7 PII**:`contact_info` 列表永不返回、详情仅登录顾客可见、游客置 null;随全表静态加密;发帖设 `expires_at=+30天`,新建命令 `nezha:purge-locallife-pii`(每日03:40)到期抹 contact_info + 删上传图,保留帖子行/状态供审计(--dry-run 实测命中0,无过期数据)。**发帖入口总开关 `locallife_ugc_enabled` 默认关(0)** = 现网不对外开放发帖,前端 FAB 显示"即将开放",需用户验收满意后手动开(真实影响开关);每用户每日上限 `locallife_ugc_daily_limit`(默认5)防刷。顺带修 nginx `fastcgi_intercept_errors`(API vhost 关闭),使 Laravel 404/502 JSON 错误响应不再被 nginx 错误页替换、保留 CORS 头(原致前端跨域拿到 Network Error)。违禁词过滤/举报/公开总开关顺延窗口⑤。实测:游客发帖→登录门、登录→填表+传图→提交→待审核、后台通过/驳回、422 校验、PII 仅登录可见 全 PASS。提交见同批前后端 commit |
| 2026-06-14 | 离线支付付款截图文件上传(端到端)落地 | 🟢 L3 实现 + 🔴 L1-7 PII 覆盖确认 | 用户 | 补齐顾客离线支付**截图文件上传**(上一轮只通文本哈希,文件字段被刻意跳过)。前端 OfflinePaymentForm + 订单详情编辑表单渲染文件输入并改 multipart 提交(POST + `_method=PUT` 方法伪装命中原 PUT 路由,因 PHP 不解析 PUT 的 multipart);File 不入 redux(避免 redux-persist 序列化丢失),经表单 ref 在下单后随 FormData 提交。后端 `offline_payment()`/`update_offline_payment_info()` 对 `input_type==='file'` 字段用 `Helpers::upload('offline_payment/')` 存盘,`payment_info` 记完整相对路径 `offline_payment/xxx.webp`;编辑时不传新图则保留原图。**截图扩展名守卫**(仅 png/jpg/jpeg/webp/gif)从源头杜绝 purge/展示判定盲区。**PII(L1-7)覆盖确认**:截图随全表静态加密(at-rest);90 天 `nezha:purge-payment-proofs` 已验证连同截图文件一并清除(--dry-run 回填 120 天实测命中 `offline_payment/...webp`)。顾客端/后台/商家端订单详情均可点开查看截图。实测:USDT(可选)+ 人民币(必填)两态——必填缺图 403、非图扩展名 403、正常 200 入库 + 文件存盘 + track API 返回 `file_url`(200 image/webp)。提交见同批前端/后端 commit |
| 2026-06-14 | 退款机制② 原路锁定 + 限额限笔 落地 | 🔴 L1 (L1-2/3/4 退款原路) | 用户 | 退款接入「原路锁定 + 限额」: USDT 退款目标 = 由原始付款 tx 反查的原 from 地址(链上校验金额+地址, 公共节点免密钥 + 降级人工核), 法币退还原付款人(政策+凭证版), 金额强制 ≤ 原订单, 禁退第三方; 单笔/单日累计/单日笔数/退款窗口 限额(后台「风控设置→退款控制」可调), 超限转 admin 审核不直接拒(放行后该订单豁免); 全程留痕 nezha_refund_records(≥5年, 免于PII自动清除). 新增后台「退款留痕/审核」页(USDT 登记商家退款哈希做链上校验/凭证上传/超限审核). 总开关 nezha_refund_control_status **默认关(0)** = 现网退款行为不变, 需测试单验证 + 用户批准再开. 实测: 真实链上 tx 反查+转账校验/限额命中/留痕/grace 全 PASS. 提交 04da684 / f1c5a8c / 2814589 / 5f5042d |
| 2026-06-14 | L1-1 护栏: 直付订单禁走平台钱包退款 | 🔴 L1 (平台不碰钱) | 用户 | Admin/OrderController 退款分支: offline_payment(直付)订单**永不**走 wallet_add_refund 平台钱包退款(退到平台钱包 = 平台替顾客记账/欠款 = 碰钱), 一律原路由商家退顾客, 平台仅留痕. 堵住"钱包开关被开即违规"的潜在地雷(当前 wallet_status=0 本就睡着, 此为防御性硬化). 提交 f1c5a8c |
| 2026-06-14 | 修复离线支付凭证采集(退款前置) | 🟢 L3 (bug修复) | 用户 | offline_payment 提交原从 null 的 method_informations 读字段名 → 顾客交易哈希/截图全被丢弃(memory 已记之已知bug); 改为从 method_fields.input_field_name 读 + 前端表单同步修(顾客输入框/绑定). 这是 USDT 退款原路反查的**数据前提**(无原始 tx 则无从反查 from 地址). 真实流量 0, 属填坑非救火. 实测: 提交后 payment_info 含交易哈希 + 前端 USDT 哈希框正常. 提交 e70cac9(前端) / 6b82c5c(后端) |
| 2026-06-14 | 商家入驻意向表 (merchant_leads) | 🟡 新增 PII 类别 | 用户 | H5「我的→商家入驻」MVP 人工入驻线索:新表 merchant_leads 收店名/联系人/电话/微信/地址/品类/备注.电话属 PII,沿用全表静态加密(见同日条目)+ MasksSensitiveAttributes(demo 模式脱敏);提交即发邮件到运营邮箱.平台此步不碰钱,正式开店仍人工.留存:暂随业务保留,后续可纳入到期清理.详见 data-protection.md |
| 2026-06-14 | PII 静态加密 + 加密备份③ 实装 | 🔴 L1 (PII加密) | 用户 | MySQL 表空间加密(全部150张 InnoDB 表 `ENCRYPTION='Y'` + keyring_file/early-plugin-load),覆盖全部 PII 字段,验证读写正常+API200;keyring 在数据&备份目录之外.每日 AES-256 加密备份(留14份,密钥 /root/.nezha/backup.key,明文不落盘).诚实边界:防磁盘/备份被盗,非防服务器被攻破;5.7 的 redo/undo/binlog 未覆盖(binlog 已关).一次 MySQL 重启~2s.属 DB/配置层变更,repo 仅同步本文档 |
| 2026-06-14 | 支付凭证 90 天留存自动清除③ | 🔴 L1 (PII到期删) | 用户 | 命令 nezha:purge-payment-proofs + 每日计划任务,抹除超期 offline_payments 的 PII 字段及截图,保留行/状态供审计;链上/交易记录不动(另留≥5年).天数后台 nezha_payment_proof_retention_days 可调(默认90).提交 a31d25c |
| 2026-06-14 | Admin 后台 TOTP 两步验证③ | 🟡 访问控制加固 | 用户 | 零依赖手写TOTP(RFC6238已验证),opt-in默认关,含8个一次性恢复码+SSH应急关闭命令.E2E验证通过.提交 aa9fa45 |
| 2026-06-14 | 代码库密钥泄露扫描③ | — (核查) | 用户 | 前后端 git 全历史核查: .env从未入库, .gitignore已屏蔽密钥/凭据, 已知机密0命中. 无需修复 |
| 2026-06-14 | PII 静态加密方案评估③ | 🔴 L1 (待批准) | — 待用户 | 应用层加密会破坏搜索/派单功能,不适用;建议改 MySQL 表空间加密(透明,搜索照常).涉MySQL重启,待用户批准后实施 |
| 2026-06-12 | 建立合规文档体系 + 机制评级清单 | — | 用户 | 新建 INVARIANTS.md + docs/compliance/(AML/业务流/数据保护/本记录),用于合规留档与防止后续误改核心机制 |
| 2026-06-12 | 上线交易红旗风控① (后端+后台) | 🟡 新增机制 | 用户 | 5规则交易监控(单笔/单日/频次/金额特征)+人工审核队列+阈值后台可调;总开关默认关,待菜单美元化后启用 |
| 2026-06-10 | 拔除二清打款腿 | 🔴 L1 | 用户 | 禁用 RestaurantDisbursement,直付订单不记平台对商家负债,平台不持币 |
| 2026-06-10 | 确立 B 方案(顾客直付商家,平台不碰钱) | 🔴 L1 | 用户 | 规避二清/帮信/外汇风险;平台靠保证金收佣 |
| 2026-06-15 | 本地生活 多语言条款 + 《个人数据处理通知》同意采集**骨架**(默认关) | 🔴 L1 (对外法律文本 + 新增同意采集，**未生效**) | 用户(暂无注册实体/未律师审校→保持默认关) | 为将来合规上线预置骨架，**当前不改变任何线上行为**。① **多语言条款/免责**：后端按 `X-localization` 返回 `locallife_{terms,disclaimer}_{en,ru,hy}`，留空回退中文；后台「护栏与文案设置」加各语种编辑框。② **《个人数据处理通知》PII 同意采集**：新增 `locallife_pii_notice{,_en,_ru,_hy}` + 独立开关 `locallife_pii_consent_enabled`(**默认 0/关**)；发帖接口加 `agree_pii` 守卫、H5 发帖表单加第二个必勾同意项，**门控「开关开 且 正文非空」两道门**。当前全空/关 → **API 返回逐字节同旧、H5 不出现第二勾项、不采集任何新同意**(Playwright 真机:默认态无第二勾项+中文条款正常、console=0;mock 开启态:两勾项渲染+提交按钮正确门控;API:`ugc_pii_required=false`)。🔴 **正式开启前置**:① 亚美尼亚注册实体(数据控制者全称+地址);② 律师审校(亚语须母语法律译者)。**用户决定:俄/亚语文案未审校全部先不上、暂无主体故 PII 通知先不上**。详见 `legal/local-life-terms.md` + `data-protection.md`。提交见同批前后端 commit |

| 2026-06-16 | 主站《用户协议》《隐私政策》上线 | 🔴 L1 (对外法律文本 / 隐私告知) | 用户 | 主站「我的」原有两入口但 `data_settings` 无内容 → 两页**只剩标题、正文空白**(隐私告知缺失 = 合规缺口)。新增法务正本 `docs/legal/{terms-and-conditions,privacy-policy}.md`(复用本地生活同口径:L1-1 平台不碰钱 / 退款原路退回 / USDT 退原地址 / PII 静态加密+保留期,事实依据 `data-protection.md`),写入 `data_settings`(`type=admin_landing_page`,`key=terms_and_conditions`/`privacy_policy`,base value=中文 HTML,后台「业务设置→条款/隐私」可编辑)并清 `rememberForever` 的 `data_settings_*` 缓存。前端统一标题:`zh-CN.js`「条款与条件」→「用户协议」、`profile` 菜单「用户隐私」→「隐私政策」。Playwright 真机:两页中文全文渲染 + console=0 + 标题/菜单一致。**被动告知页,不采集任何新同意**,与暂缓的 `locallife_pii_consent_enabled`(默认关)互不影响。⏳ 待办:① 注册实体落地后补「数据控制者=公司全称+注册地址」;② 商用前当地律师审校;③ 英/俄/亚语译本(暂全站回退中文)。对外联系邮箱 nezhadelivery@gmail.com。提交见同批前/后端 commit |
| 2026-06-17 | 直付单退款「通知商家退款 + 留痕 + 商家标记已退款」(F-4 闭环) | 🔴 L1 (退款/资金流，加固非改动红线) | 用户(批准方案) | 资金闭环 QA 的 F-4 遗留: admin 取消/退款直付单(offline_payment)后，平台过去不通知商家去退、也无"商家已退款"留痕。本次补闭环，**平台全程不碰钱(L1-1)，仅通知/留痕/状态流转**: ① admin 取消/退款直付单时**无视退款护栏开关**必建 `NezhaRefundRecord(status=pending_merchant_refund)`，记原路通道/USDT原地址(复用 lock_route 反查, L1-2/L1-3)/应退额(≤原单)/操作人，留存≥5年(L1-4); ② 推送商家 + 商家端「订单→待退款」tab/卡展示应退额·原渠道·USDT原地址·顾客邮箱+地址电话·付款凭证; ③ 商家原路退款后点「标记已退款」(可选填 USDT 退款 tx hash/备注)→记录转 `merchant_refunded`; ④ **绝不引入平台代退/平台钱包退款**(直付单 refund 带 !isDirectPay 钱包护栏 + refund_before_delivered/refund_order 对 offline_payment 不动平台现金桶)。验证: 事务回滚仿真 12/12 全过(adminWallet/商家钱包全程零变动)、blade 编译通过、vendor 内部派发渲染 details+list 200。状态列 varchar(20)→(40)(pending_merchant_refund 23字符)。提交见同批后端 commit |
| 2026-06-18 | 顾客取消已付款直付单 → 待退款留痕+联系商家提示(扩展 F-4 到顾客侧) + 确认收款防复活 | 🔴 L1 (退款/资金流，加固非改动红线) | 用户(批准方案) | QA(2026-06-18 取消/非正常结束流程)发现: 顾客自助取消 pending 直付单(已传凭证=钱已直付商家本人)后,平台过去① 不清理 offline_payments(残留 pending → 已取消单可被「确认收款」复活成 confirmed/paid,真实竞态) ② 无待退款留痕、顾客零退款指引,且旧文案让「联系客服/管理员」退款=B方案错位(平台不碰钱,admin 退不了)。本次(平台全程不碰钱 L1-1,仅通知/留痕/状态流转): ① cancel_order 对已传凭证的直付单置 offline_payments→canceled + 调 record_direct_pay_refund_pending(新增 $treatProofAsPaid: 凭证行存在即视为已付款 → 建 NezhaRefundRecord=pending_merchant_refund 留痕 L1-2/L1-3/L1-4 + 通知商家原路退款) + 给顾客发「请联系商家原路退回」站内信 + 返回中文文案; ② 确认收款(Vendor + Admin)加 order_status ∈ [canceled/failed/refunded/refund_requested/refund_request_canceled] 终态守卫,防已结束单被复活(双重防御: offline_payments≠pending + order_status 终态)。 **deny(拒收)经用户决定: 保持订单可补救 + 只改文案,不自动建待退款(避免真实顾客只是截图模糊却被迫重新下单付第二次)**。 🔴 架构约束(用户确认): B方案顾客点对点直付商家、平台无支付通道异步回调,故**无法自动退款**,退款只能「顾客联系商家原路退回 + 平台留痕」——这是 B方案固有限制,非缺陷,正式自动退款须未来接入带回调/托管的支付通道(届时另立项)。 验证: 真单实跑 order_transactions=0 / wallet_tx=0(平台不碰钱)、offline_payments=canceled、NezhaRefundRecord=pending_merchant_refund(3800/rmb)、顾客站内信送达、已取消单确认守卫拦截、确认后取消返回中文。php -l 全过。提交见同批后端 commit |
| 2026-06-19 | 订单超时兜底：杜绝订单无限停留在「待接单/备餐中」 | 🔴 L1 (退款/资金流/自动取消，加固非改动红线) | 用户(2026-06-19 批准方案) | 生产样本 #13 待接单 5h、#12 待接单 1 天、#8 备餐 1 天(接单时未填预计出餐时间致永远算不出超时)。集中规则 `app/CentralLogics/NezhaOrderTimeout.php`(全文 `docs/ORDER_TIMEOUT_RULES.md`) + 每分钟兜底任务 `nezha:order-timeout-sweep`(幂等账本 `nezha_order_timeout_events` 唯一约束保证每单每动作恰好一次)。三阶段:① 凭证审核(pending+offline)——未传凭证图(确定没付钱)10min 自动取消(无款无退款)；已传凭证图 10min 邮件商家、20min 自动取消；② 付款确认后待接单(confirmed) 10min 邮件商家、20min 自动取消；③ 备餐(processing) 超 ETA+15min 或商家未填预计出餐→升级客服(邮件商家+客服信箱)，**不自动取消**。**L1 落实**:自动取消**绝不触发平台退款**——已付款单走 `OrderLogic::record_direct_pay_refund_pending` 生成 `nezha_refund_records(pending_merchant_refund)` 留痕(L1-2/3/4)+通知商家原路退；顾客端文案永远显示真实责任人=商家+联系商家原路退回，绝不出现「自动原路退款/平台已退款」。配套:商家接单转 processing 时 `processing_time` 改必填(后端 `required_if` + 面板 SweetAlert inputValidator)根治 #8；订单详情 API 下发 `nezha_timeout`(下一步/处理截止/退款方式/预计到账)前端只渲染、删散落计时器。总开关 `nezha_timeout_status` 默认1(关=只展示不动作)，阈值全 `business_settings` 可调。验证:隔离事务(Mail::fake+通知通道置空+最终回滚+真实在范围单移出扫描)33 项断言全 PASS(三阶段动作选择+边界19/21min+幂等连跑第二轮0动作+自动取消产生退款留痕+邮件计数)。⏳ 真机前端验证待构建解锁(他窗 WIP 在前端树，按守门铁律未构建)；⚠️ 上线前先清 QA 测试单 #8/#12/#13(否则首轮 sweep 会对其自动取消+邮件真实商家)。提交见同批前后端 commit |

## 待实施(实施时在此追加记录)
- ② USDT 通道: ✅ **退款地址锁定**(由原始付款 tx 反查 from 地址 + 退款 tx 链上校验金额/地址)已落地 2026-06-14(见上表); ✅ **退款链上记录留存 ≥5年**(nezha_refund_records 表) 已落地; ⏳ **仍待**: 付款来源地址 OFAC SDN/黑名单筛查(命中拒收下单) — 🔴 L1
- ③ 剩余项:RBAC、审计日志、自动安全更新、密钥轮换、泄露应急流程 — 🔴 L1 / 🟡
  - ✅ 已完成: 支付凭证90天自动删、Admin 2FA、git密钥扫描、服务器加固(key-only/fail2ban/限速)、后台nginx Basic Auth、**PII静态加密(MySQL表空间加密)**、**每日加密备份(AES-256,留14份)**

---

## 2026-06-21 —— 本地生活/入驻「硬禁业务」机制 + 入驻审核 SOP（合规加固）

**背景**：明确"哪些业务平台坚决不能让商家上线"（换汇/加密买卖/医美注射/性服务/赌博/制裁规避等），并落成机制 + 文档两层，不再只靠人记。经平台负责人（用户）确认后实施。

**改了什么**
1. **违禁词库补全（L2 业务参数）**：线上 `business_settings.locallife_banned_words` 从 37 词补到约 100 词，新增换汇/货币兑换/代收代付/买卖usdt/承兑/医美注射/肉毒/玻尿酸/帮俄罗斯采购/规避制裁/高利贷/套现等**业务平台硬禁词**，并补回此前只在代码默认值、从未生效到线上的签证诈骗（包过签/假流水）与按摩涉黄（大保健/楼凤）词。旧值备份：后端根 `_locallife_banned_words_backup.txt`。**仅影响新发布内容，不回溯。**
2. **共享筛查器（L3 实现）**：新增 `app/CentralLogics/NezhaContentScreen.php`，统一供「用户 UGC 发帖」与「本地生活商家录入」两处共用一套词库（消除两套词库漂移）。`LocalLifeController::bannedWords()` 改为委托该类。
3. **类目合规等级三级（L3 机制载体）**：`local_life_categories` 加 `compliance_level`（0 可上 / 1 需牌照人工审 / 2 硬禁）。迁移 `2026_06_21_100000_add_compliance_level_to_local_life_categories`。回填现有 is_sensitive=1（移民/签证/按摩）→ 等级 1。后台类目编辑页加三级下拉；**等级 2 即使勾启用也强制 status=0、前端不渲染、statusToggle 拒绝启用**。is_sensitive 由控制器同步维护（=等级≥1）。
4. **本地生活商家录入接入筛查**：`LocalLifeMerchantController::validateData` 命中硬禁词即抛 ValidationException 拒绝入库。
5. **文档**：MERCHANT_GUIDE 加「哪些业务坚决不能上线（红线清单）」；ADMIN_GUIDE 8.7 加「入驻审核 SOP + 类目合规等级 + 违禁词库管理」。

**合规定级**：本批为**强化**合规控制，不触碰 L1（平台仍全程不碰钱、不改退款/制裁/留存机制）。违禁词内容属 L2（后台可调），机制代码属 L3。

**验证**：php -l 全过；后台类目 list/create 内部派发渲染 200 且「合规等级」下拉出现；NezhaContentScreen 实测 换汇/医美注射/承兑usdt/包过签 BLOCK、正规美发/租房/积分兑换券/日本代购 不误伤；activeNames 仍 8 个无回归。
**未覆盖（需运营/真人）**：后台真人点「新建类目选等级2+勾启用」确认强制停用提示；后台真人录入命中词商家看到拒绝提示；前端真机看硬禁类目不渲染。

### 2026-06-21 AI 在线客服上线（L3 实现 + L1-1 / L1-7 合规说明）
新增 H5 内置 AI 客服（`app/CentralLogics/NezhaCsAssistant.php` + `NezhaCsClassifier.php`），DeepSeek 驱动，接入 `Api/V1/ConversationController::messages_store` 的 admin 分支。
- **L1-1（平台不碰钱）**：AI 绝不承诺退款、不谈金额、不处理资金。敏感词（退款/钱/投诉/纠纷等）确定性命中即不经模型，转为引导顾客直接联系商家。给商家的「转达」是固定模板的通信路由（不含金额/承诺、AI 不参与撰写），属 B 方案"平台只协调"，不构成代收代付，不触 L1-1。
- **L1-7（PII / 数据保护）**：喂给 DeepSeek 的订单上下文已脱敏（仅状态/取餐号/餐厅名，无地址/电话/金额）；审计表 `nezha_cs_logs` 只记分类/动作/模型/tokens，不存消息正文（无 PII 复制）。数据出境提示：低风险 FAQ 消息经 DeepSeek（中国境内）处理、PII 面小；含 PII 的敏感问题不喂模型。
- **越权 / 提示注入**：AI 进程不含任何密钥/后台信息（无从泄露）；输出过滤防吐疑似密钥、防自曝 AI 身份；商家转达 30 分钟限频防刷屏/武器化。
- **开关**：`nezha_cs_ai_status` / `nezha_cs_merchant_relay_status`（L2 真实影响开关，当前均已开）。

### 追加〔2026-06-21〕招聘类反诈/反贩运护栏
- 违禁词库再补 18 个**博彩/园区诈骗招聘强特征组合词**（博彩招聘/菠菜招聘/博彩客服/网赌·网投·跑分招聘/园区招聘/缅北/缅甸·柬埔寨园区/妙瓦底/金三角/出黑·洗白·担保上岸/包吃住包机票/日结高薪诚招），刻意避开"厨师/包吃住/日结"等正经招聘单词只收明显特征组合。线上 locallife_banned_words 100→118。实测"招聘厨师包吃住日结/餐厅招聘后厨"不误伤、"博彩公司招聘客服/缅北园区招聘/金三角博彩客服"BLOCK。
- ADMIN_GUIDE 8.7 D 加「招聘类目专项红线」：明确关键词拦不住"表面干净"的招聘帖（如以招厨师名义实为违规工作），招聘类必须人工审为主 + 接举报快速下线 + 违规帖冻结留证；并提示开放招聘类目应设合规等级=1(人工审)。
- 前端 local-life 列表：浏览招聘类类目(名称含 招聘/招工/求职/找工作/工作机会/兼职)时显示常驻高危风险提示条（核实真实性/警惕高薪海外诱饵/平台不担保/发现违规举报）。⚠️前端构建因别窗 SettingPage 半成品暂缓，代码已提交。
- 合规定级：仍为强化护栏，不触 L1。

### 2026-06-21 AI 客服增量C（联系不上商家工单 + 多语言，无 L1 影响）
顾客「联系不上商家」时：给商家电话 + 自动邮件催商家 + 留工单（nezha_cs_tickets）+ 通知运营。仍属 B 方案「平台只协调通信」，平台不碰钱、不代退，无 L1-1 影响；工单/邮件不含金额承诺。多语言：按顾客语言回复。

---

## 2026-06-21 —— 🔴 L1 变更(L1-7)：本地生活违规帖「证据冻结」(legal hold)

**背景**：本地生活帖联系方式默认 30 天自动清除(L1-7)；但违规帖 / 配合执法时需保留证据。经平台负责人(用户)明确批准实施。

**改了什么**
- `local_life_posts` 加 `legal_hold/legal_hold_reason/legal_hold_at`(迁移 `2026_06_21_140000_add_legal_hold_to_local_life_posts`)。
- `nezha:purge-locallife-pii` 清除查询加 `->where('legal_hold', false)`：冻结帖 contact_info + 图片**豁免到期清除**。
- 后台帖子列表加 🔒 冻结/解除按钮(`legalHoldToggle` + 路由 `admin.local-life.legal-hold`) + 「冻结留证」徽章。
- **§5.3 用户承诺同步加例外**：正本 `legal/local-life-terms.md` + 线上 `business_settings.locallife_terms` 均加「(但依法律要求，或为处理违规内容、配合主管机关调查而确有必要时，相关信息将在必要范围与期限内保留)」。线上旧值备份 `_locallife_terms_backup.txt`。
- 文档：`data-protection.md` + `ADMIN_GUIDE 8.8` + `INVARIANTS.md` L1-7 附注。

**L1-7 边界(防过度留存)**：仅运营人工设/解(非举报自动触发)；目的限违规处理/配合执法；用尽目的应解除冻结让其正常到期清；举报人 PII(report detail)不随之冻结、仍按 180 天清。**仍不触 L1-1**(全程不碰钱)。

**验证**：php -l 全过；迁移已跑(3 列存在)；dry-run 实测 legal_hold=1 帖不进清除名单、过期未冻结帖进清除名单；后台列表内部派发渲染 200、🔒 按钮与冻结徽章出现。
### 追加〔2026-06-21〕隐私政策对齐本地生活留存
隐私政策(正本 docs/legal/privacy-policy.md + 线上 data_settings.privacy_policy)「第5条 保留与删除」加 (5)：本地生活帖联系方式默认 30 天清除 + 依法/配合调查留证例外，使**用户可见的隐私政策**与实际数据处理(§5.3 + legal hold)完全一致(透明度义务)。线上旧值备份 `_privacy_policy_live_backup.html`；改后清 Laravel 缓存 `data_settings_privacy_policy_*`，nginx 60s API 缓存自动过期；真机 /privacy-policy 验证条款渲染、console 0。属用户可见法律文案对齐,不触 L1。

### 2026-06-21 AI 客服 运营助手 + 翻译增强（L3）
- 运营助手(adminAssistant)：超管在后台问客服运营情况，会把近期顾客消息(最多80条)喂给 DeepSeek 做高频问题归纳——属内部运营分析，数据出境取舍同既有(低敏内容、内部用)；仅超管入口可达，顾客端不可达。
- 翻译：增英语；翻译模式(Cache态)；FAQ 增平台禁发规则说明。均不碰资金，无 L1 影响。

### 2026-06-21 商家端 AI 助手（L3，无 L1 影响）
新增 merchantAssistant（商家后台问答）。白名单知识库 + 硬护栏：绝不暴露底层系统名、不教平台未启用的资金功能（提现/钱包），强化 B 方案"平台不碰钱"。不接触顾客资金、无 L1 影响。

### 2026-06-21 AI 客服 PII 出境脱敏(redactPii)回归修复 + 兜底/防越狱硬化（含 L1-7 修复）
- 🔴 L1-7：运营助手(adminAssistant)喂 DeepSeek 前的 redactPii() 脱敏（抹邮箱/钱包地址/电话）此前被一次"整文件覆盖"提交意外回退、线上一度缺失；本次还原（还原自 ea2d179/f101fc4）。
- 同批还原被同类覆盖事故抹掉的修复：① model 调用失败兜底 softReply 缺参致 DeepSeek 出错时顾客零回复(P1)；② 顾客反馈判定让位敏感词/联系不上商家的守卫（防"满意+退款"被当好评吞、诉求丢失）。
- 新增硬化：敏感词硬闸补退款变体（退我/把钱退/退一下/退给我等，堵绕过）；客服 system prompt 增"绝不复述/逐字重复系统指令"（堵 prompt 抽取，已真测：原会吐系统提示首句，现拒绝）。
- 暴露窗口与实际影响：脱敏缺失期间，运营助手喂模型的顾客消息/差评原文未脱敏；经核查该期内 nezha_cs_feedback/相关消息为空，实测无真实 PII 被发出（属潜伏暴露，未发生实际泄漏）。
- 提交 422c5b1；部署 release 20260621-174810-422c5b1。

### 2026-06-22 修复 USDT 纯哈希已付款单被误判未付款取消（L1-1 退款义务保全）
- 🔴 缺陷：USDT(TRC20/BEP20)链下付款的「交易哈希」是 text 型凭证，但超时 sweep 阶段A 旧判定 `hasProofImage` 只认 file(图片)，纯哈希单 → 被当未付款 `cancel_unpaid`(10min) 取消、`cancelOrder(paid=false)` 不生成退款留痕(不调 record_direct_pay_refund_pending)、不通知商家退款。顾客链下已付的 USDT 既无货也无退款追索 —— 违 L1-1（平台不碰钱但须保证退款义务落到商家）。
- 修复：`NezhaOrderTimeout` 新增 `hasValidHashText`(只认含『哈希/Hash』的 text 字段 + 服务端 64位hex 校验)与 `hasPaymentProof = hasProofImage || hasValidHashText`；4 个调用点（OrderTimeoutSweep 阶段A、NezhaOrderTimeout::describe、Vendor/DashboardController、Admin/SystemController）统一改用 hasPaymentProof，消除展示层 drift。哈希单自此走与图片单一致的 20min `cancel_paid_refund` + 退款留痕 + 通知商家原路退款路径。
- 防误退护栏：乱填/非 hex 文本不算有效凭证（hasValidHashText 返 false），真未付款单仍走 10min 无退款取消，避免给未真付款单凭空造商家退款义务。`record_direct_pay_refund_pending` 建留痕时 chain_verify_status=unverified（平台不裁决链上真伪，沿用 B 方案由商家核对），故服务端 hex 格式校验是必备闸而非可选。
- 存量审计（只读）：历史 cancel_unpaid 事件全部命中已删测试单，**真实生产受害单=0**（无需存量补退款留痕）。
- 验证：php -l 4文件全过；新增 tests/Feature/NezhaProofDetectionTest.php 7用例/24断言全过（DatabaseTransactions+内存对象，零写库）；工作树 dry-run 实测同一 USDT 哈希单由旧 cancel_unpaid 改为 `cancel_paid_refund`(26min 有凭证超时自动取消+退款留痕)。
- 不触二清：平台仍全程不碰钱、退款仍由商家原路退；本改动只把『已付识别』补全，资金流向不变。经 /debate 多实例对抗核验后实施。

### 2026-06-25 超时自动取消：制裁命中单不生成「待商家退款」指示（L1-6 张力·防御性收紧）
- 🔴 张力：超时 sweep `cancelOrder(paid=true)` 旧逻辑对【有凭证】离线单无条件 `record_direct_pay_refund_pending` 生成「待商家退款」，**不复跑制裁筛查**。一笔付款来源命中 OFAC SDN 的 USDT 离线单若 20min 无人处理被超时取消，系统会生成退款指示＝促成商家把钱原路退回受制裁地址，与 L1-6（制裁命中即拒/不与受制裁主体交易）抵触。命中路径：商家从未确认（从未筛查）+ 商家确认后筛查 denied（凭证图仍在，sweep 仍当「有凭证已付」）。
- 修复（防御性收紧）：`OrderTimeoutSweep::cancelOrder` paid 分支生成退款留痕前复跑 `NezhaSanctionScreen::screen_order`；**仅 action=reject（确凿命中制裁）** 时不生成退款指示，改为只取消 + `record_reject` 留痕 + `escalateToSupport` 升级人工裁决退款方向（勿原路退回受制裁地址）。pass/inconclusive/筛查异常一律照常生成退款留痕（不误伤正常单，与原行为一致）。受 `nezha_sanction_screen_status`(默认1开) 门控。
- 退款方向最终裁决（原路退回受制裁地址 vs 冻结待核）属 L1 判断，留用户/律师；本改动只阻止系统【自动】促成向受制裁主体退款，不替代人工裁决。
- 验证：php -l 过；e2e（事务建删测试单）— RMB(screen=pass)→退款留痕照常生成、USDT-inconclusive→退款留痕照常生成（均不误伤）、screen_address 注入测试制裁地址→命中产出 reject 信号。reject→跳过退款+escalate 为干净条件已代码审；完整链上 reject e2e 需真实受制裁 USDT 单（H4 固有潜伏性、当前近零真单流）。
- 残留 edge：USDT-inconclusive（来源无法链上核验）仍生成退款留痕——按 L1-6「命中即拒」仅拦 reject，inconclusive 不属命中；如需更严可后续评估对 inconclusive 也 escalate。
- 经测试后提交 + nzdeploy-api 上线（2026-06-25）。

### 2026-06-28 新增「顾客举报商家(餐厅)」功能（L1-7 PII 合规 + L1-1 不碰钱）
- 新增顾客侧举报餐厅入口：`POST /api/v1/customer/restaurant/{restaurant_id}/report`（中间件 auth:api，仅登录可举报（2026-06-28 用户拍板，与 local-life 帖/评价举报禁匿名口径一致））。新表 `restaurant_reports`（restaurant_id/vendor_id/user_id/guest_id/reason/description/status）。
- **L1-7 PII**：`description`（自由文本，可能含 PII）随表静态加密——迁移显式 `ALTER TABLE restaurant_reports ENCRYPTION='Y'`（5.7 新表不继承全库加密），实测 `CREATE_OPTIONS=ENCRYPTION="Y"`。到期清除：`nezha:purge-restaurant-reports`（默认 180 天置空 description，保留举报行/reason/status 供审计，`business_settings.nezha_restaurant_report_retention_days` 可调），bootstrap/app.php withSchedule 每日 03:57 调度。
- **越权防护（IDOR）**：举报人身份（user_id/guest_id）与 vendor_id 一律服务端取定、绝不信 body —— 实测 body 注入 user_id/vendor_id/guest_id=99999 均被忽略（user_id 取自登录用户、vendor_id 由 restaurant 派生）。餐厅不存在→404。
- **防刷三层**：同用户对同店待处理去重（友好 200）/ 同用户+同店 10 分钟 Cache 限频 / 每人每日 10 次。
- **后台可见（不进黑洞）**：`admin/restaurant-report/list`（侧边栏「举报商家」入口）列表+筛选+标记已处理/驳回；进程内真渲染验证全布局非 500、举报数据/操作/侧边栏链接齐全。
- **L1-1**：全程不含任何资金字段，平台不碰钱；举报仅入后台人工审核队列、不自动惩罚商家（刷量顶多制造审核噪声、不伤商家）。
- 前端独立抽屉 `ReportRestaurantDrawer.jsx`（理由单选+说明+提交，真机 iPhone12 三态 + 真接口 200 + console0 验过），未接线、由餐厅页/订单页「···」菜单窗口接入。
- 验证：建表 + ENCRYPTION 实测；live API curl 7 场景（落库/去重/404/422×3/401）+ 登录态 Passport token 各通过；测试数据已清零。提交后端 1eff888 部署 release 20260627-165737-1eff888；前端 6bd55b0（inert 未部署）。
- 注：现有两个举报端点（local-life 帖 / 评价）为 `auth:api 禁匿名`，本端点 2026-06-28 用户拍板改为**仅登录可举报**，已由 apiGuestCheck 切到 auth:api，与上述口径一致。


### 2026-07-03 中途退回押金(营业中·A3 S3-B)— L1-8 押金退还的营业中实现(dormant)
- 定级 L1-8(押金持有+退还)。区别于 NezhaOffboard(完整离场全退), 本项做商家【仍营业】时退回部分/超额押金。开关 `nezha_topup_refund_status` 默认 0 服务端强制 dormant。业主 2026-07-03 批准设计 v1。
- 🔴 **/debate 三路红队(资金安全/合规L1-8/防薅内控)一致阻断 v0**, 核心: 押金运行时是"死抵押"(仅 offboard 离场用·无 clawback), v0 让商家营业中掏空兜底无追偿; offboard 的门抄了但其"settling全冻结/全额置零/uq_active/离场settleInflight"四前提中途退统统不成立。9 类阻断存 `docs/topup_refund_debate_archive.md`。
- 硬化 v1(运营核算制·不做自助快退): `CentralLogics/NezhaGuaranteeRefund`(approve/pay 两段) 逐门 —— G0 开关+互斥(is_deposit_credit_frozen 覆盖 settling+owing+offboarded) / G1 欠款 deposit<0 挡 + 抽佣开须人工核敞口 / G2 tier=NULL fail-closed·exempt 引 offboard·floor 锁内算 / G3 每笔制裁实时复筛四态 fail-closed(approve+pay 各一次) / G4 户名核对**代码强制**(normHolder CJK-safe + 身份指纹 kyc_apply_fp·收款账户锁定 KYC 不给现填·变更即挡无 override) / G5 钱包行锁+C4 快照+uq_active_refund 结构墙 / G6 频率+高额次日转。放款主体=平台线下法币转到 KYC 锁定账户, 不接网关/不自动扣款。
- 🔴 实施中 verification 逮到真 bug: `NezhaKycScreen::normalize_name` 用 `[^A-Z0-9 ]` 剥中文 → 两个不同中文名都归一化成空串"相等" → 中文商户户名核对形同虚设 → 改 CJK-safe normHolder + 身份指纹(免疫制裁复筛写回)。
- 迁移 2026_07_03_140000+140100(topup_requests 补退款列); 逐门反例 harness **26/0 PASS**(四处对账 + 每门造反例被挡 + 事务 rollback 零残留)。经 /debate 多实例对抗核验后实施。
- 遗留: 缴纳付款人第三锚点(缴纳侧补捕获)、clawback 活兜底(抽佣上线的 B 期再单独 /debate)。开关顺序待进 PRELAUNCH_SWITCHES(offboard 未开→refund 不得开)。
