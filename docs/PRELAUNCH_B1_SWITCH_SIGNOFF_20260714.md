# B1：三项开关签收表（2026-07-14）

状态：**三项当前均为 1、三项均未签收、production NO-GO**。

本表把“只读现值”“自动化已证明的部分”“必须由真人补的证据”和“owner 批准目标值”分开。当前值 `1` 只是 production 事实，不是批准；B1 没有改开关、配置、缓存、数据或素材。保持 `1` 或另批回退 `0` 都必须由对应 owner 明写，任何改值另取精确 Go。

## 1. 固定对象与填写规则

- API 候选基线：`a53cfb5c967daa5917ce2cb4c2489d6799434ff2`；Web 候选基线：`b4e0ea0f17e3bfc65b3eebe9e645f5334de0faed`。API 候选已覆盖并回归 `b14c9c58bee66b59a45bb338f2d742609a3466f3` 运行时代码。
- production 当前为 API `dea5dd11a2a57b57660608e9c37a4fd528aa4efe` / Web `2f81803`，候选与 production 有版本差；签收必须写清测试对象，不能把候选测试当作 production 已部署证据。
- `批准目标值` 只允许 owner 填 `0` 或 `1`；留空即未签。勾“通过”还必须给出证据引用、签名、日期时间/时区。
- 若证据所对应的 SHA、素材、通知端点、账号、开关依赖或运行环境改变，该项签收失效并重测。

## 2. 总表

| 开关 | production 只读现值 | 自动化/只读已关闭 | 尚缺人工事实 | 批准目标值（owner 填） | 最终决定（通过/附条件/拒绝） |
|---|---:|---|---|---|---|
| `nezha_preorder_status` | `1`；依赖 `schedule_order=true` | API 预约聚焦 53 tests；Web 取消动作契约 | 真实 UI 下单/取消/并发/刷新、商家作业台、产品与商家运营批准 |  |  |
| `nezha_notif_async_status` | `1` | 异步通知 7 tests；两个 queue worker online；Redis `PONG`；failed jobs=0 | FCM/邮件/Telegram 专用收件人真实回执及运营签名 |  |  |
| `nezha_merchant_video_status` | `1` | production current + shared storage：商户 12 stored=2、normalized=2；两条 cover 存在且外链为 `v.douyin.com` | 内容 owner 在目标设备批准两条封面、标题、外跳与返回体验 |  |  |

## 3. `nezha_preorder_status`

### 已有证据边界

- `schedule_order=true`，所以当前不是“总闸开但依赖关”，而是功能已暴露、owner 尚未签。
- exact-main 强制 SQLite `:memory:` 下预约聚焦 53 tests 已覆盖服务端时序、窗口、取消和作业台语义；Web 取消动作契约通过。
- 上述证据不包含物理设备完整链、真实商家操作或 production 候选部署。

### 人工验收清单

| # | 必测路径 | 预期 | 结果（通过/失败） | 设备/账号/证据引用 |
|---|---|---|---|---|
| P1 | opt-in 餐厅可选择合法预约时间；非 opt-in 餐厅无错误入口 | 可用性与店级权限一致 |  |  |
| P2 | 截止时间前后、跨日、刷新/重进 | 后端权威窗口一致，无客户端时间假成功 |  |  |
| P3 | 顾客下预约单后，顾客历史/追踪与商家作业台一致 | 时间、状态、数量和配送批次同源 |  |  |
| P4 | 合法取消、不可取消边界、重复点击/并发 | 只有一个动作 owner，不重复退款/通知 |  |  |
| P5 | 320/390 移动视口及目标物理设备 | 键盘/安全区/返回/刷新无阻断，0 横向溢出 |  |  |
| P6 | 商家运营确认接单、提醒和异常处置可执行 | 商家理解且能完成，不靠开发者解释 |  |  |

产品 owner：批准目标值 `____`；结论 `____________`；证据 `____________`；姓名/角色 `____________`；日期时间/时区 `____________`。

商家运营：结论 `____________`；店/操作者 `____________`；证据 `____________`；姓名/角色 `____________`；日期时间/时区 `____________`。

## 4. `nezha_notif_async_status`

### 已有证据边界

- exact-main 7 tests 覆盖：开时 push/HTTP/Telegram 入队、缺 chat/text 时跳过，以及关时同步回退。
- 2026-07-14 B1 只读复核：两个 production queue worker online、累计重启各 83（`--max-time=3600` 周期）、Redis `PONG`、failed jobs=0。
- `/config is_mail_active=true` 仅说明配置声明；queue/测试/配置均不证明 FCM、邮件或 Telegram 到达真实设备。

真实回执使用 [外部 QA 签收表](PRELAUNCH_B1_EXTERNAL_QA_SIGNOFF_20260714.md)，至少逐渠道给出事件 ID、provider/message ID、收件时间、锁屏/后台状态、截图和去重结果。

| 渠道 | 专用收件人/目标（遮蔽） | 前台/后台/锁屏 | provider 接受证据 | 真实收达证据 | 重复/延迟结论 | 渠道 owner 签名 |
|---|---|---|---|---|---|---|
| FCM |  |  |  |  |  |  |
| 邮件 |  |  |  |  |  |  |
| Telegram |  |  |  |  |  |  |

运营 owner：批准目标值 `____`；结论 `____________`；允许的最大延迟/失败处置 `____________`；证据 `____________`；姓名/角色 `____________`；日期时间/时区 `____________`。

## 5. `nezha_merchant_video_status`

### 已有证据边界

- 只读证据必须来自 production current release + shared storage；独立 worktree 的隔离 storage 会误报素材不存在。
- 商户 12 当前 stored=2、normalized=2；两条为 `https://v.douyin.com` 外跳，cover URL 均存在，技术可见性成立。
- 为遵守零 production 写入，本轮没有调用会增加浏览计数的公开详情 API；没有批准标题、封面、内容、版权、品牌适宜性、外跳落地页或目标设备返回体验。

| 项目 | 视频 1 | 视频 2 | 内容 owner 结论/证据 |
|---|---|---|---|
| 标题准确、不过度承诺 |  |  |  |
| 封面清晰、版权/品牌允许 |  |  |  |
| 外跳域名与最终落地内容可接受 |  |  |  |
| iOS/Android/微信/TG WebView 打开与返回 |  |  |  |
| 内容失效/下架的运营 owner 与 SLA |  |  |  |

内容 owner：批准目标值 `____`；结论 `____________`；素材/版权依据 `____________`；证据 `____________`；姓名/角色 `____________`；日期时间/时区 `____________`。

## 6. 非第四项偏差：`nezha_autooffline_status`

production 只读现值 `1` 符合 A 类应开启目标，因此不计入本表三项漂移；但当前值仍不能替代真实通知和阈值签收。其通知/阈值证据由商家运营另签：阈值 `____________`；通知回执 `____________`；姓名/角色 `____________`；日期时间/时区 `____________`。

## 7. 汇总签收与停点

| 角色 | 三项是否全部签收 | 附带条件/失效条件 | 姓名 | 日期时间/时区 |
|---|---|---|---|---|
| 产品负责人 |  |  |  |  |
| 商家运营负责人 |  |  |  |  |
| 通知运营负责人 |  |  |  |  |
| 内容负责人 |  |  |  |  |
| QA/安全复核人 |  |  |  |  |

关闭条件：三个 `批准目标值` 非空，全部人工证据和 owner 签名齐全，候选版本差另行关闭。若任一 owner 选择目标 `0`，只生成一份精确回退动作包并另取 Go；若选择 `1`，也必须先通过相应人工门。B1 到此停在文档签收，不改运行态。
