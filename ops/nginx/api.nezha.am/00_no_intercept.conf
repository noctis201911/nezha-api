# 哪吒(窗口④): API 是纯 JSON 服务, 不要用 nginx 本地错误页替换上游(Laravel)的错误响应。
# 全局 http 块设了 fastcgi_intercept_errors on, 配合 vhost 的 error_page 404/502,
# 会把 Laravel 返回的 404/502 JSON(含 CORS 头) 替换成 nginx HTML 错误页并丢掉 CORS,
# 导致前端跨域拿到 'Network Error' 而非真实状态码/错误体。此处 server 级关掉拦截。
fastcgi_intercept_errors off;
