# 哪吒外卖 — 后台操作手册

> 维护约定：每次后台新增功能后，在对应章节补充说明，随功能 commit 一起入库。
> 最新版读取：`node nz.js run "cat /www/wwwroot/api.nezha.am/ADMIN_GUIDE.md"`

---

## 目录
1. [登录与导航](#1-登录与导航)
2. [商家管理](#2-商家管理)
3. [订单管理](#3-订单管理)
4. [风控中心](#4-风控中心)（2026-06-12 新增）
5. [业务设置](#5-业务设置)
6. [后台安全（两步验证 / 凭证自动清除）](#6-后台安全)（2026-06-14 新增）
7. [商家入驻申请](#7-商家入驻申请)（2026-06-14 新增）
8. [本地生活](#8-本地生活)（2026-06-15 新增）
9. [推送通知 (Firebase FCM)](#9-推送通知-firebase-fcm)（2026-06-15 新增）
10. [佣金充值管理](#10-佣金充值管理商家预存佣金)（2026-06-16 新增）
11. [可用性监控（站外 uptime + 服务器自检）](#11-可用性监控站外-uptime--服务器自检-nzwatch)（2026-06-17 新增）
12. [顾客下单需登录（游客结算已关闭）](#12-顾客下单需登录游客结算已关闭2026-06-18)（2026-06-18 新增）
13. [邮件系统（域名发信 / 客诉收信）](#13-邮件系统域名发信--客诉收信2026-06-18-上线)（2026-06-18 新增）

---

## 1. 登录与导航

- 后台地址：`https://api.nezha.am/login/admin`
- 商家端地址：`https://api.nezha.am/login/restaurant`
- 账号：`noctis201911@gmail.com` / 密码：见私密记录（不入库）
- 侧边栏主菜单：订单 / 商家 / 用户 / 财务 / 设置

### 1.1 登录验证码（2026-06-14 改为系统图形码）

后台和商家端登录页现在用**系统自带的图形验证码**：照着图片里的字符输进去即可，看不清点图片右侧的**刷新图标 ↻** 换一张（不区分大小写）。

- 原先用的是 Google reCAPTCHA，但配置里填的是 Google **公开测试 key**（谁都能过），等于没防护，已**关闭**。
- 改回系统图形码后是真实有效的人机校验，后台 + 商家端两个入口都生效。
- **想换回 Google reCAPTCHA**（需自行到 Google 申请**真实** key）：业务设置 → 第三方配置 → reCAPTCHA，填真实 `site_key` / `secret_key`、状态改为启用；若 key 无效或仍是测试 key，会自动回退到图形码。

---

## 2. 商家管理

### 2.1 起送价（最低起订金额）〔2026-06-20 上线〕

- **在哪改**：餐厅编辑页「起送价 / minimum_order」字段（德拉姆 ֏，`0` = 不设门槛）；商家也可在商家端自设。
- **判定口径 = 券后商品金额**：起送价对比的是该单 **商品小计 − 商家活动折扣 − 优惠券**，不含配送费/税/打包费/小费，首单返现不计入。后端 place_order 校验按此口径，不够返回 406。
- **三层口径统一**：① 餐厅页购物车条按「商品 − 商家活动折扣」显示「还差 ֏X 起送」并置灰「去结算」（券要到结算页才选、此处不含券）；② **结算页**再按券后金额（含券）判定，不够时「向商家付款」按钮置灰、显示「未满起送价·还差 ֏X」并提示（CheckoutPage 拦截，不开支付抽屉/不提交，前端 commit 见下）；③ 后端 place_order 仍按券后口径返回 406 兜底防绕过。三层同口径（商品 − 商家折扣 − 券）。
- L2 业务参数，可调。口径改动见 commit c3a64e5。

---

## 3. 订单管理

- 路径：侧边栏 → **订单**
- 可查看所有订单状态、手动确认、退款操作

### 3.1 离线支付核验（现以商家自营为主，后台为后备）〔2026-06-16 起〕

顾客离线支付（支付宝/微信/USDT 转账 + 上传截图）的核验，**现在主要由商家本人在商家后台「订单 → 待确认收款」自行确认**（商家核对自己账户到账后点「确认收款」）。后台这条核验路径**保留为后备**，用于商家不在线、或某笔有争议时由运营代为处理：

- 路径：后台侧边栏 → **订单 → 离线支付核验**（`离线支付待核验/已核验/已拒绝` 列表）。
- 操作：打开订单 → 看顾客付款截图 → 「是，已收到支付」（转 confirmed、通知顾客）或填原因「未收到支付」打回。
- **效果与商家点「确认收款」完全一致**：两条路径共用同一份核验逻辑，确认人（admin / 商家）会记入留痕（`logs` 表）。
- 🔴 **平台不碰钱**：无论谁确认，核验的只是"商家本人账户是否已收到顾客货款"，平台全程不归集、不代收；钱直付商家本人。后台代核验前，最好先与商家确认其账户确已到账。

---

### 3.2 订单收尾（顾客确认收货 / 超时自动兜底）〔2026-06-17 上线〕

平台不自营配送（〔2026-06-19 起配送由商家代叫 Yandex〕 / 顾客自取），配送单**没有骑手点"已送达"**，商家也不知道顾客几点收到。所以一单怎么算"完成"由这两条路径收尾：

1. **顾客「确认收货」（主路径）**：商家把单点到「已出餐 / 待取餐」（handover）后，顾客端出现「确认收货」按钮，顾客收到餐后自己点确认 → 订单转「已送达」。
2. **超时自动兜底**：顾客忘了点时，系统每小时巡检，把 handover **超过 N 小时**（默认 24h）仍无人确认的配送/自取单自动判为「已送达」。

**为什么重要（关系到账目）**：订单走到「已送达」是**佣金结算的触发点**——此时系统对直付单从商家「预存佣金」按佣金率扣一次（`commission_deduction`，扣佣开关 `nezha_deposit_mode_status` 开启时生效）；在此之前不结算。所以**配送单不能永远卡在 handover**，否则佣金永不入账、顾客端永远"未送达"——本功能就是堵这个洞（此前 7 家店均为非自营配送，配送单全部卡 handover）。

**恰好扣一次**：顾客确认、商家标记、自动兜底等所有"送达"路径，都以"该单是否已生成交易流水"为唯一闸门，先到先记、其余跳过，**不会重复扣佣**（已用事务回滚仿真验证：deposit_balance 恰好扣 1 笔、L1-1 平台不碰钱未变）。

**两个可调设置**（`business_settings`，目前经数据库 / 告知开发调整，无独立后台页）：
- `nezha_auto_finalize_handover_status`：自动兜底开关，`1`=开（默认）、`0`=只靠顾客手动确认。
- `nezha_auto_finalize_handover_hours`：自动兜底时长，默认 `24`（小时）。

> ⚠️ **真实影响**：自动兜底默认开启、对真实配送单生效。上线时已确认线上无积压（dry-run 实测无单卡 handover 超 24h），不会误判现有订单。如要关闭或改时长，告知开发调 `business_settings` 对应键。

---
## 4. 风控中心

> 2026-06-12 上线。**2026-06-16 已正式启用（总开关 = 1）**。阈值为德拉姆(֏)量级，与菜价计价单位一致；美元标注按汇率 368.12 折算（仅参考，阈值存绝对德拉姆值、不随汇率波动）。

### 4.1 路径

侧边栏 → **订单** → **风控中心** → 三个子菜单：

| 子菜单 | URL | 说明 |
|---|---|---|
| 审核队列 | `/orders/nezha-risk/queue` | 待人工处理的可疑订单 |
| 风控日志 | `/orders/nezha-risk/logs` | 全部被拦截记录（含已处理） |
| 风控设置 | `/orders/nezha-risk/settings` | 调阈值 / 开关 / 客服联系方式 |

---

### 4.2 当前设置值

| 设置项 | 当前值 | 含义 |
|---|---|---|
| 总开关 | **1（启用 · 2026-06-16）** | 0=关闭风控；1=启用 |
| 单笔上限（法币）·硬拒单线 | **110437 ֏**（≈$300 @368.12，2026-06-17 由 $200 上调） | 超过直接拒单，不可人工放行 |
| 单日累计（法币） | **800000 ֏**（≈$2173 @368.12） | 超过转人工审核队列 |
| 单笔上限（USDT）·硬拒单线 | **110437 ֏**（≈$300 @368.12，2026-06-17 由 $200 上调） | 同法币逻辑 |
| 单日累计（USDT） | **800000 ֏**（≈$2173 @368.12） | 同法币逻辑 |
| 大额特征·审核线 | **368120 ֏**（≈$1000 @368.12） | ≥此额本应转人工审核（不拒单）。⚠️ 但单笔拒单线已降到 **$300**（110437 ֏）< 本线，任何 >$300 的单会先被单笔**直接拒**，故本审核线**当前实际不触发**；现为两段：**≤$300 通过 / >$300 直接拒**。如需中间复审带，把本线降到 $300 以下。 |
| 客服入口 | 平台客服会话（/chat?type=admin&direct=1） | **2026-06-16 已打通**：顾客被拒单/转审核时，结算支付页风控面板显示「联系客服」按钮，点击**直达平台客服会话**（登录顾客直接进 admin 对话；游客先弹登录）。无需填联系方式设置项 |
| 下单频次 | >20 单/24h 或 >8 单/10min → 审核 | 按登录用户统计（顾客下单需登录，见 §12） |
| 放行宽限期 | 60 分钟 | 客服放行后顾客重新下单的免检时间 |

---

### 4.3 审核队列操作

触发"单日累计超 800000 ֏"等审核规则时，订单进入队列，**收款码不显示给顾客**。

操作：

| 按钮 | 含义 |
|---|---|
| **放行** | 本次豁免，顾客可重新下单（宽限期内同用户不再审核） |
| **退款通知** | 标记为退款处理，记入日志 |
| **清退** | 拒绝并标记高风险 |

---

### 4.4 开启风控的前提（三项都满足再改总开关）

> ✅ **2026-06-16 已启用**（总开关 0→1）。启用前现网仅 2 单(1 paid)、真实流量≈0，属"变更安全"（从无风控→有风控）。活体验证：正常小单 pass、大额/超限/USDT 超限分别命中 review/reject、record() 进审核队列且测试记录已清。下列为通用前提，将来如需关停参照（总开关改回 0 即恢复无风控）。

1. 阈值已为德拉姆量级（当前：单笔 110437 ֏ ≈$300 / 单日 800000 ֏ ≈$2173，与菜价单位一致；单笔 2026-06-17 由 $200→$300）
2. 在"风控设置"按当前真实客单价复核阈值是否合理（注：原"整百/整千金额"特征规则因德拉姆下会误报整百菜价，已于 2026-06-14 从风控引擎删除；保留"大额阈值"特征）
3. 进入风控设置，将**总开关从 0 改为 1**，保存

---

### 4.5 风控作用范围

- **全平台统一阈值**：所有餐厅、所有商品共用同一套限额
- **不支持**按单个餐厅或单个商品设置不同阈值（如需定制需后续开发）
- **所有下单用户均为登录态**（顾客下单需登录，见 §12）→ 5 条规则全部检测。

### 4.6 退款控制（原路锁定 + 限额）— 2026-06-14 新增

> 这是**退款合规红线（L1）的执行机制**。和上面的"下单风控"是**两个独立开关**，互不影响。

**4.6.1 设置在哪里**：侧边栏 → **订单** → **风控中心** → **风控设置** → 页面下方「**退款控制**」卡。

> 🟢 **2026-06-15 已正式启用**（总开关=1），并按客单价上调限额。下表为**当前线上值**（原默认值见括注，用于回退参考）。

| 设置项 | 当前值 | 含义 |
|---|---|---|
| **退款护栏总开关** | **1（开 · 2026-06-15 启用）** | 关=退款行为和以前完全一样；开=启用下列所有限制 |
| 单笔退款上限 (֏) | **400000（≈$1087 @汇率368.12）**（原 40000） | 超过转审核（不直接拒） |
| 单商家单日退款累计上限 (֏) | **800000（≈$2174）**（原 120000） | 超过转审核 |
| 单商家单日退款笔数上限 | **10**（原 5） | 超过转审核 |
| 退款窗口（交付后 N 天内可退，0=不限） | **3**（原 7） | 超窗口转**人工审核**（非自动拒；3 天贴合外卖惯例，窗口内丝滑自动退、超期=顾客找人工申诉） |
| USDT 退款链上自动校验 | 1（开） | 校验退款 tx 的金额+地址；关=只锁定+人工核 |
| BscScan / TronGrid API Key | 空 | **留空即可**（用免密钥公共节点）；被限流时再去免费注册填入 |

**开关开启后会强制**：退款金额 ≤ 原订单；USDT 退款目标=由原始付款链上反查的 from 地址（**禁止自由填地址、禁止退第三方**）；法币退还原付款人（政策+凭证）；超限退款转审核队列。**全程留痕（≥5年）**。

**4.6.2 退款留痕 / 审核**：侧边栏 → **风控中心** → **退款留痕/审核**（`/orders/nezha-refund/records`）。

- **看什么**：每笔退款的订单/商家、通道、退款额/原单、**原路锁定地址**、**链上校验状态**、处置状态。
- **USDT 登记退款哈希**：商家原路退款后，把商家给的**退款交易哈希**填进该行的输入框 → 点「链上校验」。系统自动核对：收款地址是否==原付款地址、金额是否≥应退额。
  - 绿色 `verified`=金额+地址都对；红色 `failed`=对不上（**别放行，核对是否退错地址/退第三方**）；黄色 `manual`=链上没查到，需人工核。
- **传凭证**：人民币退款截图 / USDT 辅助截图，可在该行上传。
- **超限审核**：状态为 `pending_admin`（超限）的退款，点「放行」后**回订单详情页重新执行该退款**（限额已对该订单豁免）；或点「拒绝」。

**4.6.3 开启退款护栏的步骤（真实影响操作，先验证）**

> ✅ **已于 2026-06-15 启用**。本轮验证（启用前现网 0 笔 paid 订单/0 退款流量、影响面≈0）：① TRC20 链上校验 bug 已修（见合规 CHANGELOG）；② 活体限额逐档命中正确（500000 命中单笔、900000 命中单笔+单日、交付>3天命中窗口）；③ 链上原路反查 + 校验 BSC/TRC 均 `verified`。下列为通用步骤，将来如需关停/再开参照。

1. 先用**一笔测试订单**走通：在测试单上做一次退款，到「退款留痕」确认记录正确、（USDT）链上校验通过；
2. 确认「退款控制」里的限额对你的客单价合理；
3. 把**退款护栏总开关从 0 改为 1**，保存。
4. ⚠️ 开启属"真实影响"——会立即对所有真实退款生效。拿不准就先保持 0（关），不影响现有退款。

### 4.6.1 直付单取消/退款 → 自动通知商家退款 + 留痕 〔2026-06-17 上线 · F-4〕

当你在「订单管理」**取消**或**退款**一笔**直付订单（offline_payment，顾客直付商家本人账户）**时：
- 系统会**自动**（无视上面的「退款控制」总开关）生成一条 `待商家退款`(pending_merchant_refund) 留痕记录，并**推送通知该商家**去按原路退还顾客。
- 记录含：应退金额（≤原单）、原付款渠道（微信/支付宝/USDT）、USDT 自动反查的**原始付款地址**（退款只能退这个地址）、操作人，留存 ≥5 年。
- **平台全程不碰这笔钱**：钱一直在商家本人账户，平台只做通知与留痕（L1-1）。
- 商家在**商家后台「订单 → 待退款」**退款后点「标记已退款」，记录转 `merchant_refunded`。你可在数据库 `nezha_refund_records` 表核对（USDT 可凭商家回填的退款 tx hash 链上核验）。

> 这条补「资金闭环 QA」的 F-4 缺口：以前后台取消/退款直付单后，平台既不通知商家去退、也没有"商家已退款"的记录。现在两者都有了，且平台依然不经手资金。

### 4.7 制裁名单筛查（OFAC SDN）〔2026-06-16 新增·合规红线 L1-6〕

> 这是**合规红线**：平台不得与受制裁主体交易。顾客用 **USDT** 付款、商家/你在后台「确认收款」时，系统会反查这笔链上转账的**付款来源地址**，比对 **OFAC（美国财政部）SDN 制裁名单** 里的数字货币地址。**命中就自动拒收**——订单不会被确认、不放行出餐，并记一条「制裁命中」到风控日志。

**4.7.1 设置在哪里**：侧边栏 → **订单** → **风控中心** → **风控设置** → 页面「**制裁名单筛查**」卡。
- **总开关 `nezha_sanction_screen_status`**：默认 **1（开）**。关掉=不再筛查 USDT 来源地址（**L1-6 失效，属重大合规风险，关闭前务必想清楚**）。
- **名单来源 URL**：默认 OFAC 官方 SDN.XML 公开端点，一般不用改。
- **来源地址反查不出时**（`nezha_sanction_inconclusive_action`）：默认 **拦截待人工（hold）** = 查不出来源就不放行、转人工复核（更稳健，见 4.7.4）；可改 **放行+留痕（allow）**。
- 卡片底部显示**当前名单地址数**和**上次同步时间/状态**。

**4.7.2 名单怎么更新**：系统每天 **04:30** 自动从 OFAC 公开名单拉取最新地址（无需人工）。如想立即更新，可在服务器运行 `php artisan nezha:sync-sanction-list`。取数或解析失败时**保留上一次的名单不动**（不会因为一次拉取失败就清空、漏筛）。

**4.7.3 命中了怎么看**：侧边栏 → **风控中心** → **风控日志** → 顶部点「**制裁命中**」筛选。每条记录里能看到命中的来源地址、交易哈希、链、订单号。命中单已被系统自动拒收，**请勿出餐**；如确认是误判需放行，请先核实来源后再人工处理。

**4.7.4 查不出来源地址时**（无交易哈希 / 链上服务暂时连不上）：行为由 4.7.1 的「来源地址反查不出时」开关决定，两种都会在风控日志记一条 `sanction_inconclusive` 待人工复核：
- **拦截待人工（hold，默认）**：**不放行出餐**、中止本次确认。订单保持 `pending`（不是拒收，**来源核实或链上恢复后可重新点「确认收款」再筛一次**）。更符合"查不出即不放行"的制裁筛查通用准则。
- **放行 + 留痕（allow）**：照常确认出餐，只记一条待人工事后核。适合你还没配 TronGrid key、Tron 公共端点常限流、又不想误挂合法 USDT 单的场景。
> 提示：**TRC20（波场）** 来源反查走 TronGrid，未配 key 时公共端点易限流→更常"查不出"；填 **TronGrid API Key**（后台「退款控制」那个框，与本筛查共用）即可大幅缓解。**BSC（币安链）** 来源走公共节点 `bsc-dataseed`（免密钥、稳定），一般不会"查不出"；"退款控制"里的 **BscScan Key 当前不影响本筛查**（代码未用它），可不填。若开 hold 后仍频繁误挂，可临时切 allow。

**4.7.5 未决单一键处理（审核队列）〔2026-06-17〕**：开 hold 时被暂挂的单会进 **风控中心 → 审核队列**，命中规则显示 `sanction_inconclusive`。核实步骤：
1. 拿到该笔 USDT 的**交易哈希**（顾客付款凭证里 / 问顾客），到区块浏览器（BSC: bscscan.com / TRC20: tronscan.org）查这笔转账的**付款来源地址**；
2. 在 **风控日志 → 制裁命中** 或对照 OFAC 名单确认该来源地址**不在**制裁名单；
3. 回审核队列，在该行**填写核实结论**（必填，作为合规留痕），点「**核实放行+确认收款**」——系统会**重新链上筛查一次**：若此时反查出命中制裁名单**仍自动拒收**（人工无法放行已命中的单），否则放行并确认收款。
> 即：这个一键按钮只能放行"查不出"的单，**永远不能放行"已命中"的单**；放行操作记 `reviewed_by`/`reviewed_at`/核实备注，长期留痕。

> ⚠️ **真实影响提示**：本功能默认开启，且已载入 OFAC 现行名单，即刻对真实 USDT 确认收款生效。正常顾客几乎不可能在制裁名单上，误伤风险极低；如需暂停，按 4.7.1 把开关置 0。

---

## 5. 业务设置

### 5.1 货币（2026-06-14 以德拉姆 ֏ 计价；顾客端将另显示换算 ¥/$）
计价基准货币是 **AMD（亚美尼亚德拉姆，符号 ֏）**：商家按德拉姆录入菜价，商家后台 / 报表金额按 ֏ 显示。
- 之前没设置，系统默认回退成美元 $；现已设为 AMD（期间试过人民币 ¥，最终定为以德拉姆计价）。
- **在哪里改**：后台 业务设置 → 货币 / Currency（对应业务设置项 `currency`，值 = `AMD`）。
- ⚠️ 改币种**只换显示符号、不换金额数字**：商品价格本身不会自动换算。所以菜价请直接按"德拉姆"录入（一份兰州拉面填 `1500` 就是 ֏1500）。
- ✅ **顾客端 H5 已显示换算后的人民币(¥)与美元($)价格**（2026-06-14 上线）：浏览/菜单/购物车显示"≈¥/≈$"参考价；结算页显示**精确应付 ¥ / USDT**（即顾客实付、商家实收的金额）。
- **汇率在哪维护**：后台 → 餐厅 → 任一餐厅 → **收款信息** tab → 「**顾客端换算汇率（平台全局）**」卡（所有餐厅共用，在哪家餐厅页改都一样）。〔2026-06-16 改为只填 2 个汇率〕
  - 只需手填 **两个**：`1 元人民币 = ? ֏`（`nezha_rate_cny_to_amd`）、`1 美元 = ? ֏`（`nezha_rate_usd_to_amd`）。
  - 第三个「1 美元 = ? 元」是**只读、系统自动算出**（= 美元兑֏ ÷ 人民币兑֏），永远与前两个自洽，**不能手填**（旧版可手填，曾导致三个汇率互相打架，已废除）。改前两个框时它会实时刷新。
  - ⚠️ 平台不经手资金、商家按 ֏ 标价却收 ¥/USDT，**此汇率即事实结算汇率、商家承担汇率波动**，请定期校准别让它过期。
- ✅ **后台数据看板也显示 ¥/$ 参考值**（2026-06-16 上线）：首页「平台佣金 / 总销售额」的德拉姆金额旁，按上述同一汇率加显示 `≈ ¥… / $…` 小字（仅参考；汇率没维护时自动隐藏）。改了上面的汇率，这里同步变。
- ✅ **汇率每天自动对齐市场价**（2026-06-16 上线）：服务器每天凌晨自动从公开汇率源拉市场中间价、写回上面两个汇率框，你平时不用手动维护。
  - **看上次同步**：本页汇率卡「更新汇率」按钮下方有一行「🕒 上次自动对齐：时间」。
  - **护栏（自动保护，钱相关）**：遇到取数失败 / 数值明显异常 / 单次变动超过 8%，会不自动改、保留旧值，那一行变红「⚠️ 每周自动对齐已暂停, 等你确认」并写明原因——这时你来手动核对、必要时手填确认。
  - 仍可随时手填覆盖（第二天又会按市场价对齐；若想长期固定某汇率不让自动改，告诉我关掉自动对齐）。
- ✅ **关联提醒（已处理）**：风控阈值原按美元设计，已改为德拉姆量级（单笔 110437 ֏ ≈$300 / 单日 800000 ֏；退款限额见 4.6 现行值）。下单风控总开关已启用（=1，2026-06-16，见 4.2）；退款控制见 4.6。开启/调整阈值前请按真实客单价复核（见 4.4）。

### 5.2 加料分类（加料 / Add-on 必须先有分类才能建）
商家新建"加料 / 加项"时，"主分类"是必填项；这个分类列表由**总后台统一维护**，商家端只能选、不能新增。
- **在哪里维护**：后台 加料管理 → 加料分类（Addon Category），可新增 / 改名 / 停用。
- 2026-06-14 发现线上一个分类都没建，导致商家"主分类"下拉全空、加料根本建不出来；已先建一个默认分类「通用加料」兜底，商家现在能正常建加料了。
- 你可以把它改名，或按需要再加（如「辣度」「小料」「配料」等）；被停用的分类不会出现在商家下拉里。

### 5.3 附加费名称（2026-06-15 改为「活动期免收平台服务费」）
「附加费 / Additional Charge」是可加在顾客订单上的一笔平台服务费，名字由总后台统一设置（业务设置项 `additional_charge_name`），显示在商家「订单报表」列头（及顾客账单对应行）。
- **当前状态**：开关 `additional_charge_status` = 空（**关**）。代码里只有开关 = 1 时才会真扣这笔费用，所以**现在每单附加费恒为 0、顾客一分钱没多收**（拉新期暂不收）。
- 2026-06-15 应「拉新期暂不收服务费」，把名字从英文默认 `Additional Charge` 改为 **「活动期免收平台服务费」**（旧值：`Additional Charge`）。只改显示名字，不动收费逻辑。
- **在哪里改**：后台 业务设置 → 附加费名称字段（对应 `additional_charge_name`）。
- 🔴 **恢复收费时的红线顺序（务必照做，否则等于欺骗顾客）**：等拉新活动结束、要重新收平台服务费时——
  1. **先**把名字从「活动期免收平台服务费」改回中性名（如「平台服务费」）；
  2. **再**把开关 `additional_charge_status` 开成 1 并设好金额。
  顺序反了（名字仍写「免收」却已开始扣钱）= 顾客账单出现「免收平台服务费 ¥X」，属**造假红线**。

### 5.4 商家收款方式配置（支付宝 / USDT）〔2026-06-19 微信支付已下线〕
顾客在结算页可选 **支付宝 / USDT** 两种线下收款方式，**钱直接付进商家本人账户、平台不碰钱（B 方案）**。每家餐厅的收款码/地址需后台代录。

> 🆕 **2026-06-19 微信支付已下线**：为减少顾客理解摩擦，平台下线微信支付——离线方式「微信」(id=3) 已停用（`status=0`，行保留可逆）；后台「收款信息」页与商家钱包页的**微信收款码上传卡已移除**；顾客结算页/订单页不再出现微信。现仅 **支付宝 + USDT**。下方涉及「微信」的旧说明保留仅作历史参考，已不适用。
- **在哪里录**：后台 → 餐厅 → 选某餐厅 → **收款信息** tab。页面分两张卡（〔2026-06-19 微信收款卡已移除〕）：
  1. **支付宝收款（人民币）**：填「收款人姓名」+ 上传「支付宝收款码图片」（对应字段 `payee_name` / `rmb_qr_image`）。
  2. **USDT 收款**：选网络（TRC20/BSC）+ 填收款地址（`usdt_address` / `usdt_network`）。
- 🆕 **2026-06-15 变更**：此前微信/支付宝共用一张「人民币收款码」，现已拆成两张独立码。顾客结算页选「支付宝」显示支付宝码、选「微信」显示微信码。
  - ⚠️ **微信码留空的后果**：顾客在结算页能看到「微信」方式，但选中后会显示「本店暂未设置微信收款码」——**等于这家店收不了微信**。请给每家店都补上微信码，或提醒商家提供本人微信收款二维码（见商家手册）。
  - 顾客端「微信」收款码下方有「长按二维码保存到相册 → 打开微信扫一扫从相册选图」引导。⚠️ **手机浏览器无法直接深链唤起微信支付**（微信平台限制），所以走的是"保存+扫码"路径，不是一键跳转，这是平台限制不是 bug。
- **顾客可选哪些方式**：结算页离线方式由后台「离线支付方式」列表驱动，当前启用 `支付宝` / `USDT支付` 两项（`微信` id=3 已于 2026-06-19 停用 `status=0`）。如需停用/启用某方式：后台 业务设置 → 离线支付方式，把对应项停用/启用即可（顾客端那一段就随之消失/出现）。
- 🆕 **2026-06-16 修复 + 表单重做**：「离线支付方式」页（业务设置 → 第三方集成 → 线下人工清算参数配置）此前列表页与编辑页会 **500**（视图按旧 StackFood 字段名渲染，与哪吒定制 schema 不符）。已修复，并把「新增/编辑方式」表单重做成单段式：每行定义一个**顾客结算时要填的字段**——字段名 / 占位提示 / 类型（文本 or 文件截图）/ 是否必填，底部可填「支付说明」留底。表单写入结构与顾客端结算严格一致，**编辑保存不会再破坏顾客付款流程**。日常你不需要动这页（支付宝 / USDT 已配好；微信已于 2026-06-19 停用），仅在要加新线下方式时才用。
- 🔴 **合规**：这三类都是**商家本人收款码**，平台不归集、不代收（L1-1）。收款码图片随 restaurants 表整体静态加密存储（L1-7）；属商家常驻展示码，不在「顾客付款凭证 90 天自动清除」范围内。
- 🆕 **2026-06-17 商家端可只读核对**：你在这里录入后，商家可在**自己后台 → 商家配置 → 支付信息** 标签页**只读看到**你给他设置的收款方式（支付宝 / USDT，含收款码 / 姓名 / 地址 / 网络；〔2026-06-19 微信卡已移除〕），并标注「已配置 / 未配置」。商家**不能自行修改**（防误改导致收不到款），只能核对——如商家反馈有误，仍由你在本页改。该页同时取代了 StackFood 原版无意义的「提现方式管理」（B 方案平台不向商家打款，提现腿已拔 L1-5）。

### 5.5 预存佣金管理（保证金账本 / 充值 / 换算小工具）〔2026-06-16 统一德拉姆计价〕
位置同上：后台 → 餐厅 → 选某餐厅 → **收款信息** tab → 「**预存佣金管理**」卡。这是平台向商家收佣的"预存账本"——商家先把佣金预存进来，开启扣佣模式后每单佣金从这里扣；不足阈值会停止接单。
- **计价单位 = 德拉姆 ֏**（与菜价、佣金扣除一致）。〔2026-06-16 起：此前充值环节误当美元处理，已纠正为统一按 ֏；佣金扣除本就按 ֏，未受影响〕
- **怎么充值/调整**：在「充值/调整金额」框填数字（**按德拉姆 ֏ 填**），正数=充值、负数=扣减，可加备注，点「提交」。
  - ⚠️ 这里只记到本系统账本，**实际收款要商家另行线下转账给平台**（平台不经手资金，B 方案）。
- **换算小工具（不提交，仅帮算）**：三个框「֏ = ¥ = $」联动——在任意一个框输入金额，另两个按当前平台汇率自动算出。方便你把"商家转来的人民币"换算成应充的德拉姆。
- **字段含义**：
  - 当前预存佣金余额：账本余额（低于阈值会标红）。
  - 停接单阈值（`nezha_min_deposit_threshold`）：余额低于此值且扣佣模式开启时，该店停止接新单。
  - 扣佣模式（`nezha_deposit_mode_status`）：0=未开启（一阶段，免佣免押，当前线上状态）；1=开启（二阶段，每单扣佣 + 不足停接单）。
- 🔴 **上线前提醒**：演示阶段各店余额是种子数据（非真实充值），上线前用演示回退脚本清掉；开启扣佣模式(=1)前请确认各店真实预存到位、阈值设合理。

### 5.6 首页类目行（金刚区）= 店铺类目，后台自己加减〔2026-06-17 改为「店铺类目」驱动〕
首页搜索框下面那排快捷入口（业内叫"金刚区"），现在显示的是**店铺类目**（一家店属于哪个类目，如"槟榔零食 / 超市百货 / 川渝香菜 / 南方菜系 / 粥面包点"）。**这一行完全由后台数据驱动**——你在后台增 / 删 / 改类目，首页这行**自动跟着变，不用改代码、不用找开发**。
- **在哪里管**：后台左侧菜单「**菜系**」（这就是"店铺类目"，英文 Cuisine）。⚠️ 注意它**不是**「分类管理」——「分类管理」管的是**菜品**分类（招牌主食 / 饺子点心 等，给菜品归类用），「菜系」管的才是**店铺**类目（首页那行 + 商家所属类目）。
- **加一个新类目**：菜系 → 右上「新增」→ 填**名称**（如"烧烤夜宵"）+ 传一张**图标图片**（建议用简单的方形小图 / emoji 风格小图，**别用大照片**，否则首页那排小图标会糊）→ 保存。保存后**立刻**出现在两处：① 首页类目行；② 商家「店铺设置」里可勾选的类目下拉。
- **下架一个类目**：菜系 → 该行「状态」切到关闭（**别直接删**，删了和店的关联会断；关闭可随时再开）。
- **顺序**：首页这行按类目**创建先后**排列（先建的在前），末尾固定加一个「**全部分类**」入口（点进去看全部类目，含以后新增的）。想精确调顺序目前要找开发改一行，按需再提。
- 🔴 **关键**：点一个类目进去，只有**真有店挂在这个类目下**才看得到餐厅，否则显示"该类目下暂无餐厅"。让店挂到类目下有两种方式：① 商家自己在「店铺设置 → 菜系」里勾选（**推荐**，见商家手册对应章节）；② 你在后台「商家管理 → 编辑某商家」里替他勾选。
- **当前（2026-06-17）**：已建 5 个启用类目 = 槟榔零食 / 超市百货 / 川渝香菜 / 南方菜系 / 粥面包点（旧的"湘菜"已停用）。为方便看效果，已先把几家**演示餐厅**挂到对应类目下；**演示数据回退时这些挂接会一并消失**，届时由真实商家自行勾选。

### 5.7 顾客端餐厅列表的四种排序（「综合排序 / 距离 / 销量 / 好评率」含义）〔2026-06-16 新增〕
顾客在首页「附近推荐」上方那行排序，决定餐厅列表先后。四种排法下**营业中的店永远排在关店的店前面**，然后：
- **综合排序（默认）**：哪吒的"推荐"排法，对标各大外卖平台的综合排序——按一个「综合分」从高到低。综合分 = **距离(越近越高) 0.45 + 评分(越高越高) 0.30 + 销量(越多越高) 0.25** 三项加权。所以又近、又好评、又有销量的店靠前；很近但评分差的店不会只因为"近"就霸榜。
- **距离**：按离顾客（按其手机定位；未授权定位时按埃里温市中心）从近到远。
- **销量**：按历史总订单量从多到少。
- **好评率**：按顾客评价平均星级从高到低（与餐厅卡片上的星级同口径，无评价的店沉底）。
- **想调综合排序的权重**（比如让"销量"更重要）：三个权重写在后端代码的综合分公式里（不是后台开关），要调跟开发说一声、改一行即可。当前商家普遍还没订单，综合排序主要由"距离+评分"驱动；等真实订单累积后"销量"才会明显发挥作用。

### 5.8 首页「人气热销」橱窗 + 用『推荐』开关人工置顶〔2026-06-18 新增〕
顾客端首页「新人专享」优惠券条**下面**那一排横向小卡片（标题「人气热销 · 大家都在点」）= 自动展示**真实销量最高的菜品**（图 + 菜名 + 价格 + 月售），全平台跨店、永远有数据、不用维护。原来那张又大又难看的促销横幅已撤掉（横幅不适合展示菜品图）。
- **默认排序**：按菜品真实销量从高到低，自动更新，无需任何操作。
- **人工置顶（想主推某几道菜时用）**：后台「商品 / 菜品列表」页（左侧菜单进商品列表），每行最右「状态」开关**旁边新增了一个「推荐」开关**。打开它 → 这道菜被**排到首页「人气热销」橱窗最前面**，并带红色「推荐」角标；关掉恢复纯按销量排。
- 一道菜要进首页橱窗，前提是它本身有一定销量（在"人气候选池"里）。对从没卖过的极冷门菜，打开"推荐"也不一定挤得进橱窗——这是有意为之，避免首页出现没人买过的菜。
- ⚠️ **商家端也有同一个"推荐"开关**（商家自己的商品列表里本来就有，效果相同）。所以同一道菜的"推荐"状态你和商家都能改，以最后一次操作为准。
- **不用改代码**：纯后台一键勾选。

---

### 5.9 餐厅页「商家公告条」+「本店热销榜」〔2026-06-20 新增〕
顾客进任一餐厅页，店头信息卡下方会依次显示（有数据才显示，空则不占位）：
- **商家公告条**（暖黄色）：内容来自该餐厅的 `announcement`（开关）+ `announcement_message`（文本），**由商家自己在商家后台「店铺信息」页填**（公告开关打开 + 填内容）。后台这边一般不用管；如商家求助，可在「商家管理 → 编辑餐厅」协助开关/填写。开关关或内容空 → 不显示（原 TopBanner 里那条绿色滚动公告已并入此暖黄条，避免重复）。
- **本店热销榜 TOP1/2/3**（金/银/铜牌横滑卡）：自动取该店**近30天真实销量**前 3 的在售菜品，与首页「人气热销」「商品卡月售」**同一份销量数据**（后端 `product_data_formatting` 预聚合、零额外查询）。**全自动、无需维护、不可人工改排名**（防刷榜）；某店近30天无任何销量 → 整个榜不显示（新店或真实店未起量时即如此，不会出现假榜单）。

## 6. 后台安全

### 6.1 两步验证 (2FA) — 强烈建议开启
给后台登录在密码之外加一道手机认证器动态码，密码即便泄露别人也进不来。

**怎么开启：**
1. 登录后台后，浏览器打开：`https://api.nezha.am/admin/two-factor/setup`
2. 用手机装一个认证器 App（Google Authenticator / 微软 Authenticator / 1Password 均可）。
3. 用 App 扫页面上的二维码（扫不了就手动输下面那串密钥，类型选"基于时间/TOTP"）。
4. 把 App 上显示的 6 位码填进"第二步"输入框，点"确认并启用"。
5. **页面会显示 8 个一次性恢复码——立刻抄下来存好（密码管理器/纸条）。这是认证器丢了时的备用钥匙，关掉页面就再也看不到。**

**以后登录：** 输完邮箱密码后，会多一步要你输认证器上的 6 位码。

**认证器丢了 / 被锁在外面怎么办（三选一）：**
- 用一个**一次性恢复码**代替 6 位码登录（每个只能用一次）。
- 都没有了：让懂技术的人通过服务器 SSH 执行一条命令关闭你的 2FA：
  `cd /www/wwwroot/api.nezha.am && php artisan nezha:2fa-disable 你的后台邮箱`
  执行后即可仅用密码登录，再重新设置 2FA。

**关闭 2FA：** 在同一设置页输入当前登录密码确认即可关闭。

> 说明：2FA 默认是关的，开不开由你决定；不开的话登录跟以前完全一样。

### 6.2 顾客付款凭证自动清除（隐私合规）
顾客上传的付款凭证（转账截图/付款信息）属个人数据，系统会**每天自动清除超过保留期的旧凭证**（只抹凭证内容+截图，保留订单/交易记录供对账），满足"个人数据到期删除"的合规要求。
- **保留天数**：默认 **90 天**。如需调整，改业务设置项 `nezha_payment_proof_retention_days`（数字，单位天）。
- 链上/交易/审计记录按合规另行留存 ≥5 年，不在自动删除范围内。

### 6.3 数据库加密 + 每日加密备份（已自动运行，无需后台操作）
数据库里所有顾客信息（姓名/电话/邮箱/地址/坐标/证件号等）现在在硬盘上是**加密存储**的，并且**每天凌晨自动做一份加密备份**。这两件事都在服务器后台自动跑，**后台界面里没有按钮要点**。

**🔑 你唯一要做的一件事：把"备份钥匙"另存一份到安全的地方。**
- 备份钥匙就是服务器上这个文件：`/root/.nezha/backup.key`
- **这把钥匙丢了，那些加密备份就永远打不开了。** 请让懂技术的人把它的内容复制出来，存到你的密码管理器 / U盘 / 离线安全处各一份。
- （数据库本身的加密钥匙在 `/www/server/mysql-keyring/`，由服务器自动管理，正常不用动；换服务器时才需要它，已有备份。）
- 〔2026-06-17 容灾QA〕已把 `backup.key` 与 `keyring.master.backup` 两把钥匙打包下载到站长本机一份；请再各复制一份到密码管理器 / U盘，别和服务器放在一起。

**备份在哪、留多久：**
- 位置：`/www/backup/database/nezha-enc/`，文件名形如 `sql_api_nezha_am-日期.sql.gz.enc`
- 频率：每天 04:17 自动生成一份，**保留最近 14 份**，旧的自动删。
- 备份是加密的，所以就算这些备份文件被人拷走，没有上面那把钥匙也打不开。
- 〔2026-06-17 容灾QA 新增〕现在每天还多备份一份 `nezha-files-日期.tar.gz.enc`：里面是**上传的图片、支付凭证、和 `.env` 配置**（这些以前没进备份，丢了就没了）。
- ⚠️ **当前所有备份都在本机这一台：能防误删，防不住整台机器/硬盘坏掉或被勒索。** 真正的"异地容灾"（把备份推到云存储或另一台机）还没做，等有真实订单量后建议补上。

**怎么用备份恢复数据（出事时给懂技术的人看）：**
- 完整步骤写在服务器文件 `/root/nezha-backup/README-RESTORE.txt` 里。
- 简版：用钥匙解密 → 解压 → 导回数据库：
  `openssl enc -d -aes-256-cbc -pbkdf2 -pass file:/root/.nezha/backup.key -in 备份文件 | gunzip > /root/restore.sql`，确认无误后 `mysql` 导入。

**这套加密防什么、不防什么（实话）：** 防的是**硬盘或备份文件被偷/被拷走**；**防不了**服务器本身被黑（程序或 root 被攻破时数据仍是明文可读）。所以它是合规要求的"静态加密"那一层，不能替代日常的账号、密钥、服务器安全。
- 手动预演（看会删什么、不真删）：`php artisan nezha:purge-payment-proofs --dry-run`

---

## 7. 商家入驻申请

顾客端 H5「我的 → 商家入驻」填写的入驻意向，会进到这里。这是 **MVP 的人工入驻流程**：商家先留资料，你人工联系、审核、配预存佣金和自收款账户，再到「商家管理」正式开店。**平台不在这一步碰任何钱。**

### 7.1 在哪里看
- 路径：后台侧边栏 → **商家入驻申请**（在"帮助与支持"那一组下面）
- 每提交一条，系统会**自动发一封邮件到 `noctis201914@gmail.com`**，所以你不用一直盯后台；邮件里有店名、联系人、电话、微信、地址、品类、备注。

### 7.2 怎么处理一条申请
1. 列表里点 **查看**（眼睛图标）看详情。新申请会标红色「新」字。
2. 右侧「跟进状态」选一个再点**保存状态**：
   - **待跟进**：刚提交、还没联系（默认）
   - **跟进中**：已经在联系/谈了
   - **已完成**：已正式开店或确定合作
   - **无效**：重复、乱填、不合作
3. 确认是垃圾信息可点**删除**（红色垃圾桶）。

### 7.3 注意
- 这里只是**线索登记**，不会自动创建商家账号；正式开店仍在「商家管理」里手动建。
- 电话是顾客 PII，列表里直接显示方便你联系；数据库整体已加密存储（见 6.3）。
- **自动清理（PII 到期删）**：你把一条线索标成「已完成」或「无效」后，系统会在 **90 天**后自动抹掉它的联系人/电话/微信/地址/备注（**店名、品类、状态保留**，方便日后回看“曾有这条线索、结果如何”）。**「待跟进」「跟进中」的线索永远不会被清**，不用担心还没联系的商家联系方式被删。天数想改可改数据库 `business_settings` 的 `merchant_leads_retention_days`（默认 90）。

---

## 8. 本地生活

顾客端 H5「本地生活」频道（租房 / 招聘 / 二手 / 免费 / 服务）的内容由**你在后台手动录入**。这是 **MVP 的信息墙**：只展示信息、撮合双方私下联系，**平台不碰任何钱、不收押金、不做担保交易**。

### 8.1 在哪里看 / 录入
- 路径：后台侧边栏 → **本地生活**（定位图钉图标，在「商家入驻申请」旁边）
- 列表页右上角 **+ 新建帖子**；可按标题 / 分类搜索。
- 每条帖子有状态徽章：**草稿**（灰，不公开）/ **已发布**（绿，顾客端可见）/ **已下线**（黑，已撤回）/ **待审核**（黄，用户发帖待你审）/ **已驳回**（红，未通过）。
- 列表上方有**状态筛选**（全部 / 待审核 / 已发布 / 已驳回 / 草稿 / 已下线）；标题旁显示**来源**徽章：**平台**（你录的）/ **用户**（顾客自助发的）。有待审核帖时标题栏出现「待审核 N」红色提醒。

### 8.1b 类目管理（金刚区那一排，可自己增删改）
顾客端「本地生活」首页最上方那一排彩色格子（金刚区）= **类目**，现在**完全由你在后台管理**，加新类目无需找我改代码。

- 路径：后台侧边栏 → **本地生活类目**（在「本地生活」下面）。
- 列表显示每个类目的图标、名称、归属频道、在用帖子数、是否敏感、启用状态。
- **新建类目**：右上角 **+ 新建类目**，填：
  - **类目名**（如 美容美发）——显示在格子下方，也是帖子的分类标签。
  - **归属频道**（服务型选「服务」，租房选「租房」，二手选「二手」）。
  - **图标 Emoji**（粘贴一个 emoji，如 💇）、**图标底色**（浅色十六进制如 #FCE9F0）、**排序**（数字小靠前）。右侧有实时预览。
  - **敏感类目**勾选框：移民 / 签证 / 按摩 等勾上后，该类目的用户发帖会在帖子列表**标红「敏感」徽章**提醒你重点审核，并参与更严格的违禁词扫描。
  - **启用**勾选框：启用才显示在顾客端金刚区；停用只是隐藏，**不删数据、不影响已发布帖子**。
- **编辑 / 删除 / 启用停用**：列表每行右侧操作。改名会自动同步更新已发帖的分类文字；删除类目不会删帖（帖子仍保留分类文字，只是金刚区不再显示该类目）。
- 改完顾客端**自动生效**（前端按启用类目动态渲染，无需重新构建）。
- **类目性质（ugc / merchant）**〔2026-06-18 新增〕：每个类目要选「**个人发帖**」还是「**商家服务**」。
  - **个人发帖**（租房合租 / 二手闲置）：顾客点这个格子，在下方**信息流**里筛选对应的个人帖（C2C，谁都能发）。
  - **商家服务**（移民 / 签证 / 美容美发 / 按摩 / 包车出行 / 本地旅游）：顾客点这个格子，**跳转到该类目的「商家列表」**（见 8.1c），不进信息流。
  - 加新类目时按它是「个人发帖找信息」还是「找商家服务」来选。选错只影响点击跳哪，可随时改。
- 当前已建 8 个：租房合租(个人) / 二手闲置(个人) / 包车出行(商家) / 本地旅游(商家) / 移民(商家) / 签证(商家) / 美容美发(商家) / 按摩(商家)（移民·签证·按摩为敏感类目）。

### 8.1c 商家管理（移民/签证/美容/按摩等服务型商户）〔2026-06-18 新增〕
「商家服务」型类目（见 8.1b 的类目性质）点进去是一个**商家列表**，每个商家有自己的**店铺页**（评分 / 营业时间 / 地址导航 / 介绍 / 服务），对标饭团外卖。商家由**你在后台录入**。

> 🔴 **合规**：商家页是**纯信息展示**，平台**不参与交易、不代收款、不接预订下单**。和本地生活信息墙一样，只撮合、不碰钱。移民/签证/按摩为敏感类目，录入时请核实资质。

- 路径：后台侧边栏 → **本地生活商家**（在「本地生活类目」下面，店铺图标）。
- 列表显示每个商家的 Logo、名称、类目、评分、区域、是否敏感、上线状态；顶部可按类目筛选 / 搜名称。
- **新建商家**：右上角 **+ 新建商家**，填：
  - **商家名**（必填）、**所属类目**（必填，下拉只列「商家服务」型类目）、**商家介绍**。
  - **平台星级**（0-5，你手填的展示评分）、**Google 评分**（选填）+ Google 评分链接（选填）。
  - **营业星期**（勾周几营业）+ **每日开始/结束时间** + 营业时间补充文字。顾客端会按埃里温时区显示「营业中 / 休息中 + 今日时段」。
  - **区域**（列表「全部区域」筛选用，如 市中心/北区）、**详细地址**、**经纬度**（填了顾客点「导航」才跳地图）。
  - **服务项**：每行一项，格式「标题 | 描述 | 价格文字」。价格只是展示文字，平台不收款。
  - **Logo / 微信二维码 / 相册**（图片，选填；没传 Logo 会用商家名首字兜底，不留空）。
  - **到店优惠**勾选 + 优惠文字（列表显示「到店优惠」标签，仅展示**不做核销**）。
  - **排序**（小靠前）、**上线**勾选（勾选后顾客才看得到）。
- **编辑 / 删除 / 上线隐藏**：列表每行右侧操作。改完顾客端自动生效（无需重新构建）。
- 当前有 7 个**演示商家**（雅顺移民 / Eric Wang immigration / 环球签证服务 / 小美沙龙 / 康宁中医理疗 / 阿明包车 / 高加索深度游），正式上线前会清掉（见演示数据清单）。
- ⚠️ **二期再做**（当前未开放）：商家「写评价 / 发笔记」（顾客评价，店铺页底部按钮点了提示「即将开放」）、商家自助注册入驻、到店优惠核销。

### 8.2 怎么发一条帖子
1. 点 **+ 新建帖子**，填表单：
   - **标题**（必填）、**分类**（必填，下拉选项来自 **8.1b 类目管理** 里启用中的类目；你在那里加的新类目会自动出现在这个下拉）
   - **Tab**（必填，对应顾客端筛选栏：推荐 / 租房 / 招聘 / 二手 / 免费 / 服务）
   - 描述、封面 Emoji（如 🏠）、封面颜色、价格（AMD ֏）、价格后缀（/月、面议…）、面积标注、地点、想要数
   - **免费** / **急招** 勾选框（勾上分别显示"免费"价格、"急"标签）
   - **联系方式**（电话 / 微信等）—— ⚠️ **PII，只有登录的顾客才看得到**，游客在详情页看不到；列表页对任何人都不显示。
   - **过期时间**（留空=长期有效，将来到期会用于自动清除联系方式）
   - **状态**：想立刻让顾客看到就选 **已发布**；没填好就留 **草稿**（默认草稿，不会误公开）。
2. 保存后回到列表。列表里可：**编辑** / **发布⇄转草稿**切换（一键按钮）/ **删除**。

### 8.3 注意
- **草稿默认不公开**：新建若不改状态，默认是草稿，顾客端看不到，不会误发。
- **想要数是你手动填的展示数字**，不是真实点击量，别当真实数据看。
- **联系方式是 PII**：列表接口永不返回、详情只给登录顾客；数据库整体已加密（见 6.3）。如填了电话/微信，注意这是真人信息，删帖即一并删除。
- 顾客端「本地生活」页面要显示这些帖子，需前端接入列表接口（前端窗口的活）；后台这边录好已发布帖即可。

### 8.4 用户自助发帖（UGC）与审核 〔2026-06-15 新增〕
除了你手动录入，顾客也可以在 H5「本地生活」自己发帖。**这个功能默认是关闭的**，需要你手动开。

**① 开关：是否允许顾客发帖**
- 位置：本地生活列表页**最上方**有一块「用户发帖入口」卡片，显示当前状态（**已开放 / 未开放**）和一个开关按钮。
- **默认未开放**：顾客端「本地生活」右下角的「发帖」按钮点了只会提示"即将开放"，不会真打开发帖表单。
- 你觉得准备好了（有人手能审核），点 **开放发帖入口**；要暂停就点 **关闭发帖入口**。
- ⚠️ 这是**真实影响开关**：一开，登录顾客就能提交真实信息（含他们的真实联系方式）。开之前请确认你能及时审核。

**② 顾客发的帖默认进「待审核」，不会自动公开**
- 顾客提交后，帖子状态是 **待审核**（黄），顾客端列表里**看不到**，只有他自己在「我的发布」能看到"审核中"。
- 你在列表用状态筛选点 **待审核**，逐条处理：
  - **通过**（绿按钮）→ 帖子变 **已发布**，立刻出现在顾客端本地生活列表。
  - **驳回**（黄按钮）→ 展开一个输入框填**驳回理由**（可留空，默认套用通用理由）→ 确认 → 帖子变 **已驳回**；发帖用户在「我的发布」能看到"已驳回"和你写的理由。
- 平台帖（你录的）不受审核流程影响，仍用「发布⇄转草稿」切换。

**③ 顾客发帖里的图片和联系方式**
- 顾客可上传最多 6 张图片（自动转 webp 存储），列表卡封面优先用第一张图。
- 顾客填的**联系方式仍是 PII**：列表永不显示、详情只给登录顾客；30 天后由每日任务 `nezha:purge-locallife-pii` 自动清除联系方式+删图（保留帖子行供审计）。详见 data-protection.md。
- 反垃圾：每个顾客**每天发帖上限**（默认 5 条，超出当天拒绝）。更多护栏（违禁词过滤 / 举报 / 限流 / 免责声明）见 **8.5**。

### 8.6 AI 预审工作法（你 + Claude 一起审，省人工）〔2026-06-16 新增〕

你想"让 AI 帮你看帖、看不准的才人工"。这件事**不用改任何代码、不用加状态**——现成的「待审核」状态就够用，做法是约定一套流程：

**怎么用（你开了 UGC 开关、有顾客发帖之后）**
1. 你在 Claude Code 窗口里说一句：**"审一下待审帖"**。
2. Claude 读出所有「待审核」（status=3）的帖子，**逐条**给你：内容摘要 + 判定（🟢正常 / 🔴违禁 / 🟡拿不准）+ 理由。
3. 处置规则（已和你约定）：
   - 🟢 **明显正常** → Claude 直接改成「已发布」，立即对顾客可见。
   - 🔴 **明显违禁** → Claude 直接改成「已驳回」，驳回理由写明 **"【AI预审】…"** 方便区分。
   - 🟡 **拿不准** → **留在「待审核」不动**，等你人工看。
4. 于是你**只需要在后台筛「待审核」这一档**——里面剩下的，天然就只有 AI 拿不准的帖子。你点通过/驳回即可。

**为什么不另加一个"待人工复核"状态**：「待审核」本来就不向顾客展示，AI 处理过的帖子都已离开这档，留下的自然只剩拿不准的，效果完全一样，还少一个状态要维护、少一处可能漏判（前端某查询忘了排除新状态就会把违禁帖漏给顾客）。

**留痕**：AI 驳回时理由带 "【AI预审】" 前缀；通过的帖 Claude 会在审核日志里记一行（帖ID/时间/判定/理由），出问题能查"这条是 AI 放的还是你放的"。

**⚠️ 重要边界（合规）**
- AI 只做**预审分级**，**终审与平台责任仍在你**。监管追责追的是平台主体，不是 AI。AI 会被绕过、会误判，所以 🟡 一律退给人工、不替你冒险。
- 这套**走你本人在 Claude Code 的交互额度**，不是服务器后台 7×24 自动调用。后台全自动值守需要单独接 Claude API（按量付费），量大了再考虑，现在没必要。
- 现在 `locallife_ugc_enabled` **仍是关闭的**（律师审校前不开），待审队列是空的，这套流程是"备好等开闸"的状态。


### 8.5 发帖护栏：违禁词 / 举报 / 限流 / 免责声明 〔2026-06-15 新增·窗口⑤〕
开放顾客发帖前补齐的四道护栏，绝大多数自动生效、无需你天天盯。

> 🛠 **违禁词、免责文案、规则全文、反滥用阈值现在都能在后台自助改了**：本地生活列表页右上角 → **「护栏与文案设置」**（路径 `/admin/local-life/settings`）。不用再改数据库、不用找我。下面各项标注了在该页哪一块改。

**① 违禁词自动过滤（自动挡一道）**
- 顾客发帖（以及你在后台「新建/编辑帖子」）时，系统会扫描 **标题 + 描述 + 联系方式**，命中违禁词**直接拒绝**（提示"内容含违规词"），不会进待审核队列。
- 词库在**「护栏与文案设置」页 →「违禁词过滤」**框里改（一行一个或逗号分隔；留空=用系统内置默认词库）。已预置一份覆盖涉黄/赌博/诈骗洗钱/毒品违禁/外站强引流的种子词；为避免误伤正常帖，**只填确需拦截的实体词**，别填"租""出""微信"这类高频字。
- 命中只提示"含违规词"、**不告诉对方是哪个词**（防止被试探着绕过）。

**② 顾客举报（用户帮你看）**
- 顾客在帖子详情页有「举报这条信息」入口（需登录）。举报理由是固定 6 项（虚假诈骗 / 涉黄赌博违法 / 骚扰辱骂 / 重复垃圾广告 / 冒用侵犯隐私 / 其他），选"其他"必须填说明。
- 你在哪看：本地生活**列表页**，被举报的帖子标题旁会出现红色「举报 N」徽章，列表上方也有「待处理举报 N」总提醒。点徽章进**举报处理页**，能看到每条举报的理由、说明、时间、举报人ID。
- 两个动作：**下线该帖**（帖子撤下顾客端不再显示，并把该帖所有待处理举报标记为已处理）/ **驳回**（认为举报不成立，帖子保留，单条举报标记已驳回）。
- 防刷：同一顾客对同一帖重复举报会被合并；每人每天举报数有上限（默认 20）。

**③ 反滥用限流（防刷屏）**
- 同一顾客两次发帖间隔过短（默认 60 秒内）会被拦；24 小时内发**完全相同标题**也会被拦；加上 8.4 的每天 5 条上限。三个阈值（每日发帖上限 / 每日举报上限 / 最小发帖间隔秒）在**「护栏与文案设置」页 →「反滥用阈值」**改，留空保持当前值，最小间隔填 0 = 关闭该限制。

**④ 平台免责声明（法律免责）**
- 顾客端本地生活列表/详情底部常驻一条灰色小字："本地生活为信息展示与撮合平台…哪吒不对信息真实性及交易结果负责…"，点《本地生活信息发布规则》看全文。
- 顾客发帖必须勾选"我已阅读并同意《本地生活信息发布规则》"才能提交（不勾按钮是灰的）。
- 短提示和规则全文在**「护栏与文案设置」页 →「免责短提示」/「《本地生活信息发布规则》全文」**改。⚠️ 这是面向法律的免责文本，改动须慎重、重大修改建议先请律师过目；**规则全文的正本同时存放于后端 `docs/legal/local-life-terms.md`，在后台改了请同步那份正本**，保持一致。该页现已分「中文」与「English / Русский / Հայերեն 多语言版本（选填）」两块：**多语言框留空 = 顾客看到中文**；按顾客界面语言自动选对应版本。⚠️ **俄/英/亚语属法律译文，未经律师/母语法律译者审校前请勿填**（亚美尼亚语尤其须母语法律译者审校）；填入前请同步 `docs/legal/local-life-terms.md` 正本。注：H5 目前仅「中文/English」可切换，俄/亚语暂无切换入口、填了也还看不到。

**⑤ 《个人数据处理通知与同意》（PII · 默认关 · 慎开）〔2026-06-15 新增·骨架〕**
- 「护栏与文案设置」页最下方有一张**红框卡片**「《个人数据处理通知与同意》（PII · 慎用）」。这是给将来合规上线用的**预留开关**，**现在默认是关的，顾客发帖时不会出现第二个勾选项，流程和今天完全一样**。
- 🔴 **千万别随手打开**：勾上「开启」开关 **且** 通知正文非空时，顾客发帖会被要求**额外勾选同意《个人数据处理通知》**——这等于开始**采集个人数据处理同意**，属对外法律行为。开启前必须先满足两个硬条件：① 已有**亚美尼亚注册的公司实体**，并在正文里写明「数据控制者＝公司全称＋注册地址」；② 文案经**当地律师审校**（亚语须母语法律译者）。两者没齐**请保持关闭、正文留空**。
- 卡片里有中文 + 英/俄/亚语四个正文框（留空回退中文）。即使勾了开关，只要正文是空的也**不会**真的强制勾选（双保险）。
- 有疑问别自己开——找做技术的确认。当前状态：**未满足前置（暂无注册主体）→ 保持默认关**。

**做完这四道护栏后**：本地生活在对外开放发帖前已具备"自动挡（违禁词）+ 用户帮看（举报）+ 防刷（限流）+ 法律免责（声明）"。你确认 OK 后，再用 8.4① 的「开放发帖入口」开关对外开放（默认仍是关的）。

---

## 9. 推送通知 (Firebase FCM)

顾客端「出餐提醒」等推送，靠 Google Firebase Cloud Messaging (FCM) 发送。**2026-06-15 已配置并真机验证通过**（手机能收到通知）。下面记录配置位置和**踩过的坑**，以后维护别重蹈覆辙。

### 9.1 后台在哪填
- 路径：后台 →「第三方集成与配置」→「**Firebase 推送设置**」（路由 `business-settings/fcm-index`）
- 那页**只有最上面那个大文本框「Service File Content」要填**——粘贴**服务账号 JSON 全文**。
- ⚠️ 下面那排小框（API密钥 / FCM项目ID / 授权域名 / 应用ID 等）**全部留空**。我们顾客端走前端自己的 `.env`，不读后台这些字段，填了也没用、反而易混。

### 9.2 服务账号 JSON 从哪来
1. [Firebase 控制台](https://console.firebase.google.com) → 项目 **nezha-dummy** → ⚙️ 项目设置 →「服务账号」标签 →「**生成新的私钥**」→ 下载一个 JSON 文件。
2. 用记事本打开，全选复制，粘进 9.1 那个大文本框 →「提交」。
3. 🔴 这个 JSON 含私钥 = **密钥**，只贴这一个地方，**别贴聊天 / 别进 git / 别外发**。

### 9.3 🔴 维护红线（都是 2026-06-15 配置时真踩过的坑）
1. **绝不删除服务账号 `firebase-adminsdk-fbsvc@nezha-dummy...`**。在 Google Cloud「服务账号」页删除时，"删钥匙"和"删账号"挨得很近——删了账号会导致发送报 `account not found`，整个推送瘫痪。只在必要时删**单个钥匙行**，永不删账号。
2. 服务账号必须有 **「Firebase Cloud Messaging API Admin」** 这个 IAM 角色，否则发送报 `403 cloudmessaging.messages.create denied`。若账号被重建过，需在 Google Cloud → IAM 重新授予此角色。
3. 换/转钥匙时：先生成新钥匙→上传后台→**确认能发**，再删旧钥匙。别先删后换。
4. 前端 `.env.production` 里有 6 个 `NEXT_PUBLIC_FIREBASE_*` + `NEXT_PUBLIC_FIREBASE_VAPID_KEY`（gitignored，服务器有 `.bak` 备份）。SW 文件 `public/firebase-messaging-sw.js` 里也硬编码了同一套配置，**两处的 appId 要一致**。

### 9.4 顾客侧前提（顾客自己操作，非后台）
- **iPhone 必须"添加到主屏幕"、从主屏幕图标打开**，才能收推送（iOS 死规矩；普通 Safari 标签页不行，需 iOS 16.4+）。安卓 Chrome 无此限制。
- 顾客需在站内**授权通知权限**，前端才会把设备 token 上报到后端（存 `users.cm_firebase_token`）。

### 9.5 怎么自检推送是否正常
- 商家把某单推进状态（接单/出餐），已授权的顾客手机应收到通知。
- 后端发送逻辑在 `app/CentralLogics/Helpers.php::sendNotificationToHttp`（FCM HTTP v1，OAuth2 服务账号鉴权）。

### 9.6 推送文案（notification_messages 表 / 2026-06-17 修复并补齐）

顾客端每次订单状态变化（下单 / 接单 / 备餐 / **出餐** / 完成 / 取消 / 离线付款确认或拒收 / 退款）要推给顾客的**文字内容**，存在数据库 `notification_messages` 表（`user_type='user'`），多语言译文放 `translations` 表。**这跟 9.1～9.5 的 FCM「发送通道」是两回事**：通道是「怎么发」，本表是「发什么字」。两者缺一不可。

- 🔴 **此表为空 = 所有状态推送静默失效**（2026-06-17 修复）：表曾被导库 / 迁移洗空过一次，导致 `Helpers::order_status_update_message()` 取不到文案返回 null，`sentUserNotification()` 里的 `if ($value && $user_fcm)` 不成立 → **FCM 推送和站内信都不发**。商家点「出餐」顾客端零提醒，根因就在这里（不是通道坏了）。
- 🟢 **站内信不再依赖推送授权 + 标题本地化（2026-06-17 二次修复, commit 39ae06a）**：原 `sentUserNotification()` 把「FCM 推送」和「站内信入库」一起关在 `if ($value && $user_fcm)` 里。但 **iOS Safari/Chrome 不支持 Web Push**，iOS 顾客拿不到 FCM token，于是出餐后连消息中心都没有记录、零提醒。现已拆开：**FCM 推送只在有 token 时发；站内信对所有登录顾客一律入库**（游客 `is_guest` 不入），保证 iOS / 没开推送权限的顾客也能在 App「消息中心」看到出餐等提醒。同时修了**推送标题**——原 `translate('Order_Notification')` 无中文译文恒回退英文「Order Notification」并烘焙进推送+站内信，现按顾客 `current_language_key` 直接给「订单通知」/「退款被拒」（前台 toast 与后台 SW 都从 `data.title` 取，一处修两端生效）。
- **怎么补 / 改文案（两条路）**：
  - ① **后台页**（推荐用于日常改文案）：后台 →「通知」→「Push Notification Messages」（路由 `notification-messages`），按 key 填，含语言 tab，保存即 `firstOrNew` 落库。
  - ② **一键补默认值**（表被洗空时重建，**推荐用于灾后恢复**）：`php artisan db:seed --class=NezhaUserNotificationMessageSeeder`（幂等，可反复跑，不会重复插）。seeder 在 `database/seeders/`，含全部订单状态的中英文默认文案。
- 🔴 **locale 关键坑（务必知道）**：推送查找文案用的是**顾客的 `current_language_key`**，本项目真实顾客是 `en` 与 `zh-CN`（**不是**后台系统语言码 `zh`）。所以：基础 `message` 列存**中文兜底**（覆盖 zh-CN 顾客及任何没对上的语言），译文按 `en` / `zh-CN` 存。**后台语言 tab 只有 `zh`，单靠它填的译文 zh-CN 顾客匹配不到**，会落回中文兜底列（结果仍是中文，但要明白这层机制——别奇怪「我在 zh tab 改了，zh-CN 顾客怎么没变」）。要改 zh-CN 顾客看到的中文，请改**基础/default 那一栏**（= `message` 列）或重跑 seeder。
- **出餐（handover）文案约定**：推送里**不塞取餐号**——取餐号顾客在 App「消息中心」查（出餐后 track 接口才返回，见 commit bdc1b02）。推送只负责「**提醒去看**」，文案如「您在「{restaurantName}」的订单已出餐，请在 App 查看取餐号并尽快前往取餐。」
- **2026-06-17 已补的 key**（`user_type='user'`，`status=1`，各含 `zh` / `zh-CN` / `en`）：`order_pending_message`、`order_confirmation_msg`、`order_processing_message`、`order_handover_message`（出餐）、`out_for_delivery_message`、`order_delivered_message`、`delivery_boy_delivered_message`、`delivery_boy_assign_message`、`order_cancled_message`、`order_refunded_message`、`refund_request_canceled`、`offline_order_accept_message`、`offline_order_deny_message`。
- **文案可用占位符**（`Helpers::text_variable_data_format`）：`{restaurantName}`、`{orderId}`、`{userName}`、`{otp}`（= 取餐号）、`{tokenNumber}`、`{tableNumber}`。⚠️ 值为空时不替换、会原样显示，别用没传值的占位符。

### 9.7 商家端推送文案与送达现状（2026-06-17）

商家端通知文案同样存 `notification_messages`（`user_type='restaurant'`）。2026-06-17 已补 3 条会用到的 key：`restaurant_order_notification`（新订单）、`restaurant_account_block` / `restaurant_account_unblock`（平台暂停 / 恢复店铺）。一键重建：`php artisan db:seed --class=NezhaRestaurantNotificationMessageSeeder`。

- **故意没补**的 restaurant key：提现批准 / 驳回（`restaurant_withdraw_*`—— B 方案平台不碰钱、无提现，填了会与合规模型冲突、误导商家）、活动 / 广告 / 订阅类（MVP 未启用）。
- vendor 语言是 `zh`（注意：不是顾客的 `zh-CN`），文案基础列存中文兜底 + `zh` / `en` 译文。

🔴 **重要：补了文案 ≠ 商家就能收到新订单推送。** 新订单的 FCM 发送在 `Helpers::sentRestaurantNotification()`，当前 B 方案下三条送达渠道都还没通：

| 渠道 | 现状 | 要通需要 |
|---|---|---|
| FCM 设备推送 | ❌ 所有 vendor `firebase_token` 为空 | 商家在网页端授权浏览器通知后、前端把 token 上报到后端 |
| FCM 网页 topic 推送 | ❌ 发送条件写死 `order_confirmation_model=='deliveryman'` + 支付 `cash_on_delivery`，与实际（`restaurant` + offline_payment）不符，整段不触发 | 改 `sentRestaurantNotification()` 的门槛条件（属机制改动，需评估后再动） |
| Telegram 新单提醒（哪吒自建，最现成） | ⚠️ 平台 `telegram_bot_token` 已配，但**没有一家餐厅填 `telegram_chat_id`** | 给每家店的 `restaurants.telegram_chat_id` 填上其 Telegram chat id |

- **最快通路 = Telegram**：哪吒已内置 `sendTelegramOrderAlert()`，新单（pending / confirmed）即推一张中文卡片（单号 / 类型 / 合计 / 商品 / 时间），独立于上面 FCM 那些门槛。只要把每家店的 `telegram_chat_id` 填上即生效，**无需改代码**。

> 🟢 **补充（别被上表吓到）**：商家后台页面本身已有「**manual 轮询新单提示音**」（`order_notification_type=manual`，当前已启用）——商家**登录并停留在后台订单页**时，每约 6 秒自动检测，有新单即响一次铃 + 在屏幕右下角弹出一个不挡屏的常驻提示条（「🔔 X 个新订单待收款/待处理 · 立即接单」，点它直达对应订单列表——离线待收款单去「待确认收款」、现做/自取单去「待处理」；2026-06-17/18 两次改造：① 同一笔单按订单 id 去重只响一次、忙时不再被反复弹窗打断、提示条可关闭（原为每 10 秒反复弹挡屏弹窗+反复响铃、点确认跳「全部订单」要翻找）；② 横幅计数改为与侧栏徽标同口径、一次只聚焦最优先的桶，横幅数字 = 点进去那个列表的单数，根除原先「横幅报数但列表空」的幽灵数/死胡同），**不依赖上面任何 FCM 件**。所以「面板开着」场景已覆盖；FCM 网页推送只多出「标签页关了但浏览器还开着」这种边角，与 Telegram 重叠且更脆弱，**已决定不做**。商家提醒最终方案 = ① 后台页面 manual 提示音（店里常开）＋ ② Telegram（手机，见 9.8）。
> 真实店「亚美零食槟榔」(id=12) 的 Telegram 已于 2026-06-17 绑定并真机验证通过。

### 9.8 启用商家 Telegram 接单提醒（操作步骤）

让某家店在手机上即时收到新订单（最稳的商家提醒渠道，独立于网页 / App 推送）：
1. 让商家用手机 Telegram 搜索并打开机器人 **&#64;Nz_order_bot**，点「开始 / Start」，随便发一句（如「你好」）。
2. 后台进该餐厅：左侧「餐厅」→ 打开餐厅 →「**设置**」标签 → 找到「**Telegram 接单提醒**」卡片。
3. 点「**查看最近联系机器人的会话**」→ 列出刚给机器人发过消息的会话及其 chat id → 点对应那条的「**填入**」（或手动把 chat id 粘进输入框）。
4. 点「保存」。完成。之后该店每来一笔新订单（pending / 确认），机器人私聊推一张卡片：单号 / 类型 / 合计 / 商品 / 时间。
- 留空 = 不发；每笔订单只推一次（已去重）。
- 群发：把机器人拉进店员群、填该群的 chat id（负数）即可，全店都能收到。
- 目前仅平台运营在后台设置；商家端自助绑定后续可做。
- 实现：`Helpers::sendTelegramOrderAlert()`（发送）+ 后台「设置」页卡片（`admin.restaurant.update-telegram` / `admin.restaurant.telegram-recent-chats`）+ 字段 `restaurants.telegram_chat_id`。

## 10. 佣金充值管理（商家预存佣金）

**入口**：后台左侧栏「交易管理」区 →「佣金充值管理」（`/admin/nezha-deposit`）。

**这是什么**：B 方案下平台不碰顾客的钱，平台收入靠向商家收佣金。商家先把佣金「预存」给平台，平台按每单自动扣佣；余额不足会暂停该商家接单。本页用于查看每个商家的预存佣金余额、累计充值/扣佣/退还，并在商家把钱打给平台后**记录充值**。

**一览页能看到**：
- 顶部 4 个汇总卡：全平台预存佣金总额、欠款商家数（余额为负）、累计充值、累计扣佣。
- 每个商家一行：当前余额（**负数红色显示「欠款」**=该商家欠平台佣金）、累计充值、累计扣佣、累计退还、上次充值时间。

**怎么记录一笔充值**（商家把预存佣金转给平台后）：
1. 在该商家行点「记录充值」。
2. 填「充值金额」（= 商家实际打给平台的金额）、可填备注（如「微信转账 6/16 / 凭证号」）。
3. 点「确认入账」→ 商家余额立即增加并写入流水（留痕操作人）。

**全部流水**：点右上「全部流水」或某商家行的「流水」，可按商家 / 类型（充值 / 扣佣 / 退款返还）筛选。

**说明**：
- 充值是合法 B2B（平台向商家收佣的预付），不涉顾客资金、不构成二清（合规 L1-1/L1-5）。
- 扣佣只在业务设置「预存佣金扣佣开关」(nezha_deposit_mode_status) 打开后才发生；当前默认关闭（免佣免押拉量阶段）。
- 余额可为负（= 商家欠平台佣金，已确认采用此策略）；负余额触发停止接单，商家充值后恢复。

## 11. 可用性监控（站外 uptime + 服务器自检 nzwatch）

哪吒有**两层**“网站还活着没”的监控，分工互补：

**第一层 · 服务器自检 nzwatch（站内）**
- 跑在哪吒服务器自己身上，cron 每 5 分钟自查（负载 / 进程 / 证书 / 服务存活等），有事经 nzwatch_mail.py 发邮件到 noctis201914@gmail.com。
- 盲区：它住在服务器里——**一旦服务器宕机 / 断网 / 被墙，它自己也发不出告警**。第二层就是补这个盲区的。

**第二层 · 站外 uptime 监控（HetrixTools，站外）**〔2026-06-17 新增〕
- 服务：HetrixTools 免费档，后台 https://hetrixtools.com ，登录邮箱 noctis201914@gmail.com。
- 原理：从**哪吒服务器外面**（欧洲 4 地：阿姆斯特丹 / 法兰克福 / 伦敦 / 华沙）每 1 分钟拨测哪吒的公开入口；连续失败就发告警邮件到 noctis201914@gmail.com。服务器整体挂掉时，它在外面照样能告警。
- 当前监控 7 个公开入口：

| 名称 | 拨测对象 | 校验什么 |
|---|---|---|
| Nezha-1-Frontend-Home | 前端首页 | 页面外壳正常 |
| Nezha-2-Merchant-Login | 商家登录页 | 打得开（HTTP 200） |
| Nezha-3-API-Config | 后端配置接口 | 返回真实配置 |
| Nezha-4-Restaurants-NonEmpty | 餐厅列表接口 | 餐厅列表非空（防“能开但列表空”假活） |
| Nezha-5-Products-Popular | 热门菜品接口 | 菜品非空 |
| Nezha-6-Banners | 首页活动 banner 接口 | banner 非空 |
| Nezha-7-LocalLife | 本地生活帖子接口 | 接口正常 |

**你要做什么**
- 平时不用管，后台自动盯着。
- **收到 HetrixTools 告警邮件 = 哪吒可能真出事了** → 去查服务器或找技术处理。两层都静默 = 一切正常。
- 建议把 HetrixTools 的发件地址加进 Gmail 联系人 / 白名单，免得告警被丢进垃圾箱。

**🔴 上线注意（demo 演示数据回退时）**
- Nezha-4 / 5 / 6（餐厅 / 菜品 / banner 的“非空”检查）现在能通过，是因为当前有 demo 演示数据撑着。**上线前跑 demo 回退脚本清空演示数据后，这三个会因“列表空”而告警**，直到真实商家 / 菜品 / 活动录入。
- 处理：回退 demo 数据的那段时间，要么在 HetrixTools 后台把这 3 个监控临时**暂停（Pause）**，要么心里有数——这几封告警是预期的，等真实内容上线后会自动恢复绿。

**改 / 加 / 停监控**
- 登录 HetrixTools → 左侧 Uptime Monitors，可加 / 暂停 / 删除。免费档上限 15 个，目前用了 7 个。
- 告警发到哪个邮箱：在 Contact Lists → Default Contact 里改。
- 用 API 批量操作需要 API Key（Account → API Keys，免费档最多 2 个，用完即删作废）。


---

## 12. 顾客下单需登录（游客结算已关闭）〔2026-06-18〕

**现状：顾客必须先登录（邮箱 / Google / Apple）才能下单。** 游客（未登录）可以浏览、加购物车，但点「去结算」会弹登录框；登录后购物车自动保留并继续下单。

**为什么这么定**：游客没账号 → 没有「我的订单」记录（订单页要登录才看）、退款难追溯付款人、无法留存复购。登录一键完成、摩擦很低，所以下单统一要求登录。

**开关在哪 / 怎么改回允许游客下单**：
- 业务设置 `business_settings.guest_checkout_status`，当前 = `0`（关）。改成 `1` 即恢复允许游客直接下单。
- ⚠️ 改完不会立刻生效：`/config` 接口有 60 秒的服务器微缓存，约 1 分钟后前端才会拿到新值。
- ⚠️ 若真要恢复游客下单，还需配套恢复后端"游客用前端购物车下单"的逻辑（已回退）+ 想清楚游客订单记录怎么给顾客查——否则游客下单又会下不了/查不到。建议保持关闭。

**配套机制（顺带说明，无需操作）**：顾客登录 / 注册时，会自动把登录前加的购物车合并进账号（不会因登录丢车）。

---

## 13. 邮件系统（域名发信 / 客诉收信）〔2026-06-18 上线〕

平台所有邮件现在都用自有域名 **nezha.am**，对外不再暴露任何 Gmail。**日常你只需要登录并查看一个邮箱：`noctis201914@gmail.com`**，其余都是自动的。

### 13.1 一张表看懂

| 场景 | 顾客看到 | 实际在哪 |
|---|---|---|
| 哪吒发给顾客（验证码 / 找回密码 / 订单通知）| 哪吒外卖 `<noreply@nezha.am>`（带 logo）| 系统自动发，不用管 |
| 顾客发客诉 / 回复哪吒的邮件 | 发到 `support@nezha.am` | 自动转进 `noctis201914@gmail.com` |
| 你回复客诉 | 收到来自 `support@nezha.am` | 你在 Gmail 里点回复，自动以 support@ 身份发出 |
| 隐私政策 / 用户协议 / 风控页的联系邮箱 | `support@nezha.am` | 同上 |

### 13.2 用到的三个账号（别混）
- **Resend**（负责"发信"）：https://resend.com ，登录账号 `noctis201911@gmail.com`，域名 nezha.am 已验证。
- **Cloudflare**（负责"收信转发" + 域名 DNS）：登录账号 `noctis201911@gmail.com`。
- **收发邮件的工作邮箱**：`noctis201914@gmail.com`（客诉最终落这里、你回信也在这里操作）。
> 后台登录用 201911、日常收发邮件用 201914，是两个不同用途，记清楚。

### 13.3 发信（Resend）—— 一般不用动
- 发件人固定显示「哪吒外卖 \<noreply@nezha.am\>」，邮件顶部带哪吒 logo。
- 技术配置在服务器 `.env`（MAIL_* 走 smtp.resend.com 587 端口），不在后台页面，运营无需操作。
- noreply@ 是"只发不收"地址，顾客不会往它回信（回信地址已设成 support@）。

### 13.4 收客诉（Cloudflare 邮件路由）—— 已配好
- 顾客发到 `support@nezha.am` 的信，由 Cloudflare 自动转发到 `noctis201914@gmail.com`。
- 规则位置：Cloudflare → 选 nezha.am → 左侧 Email → Email Routing → Routing rules，能看到 `support → noctis201914@gmail.com`。
- 想加别的地址（如 `sales@nezha.am`）：同一页 Create address，转发到你的 Gmail 即可。

### 13.5 你回复客诉时自动用 support@（已在 Gmail 配好）
- Gmail 已设置「以 support@nezha.am 身份发送（Send mail as）」+「回复时用收件地址回」。
- 所以你直接在 Gmail 里对客诉点「回复」，发出去的发件人就是 support@nezha.am，顾客看不到你的 Gmail。
- 设置位置（如需检查）：Gmail → 设置（齿轮）→ 查看所有设置 → 账号和导入 → Send mail as / When replying to a message。

### 13.6 🔴 免费额度（要盯）
- Resend 免费档：**100 封/天、3000 封/月**。发给顾客的邮件 + 你回客诉都算在内。
- 现在量小没问题。**上线后订单多了**，去 Resend 后台 **Logs** 看每天发了多少；接近 100/天 就升级 Resend 付费档（或换 Brevo，免费 300/天）。

### 13.7 出问题怎么查
- **顾客说收不到验证码 / 通知**：登录 Resend → Logs，搜该顾客邮箱，看状态——Delivered=已送达、Bounced=被退回（地址错）、没记录=系统压根没发出。
- **客诉没转进 Gmail**：检查 Cloudflare → Email Routing 规则还在不在、目标地址 `noctis201914@gmail.com` 是否仍是 Verified（绿色）。
- **🔴 这些 DNS 记录千万别删**（Cloudflare → nezha.am → DNS）：
  - `send` 的 MX/TXT、`resend._domainkey` 的 TXT、`_dmarc` —— 发信认证，删了邮件会进垃圾箱或发不出。
  - 根域名的 MX（route1/2/3.mx.cloudflare.net）、`v=spf1 ... cloudflare` 的 TXT —— 收信用，删了 support@ 收不到信。

### 13.8 密钥安全
- Resend 的 API 密钥（`re_...`）存在服务器 `.env` 和 Gmail 的 Send-mail-as 设置里，**不要外泄**。
- 怀疑泄露时：Resend → API Keys 删旧建新（权限选 Sending access、限 nezha.am），再更新服务器 `.env` 的 MAIL_PASSWORD + Gmail Send-mail-as 的密码。这一步可让 AI 帮忙改服务器。

## 14. 配送申诉「没有收到餐 / 配送异常」处理〔2026-06-19 上线〕

B 方案下平台不自营配送（〔2026-06-19 起配送由商家代叫 Yandex〕 / 顾客自取）、也不经手款项，所以顾客在「订单完成页」可对配送结果提出**专用申诉**（区别于通用退款申请）。本功能**只做留痕 + 引导顾客联系商家**，平台**不会自动退款**——真实退款仍由商家原路退回（L1-1 / L1-2）。

**顾客侧**：完成页「没有收到餐」入口 → 选原因 + 补充说明 → 提交。顾客会看到：申诉时限、需要准备的证据、预计处理时间，并被引导直接联系商家 / 客服。

**运营侧（你要做的）**：
- 申诉**留痕在数据库表 `nezha_delivery_appeals`**（字段：order_id / user_id / reason_code / detail / status / sla_due_at / created_at）。同时在 `logs` 表记一条 `delivery_appeal_opened` 审计。
- ⚠️ 目前**没有自动推送提醒**给商家或运营——需运营**主动查表跟进**（建议每天扫一次 `status='open'` 的记录）。后续可再接通知渠道。
- 处理流程：核实是否真已配送 → 联系商家 → 推动商家原路退回 → 把记录 `status` 改为 `merchant_contacted` / `resolved` / `rejected`（暂经 DB，后续可做后台页）。
- **绝不代退**：平台不碰钱，任何退款都必须商家用顾客原支付方式退给顾客本人。

**可调设置（L2，`business_settings` 表，不改代码）**：
- `nezha_appeal_window_hours`：申诉窗口，默认 `48`（送达后多少小时内可申诉）。
- `nezha_appeal_resolve_hours`：处理时限 SLA，默认 `72`（写入 `sla_due_at`）。
- `nezha_appeal_processing_text`：顾客端展示的「预计处理时间」文案。

> 配套：完成页还会展示「状态来源」（你已确认收货 / 系统超时自动完成 / 商家标记）、配送责任方、可获得的配送凭证，以及支付凭证 / 收据（敏感账号已脱敏）。配送订单若无真实配送事件，如实标注「平台未接入第三方配送轨迹 / 历史订单无配送轨迹」，不伪造节点。

## 15. 订单超时兜底（杜绝订单卡在待接单/备餐中/配送停滞）〔2026-06-19 上线〕

防止订单无限停留在「待接单」或「备餐中」。系统每分钟自动巡检，按下面阈值处理（规则全文见后端 `docs/ORDER_TIMEOUT_RULES.md`）。

**三个阶段与自动动作**
- 付款凭证审核中（顾客已下单、等商家确认收款）：
  - 顾客**未上传**付款凭证 → 满 10 分钟系统自动取消（没付钱，安全）。
  - 顾客**已上传**付款凭证 → 满 10 分钟自动邮件提醒商家、满 20 分钟自动取消并邮件通知商家「按原路退款」。
- 付款已确认、等商家接单：满 10 分钟邮件提醒商家、满 20 分钟自动取消并通知商家退款。
- 备餐中：超过商家填的「预计出餐时间」+15 分钟（或商家根本没填出餐时间）→ 标记为备餐异常并升级客服（邮件商家 + 客服信箱），**不自动取消**。
- **配送阶段（仅配送单）**：出餐后超 45 分钟仍未进入配送、或配送中超 90 分钟仍未送达 → 顾客端显示诚实升级提示并引导联系商家/客服。**只展示、不自动取消**（饭已出/在配送、钱已付，取消风险最高）；平台不接入第三方配送实时轨迹，**不伪造骑手、不声称第三方在重试、不给虚假到达时间**。若后台无该状态的真实时间记录则如实显示「无后台时间记录」，不报假超时。

**平台不碰钱（红线）**：自动取消**绝不**由平台退款。顾客若已付款，系统只生成「待退款」留痕并通知商家原路退给顾客；顾客端显示的退款方式永远是「联系商家原路退回」，绝不写「平台已退款」。

**开关与阈值（`business_settings` 表，不改代码）**
- `nezha_timeout_status`：总开关，默认 `1`（开）。设 `0` = 只在顾客端显示超时提醒，不执行任何自动动作（出现误判/需要暂停时用）。
- `nezha_timeout_email_merchant_min`（默认10）/ `nezha_timeout_cancel_min`（默认20）/ `nezha_timeout_unpaid_cancel_min`（默认10）/ `nezha_timeout_remind_min`（默认5）/ `nezha_timeout_prep_orange_min`（默认5）/ `nezha_timeout_prep_red_min`（默认15）/ `nezha_timeout_handover_min`（默认45，出餐后未进入配送）/ `nezha_timeout_picked_min`（默认90，配送中未送达）。单位均为分钟。

> 顾客在订单追踪页会看到：当前状态、系统下一步、处理截止时间、退款方式与预计到账说明（均由后端统一下发，前端不自行计时）。

## 16. 评价审核（带图评价 UGC）〔2026-06-20 上线〕

顾客评价现在可以**带图**。为防止违规/隐私图片直接公开，**带图评价不会立刻显示**，要先审核通过。

### 16.1 规则一句话
- **纯文字评价**：提交后**立刻公开**，不用审核。
- **带图评价**：提交后进**待审核**，你（或 AI 预审）**通过后才公开**并计入店铺评分。
- 评价里的违禁词（和本地生活同一套词库）会被直接拦下。

### 16.2 在哪里审
后台 **商品 → 评价列表**（`/admin/foods/reviews`）。顶部有筛选：**全部 / 待审核(带红色数字) / 已驳回**。点「待审核」只看要处理的带图评价。

### 16.3 怎么审（每条带图评价做这个判断）
1. 看评价的图片缩略图（点开看大图）。
2. **通过**：图是正常的菜品/到店照片 → 点「**通过**」。评价立即公开，并把这条评分计入店铺。
3. **驳回**：图里有**身份证、人脸、车牌、他人隐私**，或与餐无关/违规 → 点「**驳回**」，填一句理由。
   - 驳回后这条评价**不公开**，**图片会被自动删除**（保护隐私），评价记录保留备查。

> 🔴 **隐私底线**：只要图里能看清**身份证号/证件/清晰人脸/他人联系方式**，一律驳回。宁可错杀。

### 16.4 顾客举报的评价
顾客在评价卡上可以「举报」（垃圾广告/辱骂/虚假/泄露隐私/其他）。举报记录进 `nezha_review_reports` 表。当前**没有自动通知**，需要时让运营/技术查这个表（status=0 未处理）；要下线某条评价，到评价列表把它的显示开关关掉即可。

### 16.5 AI 预审（可选，省人力）
带图评价多了以后，可以让 AI（在对话里）先过一遍待审核队列：正常图自动通过、明显违规自动驳回（理由标【AI预审】）、拿不准的留给你人工。**最终把关责任仍在你**。这走交互额度，不是套餐自动值守。
