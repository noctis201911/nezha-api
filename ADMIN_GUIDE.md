# 哪吒外卖 — 后台操作手册

> 维护约定：每次后台新增功能后，在对应章节补充说明，随功能 commit 一起入库。
> 最新版读取：`node nz.js run "cat /www/wwwroot/api.nezha.am/ADMIN_GUIDE.md"`

---

## 目录
1. [登录与导航](#1-登录与导航)
2. [商家管理](#2-商家管理)
3. [订单管理](#3-订单管理)
4. [风控中心](#4-风控中心)（2026-06-12 新增）
5. [业务设置](#5-业务设置)
6. [后台安全（两步验证 / 凭证自动清除）](#6-后台安全)（2026-06-14 新增）
7. [商家入驻申请](#7-商家入驻申请)（2026-06-14 新增）

---

## 1. 登录与导航

- 后台地址：`https://api.nezha.am/login/admin`
- 商家端地址：`https://api.nezha.am/login/restaurant`
- 账号：`noctis201911@gmail.com` / 密码：见私密记录（不入库）
- 侧边栏主菜单：订单 / 商家 / 用户 / 财务 / 设置

### 1.1 登录验证码（2026-06-14 改为系统图形码）

后台和商家端登录页现在用**系统自带的图形验证码**：照着图片里的字符输进去即可，看不清点图片右侧的**刷新图标 ↻** 换一张（不区分大小写）。

- 原先用的是 Google reCAPTCHA，但配置里填的是 Google **公开测试 key**（谁都能过），等于没防护，已**关闭**。
- 改回系统图形码后是真实有效的人机校验，后台 + 商家端两个入口都生效。
- **想换回 Google reCAPTCHA**（需自行到 Google 申请**真实** key）：业务设置 → 第三方配置 → reCAPTCHA，填真实 `site_key` / `secret_key`、状态改为启用；若 key 无效或仍是测试 key，会自动回退到图形码。

---

## 2. 商家管理

（待补充）

---

## 3. 订单管理

- 路径：侧边栏 → **订单**
- 可查看所有订单状态、手动确认、退款操作

---

## 4. 风控中心

> 2026-06-12 上线。总开关当前 **关闭（= 0）**，菜单价按美元重录后再开启。

### 4.1 路径

侧边栏 → **订单** → **风控中心** → 三个子菜单：

| 子菜单 | URL | 说明 |
|---|---|---|
| 审核队列 | `/orders/nezha-risk/queue` | 待人工处理的可疑订单 |
| 风控日志 | `/orders/nezha-risk/logs` | 全部被拦截记录（含已处理） |
| 风控设置 | `/orders/nezha-risk/settings` | 调阈值 / 开关 / 客服联系方式 |

---

### 4.2 当前设置值

| 设置项 | 当前值 | 含义 |
|---|---|---|
| 总开关 | **0（关闭）** | 改为 1 启用风控 |
| 单笔上限（法币） | **$500** | 超过直接拒单，不可人工放行 |
| 单日累计（法币） | **$2000** | 超过转人工审核队列 |
| 单笔上限（USDT） | **$500** | 同法币逻辑 |
| 单日累计（USDT） | **$2000** | 同法币逻辑 |
| 客服联系方式 | nezhadelivery@gmail.com | 显示在拒单/审核提示里 |
| 放行宽限期 | 60 分钟 | 客服放行后顾客重新下单的免检时间 |

---

### 4.3 审核队列操作

触发"单日累计超 $2000"等审核规则时，订单进入队列，**收款码不显示给顾客**。

操作：

| 按钮 | 含义 |
|---|---|
| **放行** | 本次豁免，顾客可重新下单（宽限期内同用户不再审核） |
| **退款通知** | 标记为退款处理，记入日志 |
| **清退** | 拒绝并标记高风险 |

---

### 4.4 开启风控的前提（三项都满足再改总开关）

1. 菜单价已按**美元**重录（现在德拉姆量级的价格会误触 $500 上限）
2. 在"风控设置"确认阈值合理
3. 进入风控设置，将**总开关从 0 改为 1**，保存

---

### 4.5 风控作用范围

- **全平台统一阈值**：所有餐厅、所有商品共用同一套限额
- **不支持**按单个餐厅或单个商品设置不同阈值（如需定制需后续开发）
- 游客用户：只检测单笔金额和金额特征，不检测频次（无稳定身份）
- 登录用户：全部5条规则都检测

---

## 5. 业务设置

### 5.1 货币（2026-06-14 设为人民币 ¥）
全站金额单位现在是 **CNY（人民币，符号 ¥）**，商家后台、顾客端、报表里的金额都按这个符号显示。（顾客直付商家走人民币/USDT，故默认人民币。）
- 之前没设置，系统默认回退成美元 $；现已设为 CNY。
- **在哪里改**：后台 业务设置 → 货币 / Currency（对应业务设置项 `currency`，值 = `CNY`）。
- ⚠️ 改币种**只换显示符号、不换金额数字**：商品价格、订单金额这些数字本身不会被自动换算。所以菜价请直接按"人民币"录入（一份兰州拉面填 `30` 就是 ¥30），不要填别的币种数额。
- 图表纵轴显示"¥(货币)"，正常。

### 5.2 加料分类（加料 / Add-on 必须先有分类才能建）
商家新建"加料 / 加项"时，"主分类"是必填项；这个分类列表由**总后台统一维护**，商家端只能选、不能新增。
- **在哪里维护**：后台 加料管理 → 加料分类（Addon Category），可新增 / 改名 / 停用。
- 2026-06-14 发现线上一个分类都没建，导致商家"主分类"下拉全空、加料根本建不出来；已先建一个默认分类「通用加料」兜底，商家现在能正常建加料了。
- 你可以把它改名，或按需要再加（如「辣度」「小料」「配料」等）；被停用的分类不会出现在商家下拉里。

---

## 6. 后台安全

### 6.1 两步验证 (2FA) — 强烈建议开启
给后台登录在密码之外加一道手机认证器动态码，密码即便泄露别人也进不来。

**怎么开启：**
1. 登录后台后，浏览器打开：`https://api.nezha.am/admin/two-factor/setup`
2. 用手机装一个认证器 App（Google Authenticator / 微软 Authenticator / 1Password 均可）。
3. 用 App 扫页面上的二维码（扫不了就手动输下面那串密钥，类型选"基于时间/TOTP"）。
4. 把 App 上显示的 6 位码填进"第二步"输入框，点"确认并启用"。
5. **页面会显示 8 个一次性恢复码——立刻抄下来存好（密码管理器/纸条）。这是认证器丢了时的备用钥匙，关掉页面就再也看不到。**

**以后登录：** 输完邮箱密码后，会多一步要你输认证器上的 6 位码。

**认证器丢了 / 被锁在外面怎么办（三选一）：**
- 用一个**一次性恢复码**代替 6 位码登录（每个只能用一次）。
- 都没有了：让懂技术的人通过服务器 SSH 执行一条命令关闭你的 2FA：
  `cd /www/wwwroot/api.nezha.am && php artisan nezha:2fa-disable 你的后台邮箱`
  执行后即可仅用密码登录，再重新设置 2FA。

**关闭 2FA：** 在同一设置页输入当前登录密码确认即可关闭。

> 说明：2FA 默认是关的，开不开由你决定；不开的话登录跟以前完全一样。

### 6.2 顾客付款凭证自动清除（隐私合规）
顾客上传的付款凭证（转账截图/付款信息）属个人数据，系统会**每天自动清除超过保留期的旧凭证**（只抹凭证内容+截图，保留订单/交易记录供对账），满足"个人数据到期删除"的合规要求。
- **保留天数**：默认 **90 天**。如需调整，改业务设置项 `nezha_payment_proof_retention_days`（数字，单位天）。
- 链上/交易/审计记录按合规另行留存 ≥5 年，不在自动删除范围内。

### 6.3 数据库加密 + 每日加密备份（已自动运行，无需后台操作）
数据库里所有顾客信息（姓名/电话/邮箱/地址/坐标/证件号等）现在在硬盘上是**加密存储**的，并且**每天凌晨自动做一份加密备份**。这两件事都在服务器后台自动跑，**后台界面里没有按钮要点**。

**🔑 你唯一要做的一件事：把"备份钥匙"另存一份到安全的地方。**
- 备份钥匙就是服务器上这个文件：`/root/.nezha/backup.key`
- **这把钥匙丢了，那些加密备份就永远打不开了。** 请让懂技术的人把它的内容复制出来，存到你的密码管理器 / U盘 / 离线安全处各一份。
- （数据库本身的加密钥匙在 `/www/server/mysql-keyring/`，由服务器自动管理，正常不用动；换服务器时才需要它，已有备份。）

**备份在哪、留多久：**
- 位置：`/www/backup/database/nezha-enc/`，文件名形如 `sql_api_nezha_am-日期.sql.gz.enc`
- 频率：每天 04:17 自动生成一份，**保留最近 14 份**，旧的自动删。
- 备份是加密的，所以就算这些备份文件被人拷走，没有上面那把钥匙也打不开。

**怎么用备份恢复数据（出事时给懂技术的人看）：**
- 完整步骤写在服务器文件 `/root/nezha-backup/README-RESTORE.txt` 里。
- 简版：用钥匙解密 → 解压 → 导回数据库：
  `openssl enc -d -aes-256-cbc -pbkdf2 -pass file:/root/.nezha/backup.key -in 备份文件 | gunzip > /root/restore.sql`，确认无误后 `mysql` 导入。

**这套加密防什么、不防什么（实话）：** 防的是**硬盘或备份文件被偷/被拷走**；**防不了**服务器本身被黑（程序或 root 被攻破时数据仍是明文可读）。所以它是合规要求的"静态加密"那一层，不能替代日常的账号、密钥、服务器安全。
- 手动预演（看会删什么、不真删）：`php artisan nezha:purge-payment-proofs --dry-run`

---

## 7. 商家入驻申请

顾客端 H5「我的 → 商家入驻」填写的入驻意向，会进到这里。这是 **MVP 的人工入驻流程**：商家先留资料，你人工联系、审核、配保证金和自收款账户，再到「商家管理」正式开店。**平台不在这一步碰任何钱。**

### 7.1 在哪里看
- 路径：后台侧边栏 → **商家入驻申请**（在"帮助与支持"那一组下面）
- 每提交一条，系统会**自动发一封邮件到 `noctis201914@gmail.com`**，所以你不用一直盯后台；邮件里有店名、联系人、电话、微信、地址、品类、备注。

### 7.2 怎么处理一条申请
1. 列表里点 **查看**（眼睛图标）看详情。新申请会标红色「新」字。
2. 右侧「跟进状态」选一个再点**保存状态**：
   - **待跟进**：刚提交、还没联系（默认）
   - **跟进中**：已经在联系/谈了
   - **已完成**：已正式开店或确定合作
   - **无效**：重复、乱填、不合作
3. 确认是垃圾信息可点**删除**（红色垃圾桶）。

### 7.3 注意
- 这里只是**线索登记**，不会自动创建商家账号；正式开店仍在「商家管理」里手动建。
- 电话是顾客 PII，列表里直接显示方便你联系；数据库整体已加密存储（见 6.3）。
